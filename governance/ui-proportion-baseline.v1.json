{
  "$schema": "./ui-proportion-baseline.schema.json",
  "version": 1,
  "description": "Chrome proportion shrink-only ratchet baseline (tempdoc 697). ONE authority for the per-ui-shot-step persistent-chrome elements whose rendered HEIGHT must not GROW beyond a recorded ceiling (maxHeightPx). Shrinking is always allowed; growth beyond maxHeightPx + tolerancePx is a violation, enforced by `jseval ui-proportion-gate` (which reuses the ui-shot `.measure.json` geometry — see scripts/jseval/jseval/ui_measure.py). Regenerate via scripts/jseval/experiments/regen_proportion_baseline.py. ACTIVE (tempdoc 697 activation): both target elements were DATA-DEPENDENT (a degraded readiness verdict for the collapsed pill; a rendered turn for the one-line user bubble) and are now captured deterministically by the dedicated isolated `chat-proportion` ui-shot step (scripts/jseval/jseval/ui_check.py), which installs the `degraded` fixtures variant (ui_fixtures._status_body — flips `readiness.composites.retrieval` to DEGRADED with `worker.health.embedding_not_ready`, a 'warn'-severity LifecycleReasonCode so the banner renders collapsed) and actually submits a chat turn (a `?`-bearing draft + Enter, escalating to UnifiedChatView.send()). `chat-mode` (the shared chain step) is untouched — it documents chat-INPUT mode only and never submits, so it was not repurposed. Both elements' `maxHeightPx` were filled in by regen_proportion_baseline.py from this step's live `--fixtures` capture; with the top-level `tolerancePx` of 2px folded in, the effective ceilings are 44px (pill) and 38px (bubble).",
  "captureState": "ui-shot --fixtures (deterministic); the `chat-proportion` isolated step (degraded status fixture + a submitted chat turn) — see tempdoc 697; the `chat-occlusion` isolated step (1250x800, document pane opened, a toast burst at exactly the cap) — see below. The 814 closure adds `chat-bands-detailed`: the same band family in DETAILED disclosure (the `degraded-detailed` fixtures variant — identical to `degraded` except it does NOT flip `ui.mode` to simple, so the app boots into Advanced and the banner force-expands) at 1366x900, ABOVE the 820px block-axis breakpoint so Detailed genuinely expands, with one overflowing user turn so the conversation actually scrolls. Round 8 moved that step from 1050x800 to 1250x800: the wide breakpoint became a `@container` query on the chat SURFACE box (viewport minus the Shell rail minus the surface padding), so 1050 no longer mounts the wide grid at all and `.document-pane` would leave the capture; 1250 gives the surface ~1042px, the same just-over-1024 worst case the original width was reaching for. The `tasks-occlusion` isolated step (tempdoc 813 Slice D) captures at 1280x800 with the `indexing` fixtures variant (ui_fixtures._status_body: a live INDEXING worker with a real backlog), which is what puts the Tasks panel's aggregate card on screen — under every other fixture the worker reports the fallback state \"SERVING\", which the progress projection refuses to read, so the panel is correctly invisible.",
  "constraintKinds": "An element declares at least one of: `maxHeightPx` (shrink-only chrome ratchet, tempdoc 697); `minHeightPx` (the 814 review pass's anti-vacuity FLOOR companion to a ceiling — a band registered because it EXPANDS must not pass its ceiling by collapsing: `.degradation-banner`'s 176px Detailed ceiling was satisfied at the 34px collapsed pill, i.e. precisely in the regression the row exists to catch; same move as `minScrollableRegions` beside `maxScrollableRegions`); `minWidthPx` (a FLOOR for primary content that surrounding chrome must not starve); `maxWidthPx` (tempdoc 816 — that floor's symmetric CEILING, for an element whose function-derived bound is its CONTAINER rather than a line length: §3's `control-row` max is `fit-content`, which no `ch` number can express, so the chip row's 'does not sprawl past the reading column' is asserted physically on a pinned-viewport step, exactly like `maxHeightPx`); `inlineSizeRole` (tempdoc 816 — bind the element to a `roles` entry; the gate resolves that role's `ch` bounds against the element's OWN captured `chPx`, and a role-bound row with no floor — neither the role's `minInlineSizeCh` nor its own `minWidthPx` — is an ERROR); `mustNotOverlapSelector` (two rects must not intersect — occlusion, which no per-element size budget can express); `minShareOfSelector` (tempdoc 814 D1/D7.1 — this element's rect.h / a named denominator selector's rect.h must be >= floor, the 'owner of the sum' share assertion); `maxBottomPx` (tempdoc 814 D6/D7.2, the F5 close — rect.y + rect.h must stay within a pinned viewport height). A STEP may also declare `maxScrollableRegions` (tempdoc 814 D3/D7.2 — geometry.scrollableCount must not exceed this) and/or `minScrollableRegions` (the 814 closure audit's finding C — the anti-vacuity FLOOR: a ceiling alone passes trivially on a capture with zero scrollers, so a step whose state is supposed to overflow declares the floor too; the pair 1/1 is what makes the one-scroller rule witness a real scroller) and/or `statusFactsSingleton: true` (tempdoc 814 D5/D7.3 — every governance/status-facts.v1.json phrase must not exceed its registered maxPersistentRenders) and/or `absentSelectors` (a selector that must NOT appear in the captured geometry) and/or `requiredSelectors` (its positive twin — a selector that MUST appear; the spine pair's positive half) and/or `nonScrollableSelectors` (tempdoc 814 §D8 — a NAMED element that must be present AND must not be a scroller: §D3's direct witness, which `maxScrollableRegions` structurally cannot be because a total count cannot say WHICH element is allowed to scroll; judged off the element's own `geometry.elements[sel].scrollable` flag, computed by `ui_measure.py` with the same predicate that builds `scrollableRegions`) and/or `forbiddenVisibleText` (814 D5 review pass — literal phrases that must not render VISIBLY in this step's state, counted by the same visibility-filtered statusFacts probe with a step-scoped ceiling of 0; the chip-yield witness, which the register-wide phrase counter structurally cannot be because that duplication is cross-worded). Enforced by `jseval ui-proportion-gate`; an element with no constraint, or a selector missing from the captured geometry, is an ERROR, never a silent pass.",
  "occlusionNote": "Sandbox round 7 registered two layout defects on the `chat-occlusion` capture. (1) `.conversation` — the RAG reading column measured 102px wide (about one word per line) at 1050x800 with the document pane open, because CONVERSATION_ZONES declared its grid track `minmax(0, 50rem)` while its neighbours reserved real min-widths; the track floor is now 24rem, so the floor here is 384px. (2) `.toast` vs `.header` — the OverlayHost `.top-right` column docked at 48px, inside the chat surface's 56-88px header band, completely covering the shape badge; the dock now clears the surface header row as well as the topbar. Round 8 then showed the dock offset alone was not enough for the toast column: a `REQUIRES_ACK` toast had NO timeout, so two of them were still covering the Library header's control row ~6 minutes and several navigations later. That is fixed at the toast, not at the slot — every toast is now time-bounded (AdvisoryToastHost's TOAST_DURATION_MS / ACK_TOAST_DURATION_MS), the record keeping its durable home in the advisory inbox drawer + rail badge. HONEST RESIDUAL: the `top: calc(2.5rem + 3rem + 0.5rem)` reservation is calibrated against the CHAT surface's header band (measured y 56-88) and does NOT clear surfaces that stack a `<jf-surface-tabs>` strip above their own header — Library's header band ends at y=143 against chat's y=89, so the slot's persistent occupants (jf-provenance-badge, jf-plugin-error-overlay) can still land on a Library-shaped header. A per-surface header offset, not a single global constant, is what would close it. (3) `jf-task-list` vs the rail's bottom controls — tempdoc 813 Slice D. The OverlayHost `.bottom-left` slot docked at `left: 1rem`, i.e. INSIDE the 3.25rem rail column, so the Tasks panel covered the Settings button and the Help affordance outright; the slot now reserves the rail's band via the `--rail-width` token Shell.ts lays its grid track out with, so the reservation follows rail expansion (11rem) instead of being calibrated to one state. Registered as TWO overlap rows (panel vs Settings, Help vs panel) plus a `minWidthPx: 140` floor on `jf-task-list` itself — the NON-VACUITY companion: a hidden element is still found by `ui_measure`'s deepQuery but yields a 0x0 rect, which satisfies `_rects_overlap` trivially, so without a size constraint the two overlap rows would report clean precisely when the panel failed to render. 140px is a deliberate FLOOR, not a measured width (the panel is content-sized under a 24rem cap; measured 159px at this step — the floor was lowered from 160, which passed only on the 2px tolerance). The pre-existing `.toast` row shares this latent vacuity and does NOT yet carry a companion — noted for Lane-2/T-B rather than fixed here.",
  "rolesNote": "Tempdoc 816 — the INLINE axis joins the block axis in this register, and it joins it with a different KIND of invariant, deliberately (816 §2b). Height is zero-sum inside a viewport, so its invariant is a budget over the sum (814's share floors / chrome ceilings above). Width is handed out top-down and rows stack, so the unconsidered default is greedy 100% and the right invariant is a PER-ELEMENT functional bound in CONTENT units: an element's width claim must be justified by its function, not by available space. Hence `roles` below: each role cites the function that produces its number, element rows reference a role via `inlineSizeRole` instead of repeating it, and the number is resolved at measure time against the bound element's own rendered font (`chPx`) rather than being stored as px. THE SAME NUMBER RENDERS: each role's `token` names the CSS custom property in modules/ui-web/src/styles/tokens.css that applies it, and the gate fails on drift between the two — one authority renders AND judges (§4a.3). ANTI-VACUITY, inherited from this register's doctrine: a ceiling alone is satisfied by an element that failed to render, so a role-bound row must also carry a floor (the role's `minInlineSizeCh` or the row's own `minWidthPx`), and a selector missing from the capture stays an ERROR. ADOPTION IS BY EXPLICIT ROW, one surface at a time (§4a's adoption path) — 'every text-bearing element declares a role' is the named end-state, NOT the current rule; `pane` is declared `documentational` because the existing minWidthPx/minShareOfSelector rows already cover panes physically and force-migrating them would buy nothing.",
  "roles": {
    "prose": {
      "why": "Line-length legibility — the reading measure. External authorities cluster below this (USWDS ~75ch, Tailwind 65ch, WCAG 1.4.8 AAA <= 80ch); 88ch is the value this surface already shipped and is kept for the first instance so exactly one thing changes, with narrowing judged live (816 §5's open owner decision).",
      "maxInlineSizeCh": 88,
      "token": "--measure-prose"
    },
    "text-entry": {
      "why": "Max: what you type you immediately re-read, so an entry field takes the reading measure. Min: a typical query unwrapped (~30ch) — below that the field stops being usable for its own content, which is the floor that keeps the ceiling non-vacuous.",
      "minInlineSizeCh": 30,
      "maxInlineSizeCh": 88,
      "token": "--measure-text-entry"
    },
    "notice": {
      "why": "A notice's content box is its own text at reading measure and no more; its BACKGROUND may still bleed edge to edge (816 §2's full-bleed-chrome role: the chrome is full-width, the content is content-sized). No `minInlineSizeCh` — a notice's floor is its longest word plus its action control, which is content-dependent, so rows binding this role carry their own `minWidthPx` floor instead.",
      "maxInlineSizeCh": 88,
      "token": "--measure-notice"
    },
    "pane": {
      "why": "A space-sharing pane's function genuinely IS 'share of workspace', so its bounds belong to the container-% family (`minWidthPx` floors and `minShareOfSelector`), NOT to content units — 816 §2b's crossover case. Declared for vocabulary completeness; the `.conversation` / `.document-pane` / `jf-task-list` rows above already express it physically, so nothing is force-migrated.",
      "documentational": true
    }
  },
  "tolerancePx": 2,
  "steps": [
    {
      "uiShotStep": "chat-proportion",
      "elements": [
        {
          "selector": ".degradation-banner-collapsed",
          "maxHeightPx": 42
        },
        {
          "selector": ".message.user",
          "maxHeightPx": 36
        }
      ]
    },
    {
      "uiShotStep": "chat-occlusion",
      "elements": [
        {
          "selector": ".conversation",
          "minWidthPx": 384
        },
        {
          "selector": ".document-pane",
          "minWidthPx": 384
        },
        {
          "selector": ".toast",
          "mustNotOverlapSelector": ".header"
        }
      ]
    },
    {
      "uiShotStep": "tasks-occlusion",
      "elements": [
        {
          "selector": "jf-task-list",
          "minWidthPx": 140,
          "mustNotOverlapSelector": "button[data-surface-id=\"core.settings-surface\"]"
        },
        {
          "selector": "[data-help-affordance]",
          "mustNotOverlapSelector": "jf-task-list"
        }
      ]
    },
    {
      "uiShotStep": "chat-bands",
      "elements": [
        {
          "selector": ".degradation-banner-collapsed",
          "maxHeightPx": 42
        },
        {
          "selector": ".activity-rail",
          "maxHeightPx": 40
        },
        {
          "selector": ".composer",
          "maxHeightPx": 220
        },
        {
          "selector": ".conversation-zone",
          "minShareOfSelector": { "selector": "jf-unified-chat-view", "floor": 0.55 }
        }
      ],
      "maxScrollableRegions": 1,
      "statusFactsSingleton": true
    },
    {
      "uiShotStep": "chat-bands-detailed",
      "elements": [
        {
          "selector": ".degradation-banner",
          "maxHeightPx": 176,
          "minHeightPx": 64
        },
        {
          "selector": ".activity-rail",
          "maxHeightPx": 40
        },
        {
          "selector": ".composer",
          "maxHeightPx": 220
        },
        {
          "selector": ".conversation-zone",
          "minShareOfSelector": { "selector": "jf-unified-chat-view", "floor": 0.45 }
        }
      ],
      "minScrollableRegions": 1,
      "maxScrollableRegions": 1,
      "statusFactsSingleton": true
    },
    {
      "uiShotStep": "chat-composer-small",
      "elements": [
        {
          "selector": ".composer",
          "maxBottomPx": 768
        }
      ]
    },
    {
      "uiShotStep": "chat-spine-single",
      "elements": [],
      "absentSelectors": [".run-spine"]
    },
    {
      "$comment": "Tempdoc 814 D7.2's spine PAIR, positive half (landed in the 2026-08-06 review pass; W4 had deferred it as needing DOM changes that have since merged). Two user turns + the agent affordance MUST mount the spine — the regression `chat-spine-single`'s absence assertion cannot catch on its own (a spine that never mounts at all passes the negative row trivially). DIVISION: the capture witnesses PRESENCE; 'marker count == segment count' stays unit-tier (adaptiveSpacing / UnifiedChatView tests) because segmented nodeIds need a real agent SSE run, which `install_fixtures` stubs empty.",
      "uiShotStep": "chat-spine-multi",
      "elements": [
        {
          "selector": ".composer",
          "maxHeightPx": 220
        }
      ],
      "requiredSelectors": [".run-spine"],
      "maxScrollableRegions": 1
    },
    {
      "$comment": "Tempdoc 814 D5's chip-yield CAPTURE witness (review pass 2026-08-06). The chip yield is a cross-worded duplication — the banner says 'Semantic search degraded.' and the chip 'Service degraded' — so the phrase-singleton register structurally cannot witness it; what a capture CAN decide is that the banner-owning surface never shows the chip's wording. This step reaches the degraded chat surface WITHOUT submitting, because a submitted turn leaves `aiState.activity` stuck on 'thinking' under --fixtures (the stubbed SSE never drains) and the chip then reads 'Thinking…' whether the yield works or not — measured on `chat-bands`: 'Thinking' 1, 'Service degraded' 0, a green for the wrong reason. `requiredSelectors` is the non-vacuity companion: the assertion only means something while the banner is the authority on screen.",
      "uiShotStep": "chat-chip-yield",
      "elements": [
        {
          "selector": ".degradation-banner-collapsed",
          "maxHeightPx": 42
        }
      ],
      "requiredSelectors": [".degradation-banner-collapsed"],
      "forbiddenVisibleText": ["Service degraded"],
      "statusFactsSingleton": true
    },
    {
      "$comment": "Tempdoc 814 §D8.1 — the RECORD-path capture that closes half of §V residual 1 ('the evidence rail is fixture-unreachable'). The `agent-run` fixtures variant hangs `attributes.sources` on the record's last assistant message, and `hydrateAnswerEvidenceFromRecord` mounts `.evidence-rail` from it after the run's post-terminal thread refresh. FOUR rows, each non-vacuous only because of the others: `requiredSelectors` says the rail MOUNTED (without it every other row passes on a capture where it never rendered); `nonScrollableSelectors` is §D3's DIRECT witness, which the surface-wide `maxScrollableRegions` structurally cannot be (a capture where `.conversation` stopped scrolling and the rail started still counts 1); `absentSelectors` on `.sources-affordance` is §D5's source-count single authority on camera (the chip must not RENDER while the rail owns the count, not merely be CSS-hidden); and the share floor says §D1 still holds WITH the rail in the grid. `forbiddenVisibleText` pins the mechanism correction itself: before this variant the unmapped `/api/chat/dispatch` fell through to `{}`, both parsers saw no terminal frame, and every agent-mode capture painted 'Connection lost'.",
      "uiShotStep": "chat-evidence-rail",
      "elements": [
        {
          "selector": ".activity-rail",
          "maxHeightPx": 40
        },
        {
          "selector": ".composer",
          "maxHeightPx": 220
        },
        {
          "selector": ".conversation-zone",
          "minShareOfSelector": { "selector": "jf-unified-chat-view", "floor": 0.55 }
        }
      ],
      "requiredSelectors": [".evidence-rail"],
      "nonScrollableSelectors": [".evidence-rail"],
      "absentSelectors": [".sources-affordance"],
      "forbiddenVisibleText": ["Connection lost"],
      "maxScrollableRegions": 1
    },
    {
      "$comment": "Tempdoc 814 §D8.2 — the EXPANDED activity-rail body, the other half of §V residual 1, and the home of §D2's bounded-expansion assertion (the half the closure audit recorded as untested because no capture could open a POPULATED rail). The three body rows come from the two fixture halves: `.activity-budget` + `.activity-context` from the SSE `budget_update` (the context meter needs promptTokens AND contextWindow > 0 or `projectContextHorizon` returns null and the meter silently does not render — which is why the payload carries both), `.activity-lifecycle` from the record's DONE lifecycle. `.activity-rail` carries BOTH bounds, and the floor is the load-bearing half: this is the one step where the rail is supposed to be EXPANDED, so a ceiling alone would be satisfied exactly by the regression that collapsed it back to its 25px summary (the same anti-vacuity move `chat-bands-detailed`'s banner floor makes). Measured 86px expanded vs 25px collapsed, so a 64px floor discriminates the two with room on both sides. MEASURED, not assumed (the §D8 negative-control pass): with the `<details>` left CLOSED, all three body `requiredSelectors` rows still report ok — `ui_measure`'s deepQuery finds a collapsed `<details>`' children in the DOM — so presence alone does NOT witness expansion. `minHeightPx` is the only row that went red (UNDER_MIN_HEIGHT 25 < 64). Read the two kinds of row accordingly: the presence rows say the run REPORTED these facts, the floor says the rail is actually OPEN. SHARE FLOOR 0.45, NOT the 0.55 the sibling Simple-mode steps carry, and this is a deliberate register decision rather than a measured accommodation: §D2 says in-flow chrome is summary-height and DETAIL IS ON-DEMAND, so a user-opened disclosure is licensed to spend more of the surface — bounded, which is exactly §D1's Detailed-mode floor (0.45, the same number `chat-bands-detailed` registers for the force-expanded banner). Measured 0.4729 with the body open. What the row forbids is the thing D2 actually forbids: an expansion that keeps growing at the conversation's expense.",
      "uiShotStep": "chat-activity-rail-open",
      "elements": [
        {
          "selector": ".activity-rail",
          "maxHeightPx": 110,
          "minHeightPx": 64
        },
        {
          "selector": ".composer",
          "maxHeightPx": 220
        },
        {
          "selector": ".conversation-zone",
          "minShareOfSelector": { "selector": "jf-unified-chat-view", "floor": 0.45 }
        }
      ],
      "requiredSelectors": [
        ".activity-budget",
        ".activity-context",
        ".activity-lifecycle",
        ".evidence-rail"
      ],
      "nonScrollableSelectors": [".evidence-rail"],
      "forbiddenVisibleText": ["Connection lost"],
      "maxScrollableRegions": 1
    },
    {
      "$comment": "Tempdoc 816 §5 — the INLINE-axis camera, and the register's first role-bound rows. A WIDE viewport (1920x900) on purpose: the defect class grows with monitor width, so every existing camera (1250-1366, 814's pinned design basis) sat exactly where full-bleed stretching is least visible, and fourteen agent rounds plus a measured review pass read 'unbounded' as 'nothing broken'. Measured here BEFORE the fix, at 1920: `.composer` 1836, its textarea 1760 (251ch), `.escalation-strip-docked` 1836, the banner's content row 1804 (209ch) — while `.conversation` was already bound and centred at 800. Six rows, each non-vacuous only because of another: the textarea's role ceiling is paired with the role's own 30ch floor; the composer/strip ceilings are paired with presence floors (a hidden element yields a 0x0 rect, which satisfies a ceiling trivially — the same latent vacuity `jf-task-list` records above); `.degradation-banner`'s 1600px FLOOR is the full-bleed-chrome companion to its content row's `notice` ceiling — it says the background still bleeds edge to edge, so 'content box capped' cannot be satisfied by shrinking the whole banner; and `.conversation`'s floor keeps the alignment anchor honest. `.composer`'s height ceiling is 816 §2b's wrapping-coupling guard ON THIS CAMERA: tightening an inline bound makes wrapped content taller, so the step that tightens it watches its own height too.",
      "uiShotStep": "chat-wide-docked",
      "elements": [
        {
          "selector": ".conversation",
          "minWidthPx": 384
        },
        {
          "selector": "jf-composer",
          "minWidthPx": 320,
          "maxWidthPx": 816
        },
        {
          "selector": "jf-composer textarea",
          "inlineSizeRole": "text-entry"
        },
        {
          "selector": ".escalation-strip-docked",
          "minWidthPx": 160,
          "maxWidthPx": 816
        },
        {
          "selector": ".degradation-banner",
          "minWidthPx": 1600
        },
        {
          "selector": ".degradation-banner .notice-row",
          "inlineSizeRole": "notice",
          "minWidthPx": 200
        },
        {
          "selector": ".composer",
          "maxHeightPx": 220
        }
      ],
      "requiredSelectors": [".degradation-banner-collapsed", ".message.user"],
      "maxScrollableRegions": 1
    },
    {
      "uiShotStep": "search-v2-window",
      "$comment": "Tempdoc 818 §6g C2 — the Search v2 window enters this register. It had been BORROWING the register's numbers by prose comment for five slices (railSizing.ts derives its two floors from the `chat-occlusion` rows below) while declaring no rows of its own: the numbers came in, the judgment stayed out, and the §6c critical pass is what that cost — a layout-axis regression no unit test could see, shipped for two slices because 'the screenshot path was down'. `.rail`'s CEILING is finding 1's rendered witness: the window's three regions sit on a horizontal track, and when `.body` (the scroll-policy region) and `.win` (that track) were carried on ONE node the track inherited `flex-direction: column`, so the rail spanned the full window — SPRAWLED against a ceiling of 448, which is the code's own SESSION_RAIL_CEILING_PX ('a rail wider than twice its automatic width is no longer periphery'). `.centre`'s FLOOR is finding 7's: a remembered over-wide rail is never re-clamped against a narrower window, starving the reading column below the 384px the `chat-occlusion` row already pins for its sibling. The `.rail` minWidthPx is the anti-vacuity companion this register's doctrine requires — a rail that failed to render yields a 0x0 rect and would satisfy a ceiling trivially; 52 is the product's collapsed strip (`--rail-width`), the rail's own minimum honest form. The scroller counts are MEASURED, not derived: this window's claim is one scroller per REGION (not 814 §D3's one per surface — see the note on that citation in SearchV2View.ts), so the honest number is what the capture observes. At this step exactly one region overflows (the live results list), and the floor/ceiling pair is 1/1 so the rule witnesses a real scroller instead of passing vacuously on a capture with none. `.centre` is asserted non-scrollable because it is `overflow: hidden` BY DESIGN, which is exactly the property that makes finding 3's clipping possible — a regression that made it scroll would hide that defect rather than fix it.",
      "elements": [
        {
          "selector": ".rail",
          "minWidthPx": 52,
          "maxWidthPx": 448
        },
        {
          "selector": ".centre",
          "minWidthPx": 384
        },
        {
          "selector": ".document-pane",
          "minWidthPx": 384
        }
      ],
      "requiredSelectors": [".rail", ".centre", ".reading"],
      "nonScrollableSelectors": [".centre"],
      "minScrollableRegions": 1,
      "maxScrollableRegions": 1
    },
    {
      "uiShotStep": "search-v2-small",
      "$comment": "Tempdoc 818 §6g C2 — the short-viewport camera (1366x790, below the shared 820px block-axis breakpoint). HONEST SCOPE, because the register cannot state it and a future reader would otherwise assume it: this step does NOT witness §6c finding 3. That finding is the run CONTROLS clipped below the fold, and a run cannot be held in flight under `--fixtures` — Playwright's `route.fulfill` serves only a complete body, so a fixtured run drains to its terminal instantly (agent_stream_fixture.py:11-17, the cost 814 §D8.3 already recorded) and this window unmounts its run region at that terminal. Without the run the deck measures ~290px against a ~430px column, so a `.run-controls` row here would pass for reasons unrelated to the finding — a proxy assertion in register form. Finding 3's witness is the one-time live pass (818 §6e.5's measured-audit tier) and the law is listed `unverified` in the promotion table. What this step DOES witness is the committed state at short height, where the transcript and the deck genuinely compete for the centre column: the deck must stay within the fold, and `requiredSelectors` proves the capture actually reached that state rather than passing on an empty window. The scroller pair is 2/2, measured: at this step the transcript AND the list both overflow, and neither nests inside the other — which is the whole of this window's one-per-region claim, asserted as a number rather than as prose. A `minHeightPx` floor on `.transcript` is deliberately NOT authored: the code's short reading floor (160px) is the grip's DRAG clamp and the automatic layout enforces no such floor, so a row here would be asserting a constant that does not describe the un-dragged state. The first capture measured the transcript at 256px — comfortably above that floor, so the risk this row was reserved for does not exist AT THIS STATE. It is not retired, only relocated: the state where the sum genuinely threatens the transcript is the one with the run region mounted, which is exactly the state no fixture can reach (see the run note above), so it belongs to the live pass with the rest of finding 3.",
      "elements": [
        {
          "selector": ".deck",
          "maxBottomPx": 790
        }
      ],
      "requiredSelectors": [".transcript", ".deck", ".frozen"],
      "nonScrollableSelectors": [".centre"],
      "minScrollableRegions": 2,
      "maxScrollableRegions": 2
    }
  ]
}
