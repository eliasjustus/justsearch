{
  "version": 1,
  "$comment": "Region -> just-in-time delivery register (tempdoc 620 Move 2). Promotes scripts/agent-analytics/lib/governed-regions.mjs into data, the promotion that file's predecessor anticipated ('promote to governance/consult-register.v1.json only if it grows'). Consumed by consult-doc-hint.mjs (PreToolUse 'Consult' -> pushes governing docs when a region is edited) and maintain-doc-hint.mjs (Stop 'Maintain' -> blocks once if a region's governing doc went un-updated). Schema per row: pathIncludes (array of path substrings; matches if the edited path contains any) + region (label) + docs ([{path, why}] surfaced by consult) + maintain (optional bool; when true, maintain-doc-hint blocks turn-end if none of the docs were updated this session. Omit for consult-only DELIVERY rows that push a doc to read without demanding you maintain it -- e.g. the 616 read-0x architecture docs) + recipe (optional array of step strings; surfaced by consult as a numbered just-in-time procedure -- the relocation home for path-triggerable workflow recipes evicted from the always-loaded layer, e.g. common-workflows.md's gRPC/REST/config/agent-tool recipes, tempdoc 620 Phase 2; a recipe row typically sets docs:[] so it never blocks at Stop). A delivery row is how the always-loaded layer evicts region-specific context: load it only when the region is touched.",
  "regions": [
    {
      "id": "shell-v0",
      "pathIncludes": ["modules/ui-web/src/shell-v0/"],
      "region": "a shell-v0 frontend surface",
      "maintain": true,
      "docs": [
        {
          "path": "docs/decisions/0032-fe-lit-web-components.md",
          "why": "the frontend is **Lit web components**, not React (the React stack is retired)."
        },
        {
          "path": "docs/explanation/27-frontend-presentation-kernel.md",
          "why": "single presentation authorities (tone/originator/layout/modality); a re-authored badge/modal/transient is meant to be unrepresentable, not just discouraged."
        }
      ]
    },
    {
      "id": "surface-catalog-parity",
      "pathIncludes": ["CoreSurfaceCatalog.java"],
      "region": "the Java surface catalog (FE↔Java parity)",
      "docs": [],
      "recipe": [
        "Editing CoreSurfaceCatalog.java — run node scripts/ci/check-surface-composition.mjs. Tempdoc 852 S0 gave that gate a parity leg: a surface id declared in BOTH this file and modules/ui-web/src/shell-v0/plugin-api/CorePlugin.ts must agree on audience and placement. No other gate can see a one-sided change — the interaction-surface gate parses this file only, check-window-cutover reads CorePlugin.ts only — so moving one side alone ships a broken or duplicated USER window with every gate green. The ui-web-gates recipe already covers the CorePlugin.ts direction; this row covers the Java one.",
        "Also run node scripts/governance/run.mjs --gate surface-altitude --mode gate (a surface's altitude is DERIVED from the authorities it consumes, so a new or changed Surface(...) can create a derivation conflict)."
      ]
    },
    {
      "id": "ui-web-gates",
      "pathIncludes": ["modules/ui-web/src/"],
      "region": "the ui-web frontend (pre-merge gate set)",
      "docs": [],
      "recipe": [
        "Any modules/ui-web/src/** edit — run the ui-web gate set before merge (node scripts/ci/<name>.mjs): check-presentation-purity, check-observed-state-collapse, check-theme-token-closure, gen-token-names --check, gen-component-vocabulary --check, strip-token-fallbacks --check modules/ui-web/src, check-color-tokens, check-a11y-closure, check-controls-a11y, check-adaptive-closure, check-layout-purity, check-surface-composition, check-message-single-model, check-run-renderers, check-inflight-liveness, check-composition-surfaces, check-declared-surfaces, check-live-channels, check-contrast-matrix, check-accent-as-text, check-offline-single-sense, check-printable-keybinding-policy (with its self-test: node scripts/ci/check-printable-keybinding-policy.test.mjs).",
        "Plus the kernel gates: node scripts/governance/run.mjs --gate ambient-purity,style-literal-ratchet,atom-fork-ratchet,modality-contract,transient-arbitration,modal-arbitration --mode gate (run.mjs takes ONE id per --gate — invoke it once per id; `node scripts/ci/run-ui-web-gates.mjs` expands this list and runs the whole set, tempdoc 872).",
        "modules/ui-web/src/shell-v0/** edits additionally: check-steering-arbitration, check-search-issuance, check-verdict-derivation, check-ai-verdict-derivation, check-message-classes, check-capability-availability, check-realized-capability, check-consequence-classification, check-folder-status-derivation.",
        "shell-v0/views/** edits additionally: check-surface-task-state-retention, check-thread-event-kinds.",
        "Frontend typecheck + unit tests: cd modules/ui-web && npm run typecheck && npm run test:unit:run. (Relocated from the CLAUDE.md Pre-merge table — tempdoc 681; check-premerge-table validates these refs here.)"
      ]
    },
    {
      "id": "ui-baseline-schemas",
      "pathIncludes": ["governance/ui-a11y-baseline", "governance/ui-proportion-baseline"],
      "region": "the UI measurement baselines (a11y known-rules / chrome proportion)",
      "docs": [],
      "recipe": [
        "Editing governance/ui-a11y-baseline.v1.json or governance/ui-proportion-baseline.v1.json (or either .schema.json) — run node scripts/ci/check-ui-baseline-schemas.mjs. Both registers declare a relative $schema, and until that gate landed nothing read those pointers: ui-a11y-baseline named ./ui-a11y-baseline.schema.json from the day it shipped and the file did not exist, so the register was unvalidated while LOOKING validated. The gate asserts the pointer resolves, the schema compiles, and the document conforms — plus that no two a11y rows share a uiShotStep, which draft-07 cannot express and which would give one capture two accepted rule sets.",
        "A schema edit is an authority edit: if you add a field to a register, add it to its schema in the same change, or the gate fails on the additionalProperties: false it deliberately keeps."
      ]
    },
    {
      "id": "threat-model",
      "pathIncludes": ["modules/shell/src-tauri/tauri.conf.json", "ApiSecurityFilters.java"],
      "region": "the loopback egress / inbound-access security surface",
      "maintain": true,
      "docs": [
        {
          "path": "docs/reference/security/threat-model.md",
          "why": "the privacy guarantee ('nothing leaves your machine') and its mechanical anchors (CSP loopback-lock, Host/Origin/token filters, no telemetry) — re-verify the threat model if you change the CSP or the request-filter security plumbing."
        }
      ]
    },
    {
      "id": "app-agent",
      "pathIncludes": ["modules/app-agent/", "modules/app-agent-api/"],
      "region": "the in-product agent system",
      "docs": [
        {
          "path": "docs/explanation/22-agent-system-architecture.md",
          "why": "the agent loop, operation-substrate tool dispatch, token budget, durability, and the MCP tool surface -- read before changing agent behavior (616 read-0x delivery)."
        }
      ]
    },
    {
      "id": "adapters-lucene",
      "pathIncludes": ["modules/adapters-lucene/"],
      "region": "the Lucene retrieval adapter",
      "docs": [
        {
          "path": "docs/explanation/18-adapters-lucene-deep-dive.md",
          "why": "HNSW search, SPLADE learned-sparse retrieval, hybrid fusion, and the commit-metadata system -- the ranking-quality surface (616 read-0x delivery)."
        }
      ]
    },
    {
      "id": "worker-inference",
      "pathIncludes": ["modules/app-inference/", "modules/ort-common/"],
      "region": "the Worker inference / ORT-session composition",
      "docs": [
        {
          "path": "docs/explanation/24-worker-inference-composition.md",
          "why": "how the Worker builds ORT sessions and wires the six encoder roles onto one typed composition pipeline (616 read-0x delivery)."
        }
      ]
    },
    {
      "id": "workflow-grpc-method",
      "pathIncludes": ["modules/ipc-common/src/main/proto/"],
      "region": "the gRPC contract (proto)",
      "docs": [],
      "recipe": [
        "Define the method + messages in the .proto (e.g. indexing.proto).",
        "Implement the service method in modules/worker-services/.",
        "Add the forward in DelegatingIngestService.java (modules/indexer-worker/.../grpc/) — the server registers this wrapper, not the impl; without it the RPC returns UNIMPLEMENTED at runtime.",
        "Add the client call in modules/app-services/ (RemoteKnowledgeClient or the relevant client).",
        "Add a contract test.",
        "Verify: ./gradlew.bat :modules:ipc-common:build :modules:worker-services:test :modules:indexer-worker:test",
        "Full reference: docs/reference/contributing/common-workflows.md (Add a gRPC method)."
      ]
    },
    {
      "id": "workflow-rest-endpoint",
      "pathIncludes": ["modules/ui/src/main/java/io/justsearch/ui/api/"],
      "region": "the Head REST API (Javalin)",
      "docs": [],
      "recipe": [
        "Add the handler/controller under modules/ui/src/main/java/.../api/.",
        "Register the route via the relevant .../api/routes/*Routes.java class, then wire that route class from LocalApiServer.java if needed.",
        "Update docs/reference/api-contract-map.md if the endpoint is public.",
        "Honor the Hard Invariants: loopback-only bind (127.0.0.1), no legacy endpoints (/api/search, /api/settings), and Head never touches Lucene — delegate index IO to the Worker via gRPC.",
        "Verify: ./gradlew.bat :modules:ui:test",
        "Full reference: docs/reference/contributing/common-workflows.md (Add a REST endpoint)."
      ]
    },
    {
      "id": "workflow-config-key",
      "pathIncludes": ["EnvRegistry.java", "ConfigKey.java", "ResolvedConfigBuilder.java"],
      "region": "the configuration resolution layer",
      "docs": [],
      "recipe": [
        "Add the entry in EnvRegistry.java (operator-facing) or ConfigKey.java (YAML-only).",
        "Wire it in ResolvedConfigBuilder.java — add a putYaml*() contribution + a resolve*() call in the relevant build*().",
        "Expose it via a ResolvedConfig record field.",
        "Document it in docs/reference/configuration/environment-variables.md.",
        "Verify: ./gradlew.bat :modules:configuration:test",
        "Regrowth ratchet (tempdoc 799 K.4): a NET-NEW key fails the config-surface gate. Run `node scripts/docs/generate-runtime-config-matrix.mjs` then `node scripts/governance/run.mjs --gate config-surface --mode gate`. Growth is allowed but must be DECLARED — add a changeset under gates/config-surface/.changesets/ classified `declared-growth` with a tempdoc/adr justification. Before declaring, check the knob is not already shadowed by a hardcoded constant: tempdoc 754 found 28 that were (a documented knob that does nothing is worse than no knob).",
        "Full reference: docs/reference/contributing/common-workflows.md (Add a configuration key)."
      ]
    },
    {
      "id": "workflow-agent-tool",
      "pathIncludes": [
        "registry/operations/",
        "agent/tools/",
        "AgentToolsOperationCatalog",
        "OperationPolicy.java"
      ],
      "region": "the agent tool / operation substrate",
      "docs": [
        {
          "path": "docs/decisions/0030-policy-on-operations-vs-mcp-hints.md",
          "why": "this ADR is why JustSearch treats operation metadata as ENFORCED policy rather than MCP-style untrusted hints. The divergence is justified BY the enforcement, so an axis that stops being enforced invalidates the argument, not just the prose."
        }
      ],
      "recipe": [
        "Add/update the operation definition in CoreOperationCatalog (modules/app-services/.../registry/operations/) or AgentToolsOperationCatalog (modules/app-agent/src/main/java/io/justsearch/agent/tools/AgentToolsOperationCatalog.java).",
        "Implement/update the handler. The five knowledge/file agent tools ARE their own OperationHandlers — SearchTool, BrowseTool, IngestTool, ReadDocumentTool and FileOperationsTool in modules/app-agent/src/main/java/io/justsearch/agent/tools/ implement the interface directly, with no adapter class in between. Two agent operations, core.remember and core.navigate-to-surface, keep dedicated handlers in modules/app-services/.../registry/operations/handlers/, as do all non-agent operations.",
        "Confirm AgentToolEmitter projects the intended model-visible wire name + schema.",
        "Declare safety/approval metadata at the operation layer, and run node scripts/ci/check-policy-axis-liveness.mjs. Tempdoc 879: every OperationPolicy axis must have a production reader outside the registry validators, and every Optional axis must be declared by at least one operation. An axis that fails is not 'not wired yet' — wire it to a consumer whose behaviour the declaration changes, or delete it (field, every declaration site, validators, wire projection, both schema copies, generated FE types, docs). There is deliberately no opt-out: an escape hatch for 'declared ahead of its consumer' is the deferral this gate exists to refuse.",
        "Update docs/explanation/22-agent-system-architecture.md.",
        "Verify: ./gradlew.bat :modules:app-agent:test :modules:app-services:test",
        "Full reference: docs/reference/contributing/common-workflows.md (Add an agent tool)."
      ]
    },
    {
      "id": "model-blobs",
      "pathIncludes": ["models/"],
      "region": "the models/ tree (weights are never tracked in this repo)",
      "docs": [],
      "recipe": [
        "This public repo NEVER tracks model blobs — 0 .onnx in its history; CI builds without them.",
        "models/.gitignore excludes the weights so `git add -A` cannot stage ~2.6 GB via the *.onnx LFS filter. Do not 'fix' that ignore rule.",
        "The PRIVATE repo LFS-tracks them, so the inherited rule 'do not gitignore model files' applies THERE and was carried here at v0.1.0 despite never being true of this repo (inherited-authority drift — tempdoc 799 §C.4)."
      ]
    },
    {
      "id": "index-rebuild-after-field-change",
      "pathIncludes": ["SSOT/catalogs/fields.v1.json", "IndexingDocumentOps"],
      "region": "the index field schema / document extraction path",
      "docs": [],
      "recipe": [
        "Adding a field to fields.v1.json or changing extraction in IndexingDocumentOps does NOT update existing documents.",
        "Existing indices must be REBUILT: `jseval run --reset` (eval mode) or `jseval run --start-backend --clean`.",
        "Test corpora indexed before your change will silently lack the new fields — a green run over a stale corpus proves nothing about the new field."
      ]
    },
    {
      "id": "dev-mcp-surface",
      "pathIncludes": [
        "scripts/dev/justsearch-dev-mcp/",
        "scripts/dev/justsearch-dev-mcp.mjs"
      ],
      "region": "the justsearch-dev MCP surface (the agent-facing dev tools)",
      "maintain": true,
      "docs": [
        {
          "path": "docs/reference/contributing/mcp-dev-tools.md",
          "why": "the canonical inventory of this surface — tool list, fetch_api_json endpoint keys, api_call allowlist. Tempdoc 844 §6.3 found the same inventory stated in four places and wrong in all four, because nothing compared the doc to the server. Update this page in the SAME change as the tool registration."
        }
      ],
      "recipe": [
        "Adding, removing or renaming a tool / an endpoint key / an allowlist entry: update docs/reference/contributing/mcp-dev-tools.md in the same change (the tool table AND the `exposes exactly these **N** tools` count).",
        "Then run `node scripts/ci/check-dev-mcp-doc-sync.mjs` — it spawns the server, calls tools/list, and compares all three inventories in both directions. It needs no dev stack.",
        "If you changed the gate itself, run its co-located test too: `node scripts/ci/check-dev-mcp-doc-sync.test.mjs`.",
        "Changing tool BEHAVIOUR (projection, probes, error codes, dist resolution): run `node scripts/dev/test-dev-mcp-surface-honesty.mjs` (pure units) and `node scripts/dev/test-dev-mcp-projection-live.mjs` (real handlers over stdio against a throwaway loopback backend). Neither needs a dev stack.",
        "Tempdoc 844 §12.2 is the standing criterion for this surface: a dev tool must not report state it did not verify, and must not report success it did not confirm — an explicit unknown (`null`) beats a confident default (`[]`, `false`, `OK`)."
      ]
    },
    {
      "id": "dev-stack-stale-jar",
      "pathIncludes": ["modules/app-services/", "modules/app-agent-api/"],
      "region": "Java on the head-process classpath (dev-stack freshness)",
      "docs": [],
      "recipe": [
        "The MCP justsearch_dev_start does not always re-install the worker dist when upstream Gradle tasks report UP-TO-DATE.",
        "After editing Java here (or in any module on the head-process classpath), run `./gradlew.bat :modules:ui:installDist` explicitly BEFORE dev_start / restart, so the jar in modules/ui/build/install/ui/lib/ is fresh.",
        "Symptom if skipped: the wire payload reflects the PREVIOUS build despite a successful `gradlew build`. Discovered tempdoc 511-followup Track E live-verify."
      ]
    }
  ]
}
