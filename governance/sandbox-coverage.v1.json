{
  "version": 1,
  "tempdoc": 728,
  "description": "Sandbox release-candidate validation coverage register (tempdoc 728, Part B/C). Projection-not-fork applied to release validation: what a clean-Sandbox round must verify DERIVES from what the app ships, instead of being remembered. scripts/sandbox/gen_coverage_brief.py reads the committed surface artifacts (route-manifest cohorts, CorePlugin.ts surface ids, coreInteractionShapes.ts) and FAILS CLOSED if any derived cohort/surface/shape is neither covered nor exempted here — so a newly-shipped surface (the /mcp and trust-panel misses that motivated this doc) can no longer be silently forgotten. Tiers: 'sandbox' = the clean-install / real-GUI / real-external-agent view only the Sandbox has; 'host' = protocol/contract correctness a host test already owns (the Sandbox still confirms reachability); 'sandbox-must-watch' = findings that cannot be CI-gated (Windows trust prompts) and are re-injected into every round's brief (Part C). Same positive-coverage shape as check-ui-step-coverage / check-declared-surfaces.",
  "sources": {
    "routeManifest": "modules/ui-web/src/api/generated/route-manifest.snapshot.json",
    "surfaceCatalog": "modules/ui-web/src/shell-v0/plugin-api/CorePlugin.ts",
    "interactionShapes": "modules/ui-web/src/shell-v0/plugin-api/coreInteractionShapes.ts"
  },
  "cohortCoverage": [
    { "cohort": "mcp", "tier": "sandbox", "requiredRoutes": ["POST /mcp"], "validateHow": "On a clean install, confirm an EXTERNAL MCP client reaches POST /mcp: `npx @modelcontextprotocol/inspector --cli http://127.0.0.1:<port>/mcp --transport http --method tools/list` returns the tool set. requiredRoutes pins POST /mcp so a token-only GET can't satisfy coverage. Protocol conformance is host-owned (McpProtocolHandlerTest); the Sandbox proves clean-install reachability + discoverability. REQUIRED PROCEDURE for the published TYPED_CONFIRM trust-gate claim (see claims[] below): the Inspector CLI's `--tool-arg` string-coerces every value and CANNOT express `justsearch_ingest`'s `paths: string[]` argument -- do not attempt tools/call through it, that procedure is unfollowable (verified against a real round; see tmp/sandbox evidence from the round that hit this). Instead run the staged driver `node <mapped folder>\\mcp-client\\mcp-typed-confirm.mjs --target <path>` (see `<mapped folder>\\mcp-client\\README.md`), which spawns the REAL shipped MCPB stdio bridge (`mcp-client\\index.js`, a verbatim copy of packaging/mcpb/server/index.js) as a child process and drives `initialize` -> `notifications/initialized` -> `tools/call justsearch_ingest(paths:[<path>])` over its stdin/stdout -- the same transport a real MCP host (e.g. Claude Desktop) uses, so this exercises the actual shipped artifact. Confirm the tools/call response reports a PENDING authorization (isError:true, 'requires your approval (gate: TYPED_CONFIRM)'), not a silent/immediate ingest. The gate response text does NOT carry the pending id -- mcp-typed-confirm.mjs resolves it itself (via GET /api/advisory/authorization-pending/stream) and prints it as `PENDING_ID=<id>` right after the gate result; read that line, do not hand-scrape SSE. Resolve it via `POST /api/authorizations/approve` with JSON body `{\"pendingId\": \"<id>\", \"execute\": true}` (API-reachable, no GUI needed) -- the body key for the id is `pendingId` (AuthorizationController.handleApprove reads body.pendingId; any other key name for the id 400s), and `execute: true` is MANDATORY: without it the call only mints a consent capsule and returns, it does not dispatch the ingest, because an MCP-originated pending has no live browser caller left to replay the capsule against (see AuthorizationController's handleApprove Javadoc, tempdoc 655 addendum). A prior round's procedure named a different (wrong) body key for the id with no execute flag at all, got 400s, and misfiled the result as \"approve never executes the operation\" -- a false HIGH regression against this documentation defect, not the product. scripts/ci/check-sandbox-authorization-field.mjs now fails the build if that retired key name reappears here or in mcp-client-README.md. Then confirm the ingest actually proceeds after approval. `tools/list` reachability alone does NOT exercise this claim -- it is a separate, mandatory step this round must perform. STATUS (2026-07-15): a prior PowerShell driver was found to HANG waiting on the tools/call response and has been removed. Live isolation against a running dev stack independently verified: the server is fixed and conformant (notifications/initialized returns 202 Accepted with no body; unknown requests still return -32601), and the shipped bridge driven over stdio by this exact Node driver's message sequence WORKS end-to-end -- initialize succeeds, and tools/call justsearch_ingest with a real array paths returns the TYPED_CONFIRM gate response. The server, the shipped bridge, and this driver's message sequence are live-verified; the round running this procedure still must execute mcp-typed-confirm.mjs itself and report the result (including the post-approval re-check)." },
    { "cohort": "knowledge", "tier": "sandbox", "validateHow": "Search + ingest journeys: add a folder / ingest SciFact via the UI, run real searches, compare UI results to /api/knowledge/* responses." },
    { "cohort": "indexing", "tier": "sandbox", "validateHow": "Library indexing journey: per-folder row state, Tasks panel live updates, status-bar queue agree with /api/knowledge/status during ingest." },
    { "cohort": "inference", "tier": "sandbox", "validateHow": "One idle chat completion at GPU speed; runtime selects cuda12; /api/inference/status truthful." },
    { "cohort": "ai-runtime", "tier": "sandbox", "validateHow": "Install AI journey: full model + cuda-runtime download reaches installedFully; /api/ai/runtime/status truthful." },
    { "cohort": "conversation", "tier": "sandbox", "validateHow": "Chat/RAG: grounded cited answer; escalation-ladder rungs honest (see shapeCoverage)." },
    { "cohort": "interaction", "tier": "sandbox", "validateHow": "The one interaction surface renders; mode labels match the actual response." },
    { "cohort": "agent", "tier": "sandbox", "validateHow": "The Agent 'delegate a task' rung makes obvious what it will do before it runs; disabled with a clear reason when AI is offline." },
    { "cohort": "openai-compat", "tier": "sandbox", "validateHow": "An external client can call /v1 chat completions on a clean install (loopback)." },
    { "cohort": "boot", "tier": "exempt", "reason": "Internal process-lifecycle API; the /api/boot/phases data it exposes is rendered by BootPhasesPanel.ts inside core.logs-surface (sandbox tier), so it is covered via that surface, not directly." },
    { "cohort": "config", "tier": "exempt", "reason": "Internal configuration API; effective-config is an inspection aid, not a validated surface." },
    { "cohort": "debug", "tier": "exempt", "reason": "The GET routes are diagnostic-only (prod=false), not a shipped user surface. The 4 mutating POST routes (POST /api/debug/clear-condition, POST /api/debug/logging, POST /api/debug/reset-index, POST /api/debug/trip-condition) are reachable in prod but carry prod session-token auth, unlike the GET diagnostics -- narrowed from a blanket prod=false claim." },
    { "cohort": "governance", "tier": "exempt", "reason": "Internal governance/registry substrate." },
    { "cohort": "i18n", "tier": "exempt", "reason": "Internal localization plumbing." },
    { "cohort": "infra", "tier": "exempt", "reason": "Capabilities/contract-version substrate (host.* sub-API), not user-facing." },
    { "cohort": "observability", "tier": "exempt", "reason": "Diagnostics/metrics/telemetry routes (POST /api/diagnostics/export, GET /api/diagnostics/ingestion/*, GET /api/diagnostics/traces, GET /api/metrics/*, GET /api/telemetry/health, GET /api/health/events/stream) are opt-in and internal. The two mandatory lifecycle endpoints in this cohort, GET /api/health and GET /api/status, are NOT opt-in -- they are exercised via `surface:core.health-surface` (sandbox tier) and the collect-evidence.ps1 API sanity ladder, so the blanket opt-in framing applies only to the diagnostics/metrics/telemetry subset." },
    { "cohort": "plugins", "tier": "exempt", "reason": "Plugin-registry substrate, internal." },
    { "cohort": "registry", "tier": "exempt", "reason": "Internal registry snapshot/consumer-presence substrate." },
    { "cohort": "resource", "tier": "exempt", "reason": "Resource-declaration substrate, internal." },
    { "cohort": "runtime", "tier": "exempt", "reason": "Runtime-manifest infra (port discovery); the /mcp reachability it also advertises is covered under the mcp cohort." },
    { "cohort": "schema", "tier": "exempt", "reason": "Schema-serving API, internal tooling." },
    { "cohort": "other", "tier": "exempt", "reason": "Per-member: /api/library/resolve-hash is consumed by indexingJobsBridge.ts / resolvePathLazy.ts and is exercised via the Library/indexing journeys (cohort:indexing, cohort:knowledge). /api/scans/{scanId}/progress has no FE consumer found -- unwired, harmless. Remaining members (OPTIONS /*, POST /api/admin/inference/reload, POST /api/admin/runtime/reload, GET /api/meta/openapi.json, GET /api/meta/routes, GET+POST /api/ui/ready) are internal/dev-tooling utility routes with no coherent user surface." }
  ],
  "surfaceCoverage": [
    { "surfaceId": "core.library-surface", "tier": "sandbox", "validateHow": "Library / Add Folder journey; toast occlusion; progress visibility.", "reach": { "navPath": "Left rail (RAIL placement, CorePlugin.ts:30); command palette 'Go to Library' (shell.go-to-library, substrates/actions/index.ts:794)" } },
    { "surfaceId": "core.brain-surface", "tier": "sandbox", "validateHow": "Install AI / Brain: Simple must suffice for first-run; Advanced must not surface scary internals as default.", "reach": { "testid": "brain-simple-panel" } },
    { "surfaceId": "core.unified-chat-surface", "tier": "sandbox", "validateHow": "The one chat/search window; walk the full escalation ladder (see shapeCoverage); AI-requiring rungs disable honestly when offline.", "reach": { "navPath": "Left rail, default landing surface (RAIL placement, CorePlugin.ts:117); command palette 'Go to Chat' / 'Go to Search' (shell.go-to-chat / shell.go-to-search, substrates/actions/index.ts:793,797)" } },
    { "surfaceId": "core.settings-surface", "tier": "sandbox", "validateHow": "Settings render + persistence across restart.", "reach": { "testid": "settings-accessibility" } },
    { "surfaceId": "core.security-surface", "tier": "sandbox", "validateHow": "TRUST SURFACE: encryption at-rest status matches reality; the irreversible forget-passphrase consequence is unmissable before commit; recovery key surfaced.", "reach": { "navPath": "Left rail: Security & Privacy (RAIL placement, CorePlugin.ts:138)", "note": "SecuritySurface.ts:368 has data-testid=\"enc-recovery-key\" but it only renders during the setup/regenerate recovery-key-save flow, not on landing -- not used as the general reach anchor." } },
    { "surfaceId": "core.memory-surface", "tier": "sandbox", "validateHow": "TRUST SURFACE (DEEPLINK): user can see what the AI learned and forget a fact; empty state reads private-by-default, not broken.", "reach": { "navPath": "URL deep-link #justsearch://surface/core.memory-surface (DEEPLINK placement, CorePlugin.ts:150; hash scheme verified at router/parser.ts:11-12, router/sources/URLSource.ts:101). No 'Go to' command-palette entry is registered for this surface as of round 6." } },
    { "surfaceId": "core.presentation-gallery-surface", "tier": "sandbox", "validateHow": "TRUST SURFACE (DEEPLINK, Skins): applying a built-in skin never leaves an illegible/broken surface; choice survives restart.", "reach": { "testid": "gallery-grid" } },
    { "surfaceId": "core.presentation-editor-surface", "tier": "sandbox", "validateHow": "TRUST SURFACE (DEEPLINK, Skins editor): editing/importing a skin JSON never leaves an illegible/broken surface.", "reach": { "testid": "editor-palette" } },
    { "surfaceId": "core.health-surface", "tier": "sandbox", "validateHow": "Health/System during Install AI and ingest; UI/API agreement (no 'Reconnecting...' while REST is healthy).", "reach": { "navPath": "System page tabs: System Health | Logs | Activity | AUDIT (SystemSurface.ts:6-13, one <jf-surface-tabs> composing Health/Logs/Activity); command palette 'Go to System Health' (shell.go-to-health, substrates/actions/index.ts:796)" } },
    { "surfaceId": "core.help-surface", "tier": "sandbox", "validateHow": "Built-in help search for 'getting started' returns a usable result.", "reach": { "navPath": "Rail's dedicated '?' affordance, or command palette / URL deep-link (CorePlugin.ts:50-53)" } },
    { "surfaceId": "core.browse-surface", "tier": "sandbox", "validateHow": "File browse surface renders and is navigable.", "reach": { "navPath": "Command palette 'Go to Browse' (shell.go-to-browse, substrates/actions/index.ts:798; CommandPalette.ts:439)" } },
    { "surfaceId": "core.activity-surface", "tier": "sandbox", "validateHow": "Activity/Tasks surface reflects live job state.", "reach": { "testid": "activity-surface-action-ledger", "navPath": "System page tabs: System Health | Logs | Activity | AUDIT (SystemSurface.ts:6-13; the TRUST Activity tab keeps its 'Audit' marker per SystemSurface.ts:12-13)" } },
    { "surfaceId": "core.logs-surface", "tier": "sandbox", "validateHow": "Logs surface renders without exposing scary internals as the default view.", "reach": { "navPath": "System page tabs: System Health | Logs | Activity | AUDIT (SystemSurface.ts:6-13, one <jf-surface-tabs> composing Health/Logs/Activity)" } },
    { "surfaceId": "core.command-palette", "tier": "sandbox", "validateHow": "Command palette is discoverable and exposes Install AI / key actions (discoverability check).", "reach": { "navPath": "Ctrl+K keyboard shortcut, configurable (CommandPalette.ts:6)" } },
    { "surfaceId": "core.governance-surface", "tier": "exempt", "reason": "Developer/diagnostic DEEPLINK surface, not part of a first-run user journey." },
    { "surfaceId": "core.api-explorer-surface", "tier": "exempt", "reason": "Developer API-explorer DEEPLINK surface (prod=false aid), not a shipped user journey." }
  ],
  "shapeCoverage": [
    { "shape": "core.rag-ask", "tier": "sandbox", "validateHow": "Documents rung: grounded, cited answer; label honest.", "reach": { "testid": "retrieve-tier" } },
    { "shape": "core.free-chat", "tier": "exempt", "reason": "NOT REACHABLE BY ANY USER as of round 7 (tempdoc 798 B2) - round 6's open question is now answered, so the row stops asserting coverage it cannot have. deriveAffordance() never returns 'none' from its own logic (agencyPosture.ts:95-99) and the production Ask trigger hardcodes route:'ask' -> 'documents' (UnifiedChatView.ts:2577-2588); the only production site setting 'none' is restoreRecentConversation() (UnifiedChatView.ts:1897), which needs a free-chat conversation to already exist and nothing creates one. Exempt is honest bookkeeping, NOT a fix: the fix is making an unscoped Ask resolve to 'none' as UnifiedChatView.ts:8 documents, after which this row goes back to tier sandbox. EXPLICITLY FORBIDDEN routes to a green flag: renaming evidence files to satisfy the coverage token match, or stamping a shape-id purely to flip the flag - both make a feature no user can reach look covered.", "reach": { "navPath": "URL deep-link justsearch://answer?q=...&shape=core.free-chat, which resolves (router/parser.ts:111-122) and presets the 'none' affordance (UnifiedChatView.ts:775-779) - the ONLY path that reaches this shape", "note": "round 7 answer to round 6's open question: nothing in the shipped UI surfaces that deep link, so no human-discoverable entry point exists." } },
    { "shape": "core.extract", "tier": "sandbox", "validateHow": "Structured rung: schema extraction; label/affordance honest.", "reach": { "testid": "schema-attach" } },
    { "shape": "core.agent-run", "tier": "sandbox", "validateHow": "Agent rung: 'delegate a task' makes obvious what it will do before it runs.", "reach": { "testid": "escalation-delegate" } },
    { "shape": "core.workflow-run", "tier": "exempt", "reason": "NOT REACHABLE BY ANY USER as of round 7 (tempdoc 798 B2) - round 6's open question is now answered, so the row stops asserting coverage it cannot have. The workflow trigger renders only when workflowPending===true (UnifiedChatView.ts:3453-3488), set only when shapeId==='core.workflow-run' (UnifiedChatView.ts:785-788), stamped only by jf-chat-shape-mount (ChatShapeMount.ts:122) - which the unified chat surface deliberately bypasses as a multi-shape host (CorePlugin.ts:107-121). No deep-link substitute: buildRequestBody() has no case for this shape (unifiedChatRequest.ts:68-96) so workflowId is omitted and the backend correctly rejects with BAD_REQUEST (WorkflowShapeRunner.java:395-403). This is a retire-with-a-sweep residue: tempdoc 565 §15.C retired the standalone workflow surface and the replacement was never wired to a live trigger. Exempt is honest bookkeeping, NOT a fix: shipping a discoverable entry point (composer affordance or command-palette entry that arms workflowPending) returns this row to tier sandbox. EXPLICITLY FORBIDDEN routes to a green flag: renaming evidence files to satisfy the coverage token match, or stamping a shape-id purely to flip the flag - both make a feature no user can reach look covered.", "reach": { "note": "round 7 answer to round 6's open question: NO entry point exists in any shipped build - not a deep link (buildRequestBody omits workflowId, so the backend rejects it), not a command-palette entry, not a composer affordance." } }
  ],
  "claims": [
    { "claim": "The app exposes a private retrieval backend to AI agents over POST /mcp (loopback).", "source": "README.md (## Two ways to use it / MCP section)", "coversId": "cohort:mcp" },
    { "claim": "MCP tool set is available to external agents.", "source": "docs/reference/mcp-production-server.md; packaging/mcpb/README.md", "coversId": "cohort:mcp" },
    { "claim": "Loopback-only, no analytics/telemetry exporter; documents never leave the machine.", "source": "README.md (## Privacy); docs/reference/security/threat-model.md", "coversId": "surface:core.security-surface" },
    { "claim": "Untrusted MCP-client actions are gated (TYPED_CONFIRM trust model).", "source": "docs/reference/mcp-production-server.md (## Trust Model)", "coversId": "cohort:mcp" }
  ],
  "mustWatch": [
    { "id": "install-trust-prompts", "reason": "SmartScreen / Defender / Smart App Control / unsigned-publisher warnings on install cannot be reproduced in CI (no clean Windows); every round records the exact prompt text/screenshots and whether automation (/S) was honored.", "origin": "recurring across rounds; not CI-gateable", "observability": "blocked-by-posture", "note": "NOT observable under the CURRENT sandbox posture: generate_wsb's LogonCommand force-disables Smart App Control at boot (sandbox-launch.py's generate_wsb, ~line 507-517), and the installer arrives via folder mount, which never attaches Mark-of-the-Web -- so SmartScreen has nothing to trigger on. Making it fire would need BOTH an in-sandbox download (for MOTW) AND skipping the SAC-disable for that round, which trades away the bypass the round needs to function at all. The real fix is code signing (see the comment at sandbox-launch.py ~line 507-517); deferred by owner budget. This is a posture limitation, not a claim the prompts don't exist -- record it as unobservable this round, not as a pass." },
    { "id": "ui-api-truthfulness-under-load", "reason": "Shell showing 'Reconnecting...' or stale cards while /api/health, /api/status, /api/knowledge/status are READY is a clean-environment/timing finding CI cannot see; must-watch on every round.", "origin": "recurring; not CI-gateable", "observability": "sandbox" },
    { "id": "warm-reinstall-over-existing-data", "reason": "uninstall (user data survives per ADR-0024) -> reinstall -> first boot against the pre-existing enriched index must reach a healthy embeddingCompatState and hybrid search must execute dense retrieval (regression home for the 734 A.1 recovery path).", "origin": "regression home for tempdoc 734 A.1 (dense/HYBRID retrieval recovery path); not CI-gateable, needs a real clean-install/uninstall/reinstall cycle", "observability": "sandbox" },
    { "id": "clean-environment-path-timing", "reason": "Tools installed mid-session (e.g. Node.js by an MSI) are invisible to already-running shells/processes that started before the install -- a stale session PATH reads as 'tool not installed' when it is genuinely on disk. The harness must self-repair (probe well-known install dirs + the MACHINE PATH, not just the session PATH) rather than report a false negative and silently drop coverage; see collect-evidence.ps1's npx/Node PATH-repair gate.", "origin": "tempdoc 727-followup smoke round: collect-evidence.ps1 falsely reported 'Node.js may not be installed' on its first run, silently dropping all /mcp coverage; not CI-gateable, only reproducible on a genuinely clean environment with staggered installs", "observability": "sandbox" },
    { "id": "expired-pending-approval-ceremony", "reason": "An expired/already-consumed authorization pending can still render a fully convincing Authorize dialog -- typing the confirm phrase enables Approve, but clicking Approve (or Deny) silently no-ops with no grant, no error, and no way to dismiss the modal. Invisible to an API-only round (item 4 scored a clean PASS on the API); only the GUI tier surfaced it. Check remains UI-tier until/unless a host RAIL step lands.", "origin": "HIGH finding, tempdoc 727-followup smoke round, reproduced under two varied conditions (expired vs fresh pending) on the 0.2.0 build; may already be fixed on later builds but is a standing must-watch until a regression test exists", "observability": "sandbox" }
  ],
  "cohortExempt": [],
  "surfaceExempt": []
}
