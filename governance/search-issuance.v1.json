{
  "note": "Tempdoc 577 Goal 1 Extension II / partial Move H — the ONE search-issuance seam. Every search the search SURFACE issues is constructed by buildSearchIntent and fired from the single runSearch fetch in searchState.ts; no second body-shaping / POST site within the surface layer (shell-v0). This is the single-issuance-seam half of 570 Move H; the full coverage-projects-from-the-mode-catalog form waits for Goal 3 / Move A. Lighter scripts/ci tier; wired as a ci.yml step + the CLAUDE.md pre-merge list. Honest limit (same as the steering / run-renderers gates): a surface routing through a DIFFERENT named client (e.g. importing api/domains/search) is import-invisible to this path-substring scan — register + discipline, not absolute.",
  "seam": {
    "symbol": "buildSearchIntent",
    "file": "modules/ui-web/src/shell-v0/state/searchState.ts"
  },
  "scope": "modules/ui-web/src/shell-v0",
  "issuancePattern": "/api/knowledge/search",
  "allowedIssuers": [
    "modules/ui-web/src/shell-v0/state/searchState.ts"
  ],
  "allowedReferences": [
    "modules/ui-web/src/shell-v0/plugin-api/capabilities/data.ts"
  ]
}
