---
classification: rule-retired
slot: ask-when-uncertain
tempdoc: 681
---

Retires the `ask-when-uncertain` rule (tier-register row 17, tier `prose-only`;
anchor removed from CLAUDE.md). Tempdoc 681 Move 3, tranche 1: the rule
described behavior the current model generation and harness provide by default
(the harness's own guidance governs when to ask vs proceed, and question tooling
exists), and the evidence check found zero observations-inbox entries for its
failure mode (failure-to-ask) while the operator's session prompts consistently
steer against over-asking. Watch-window terms per 681 §De-risk: if
failure-to-ask incidents appear in the inbox/retros after this retirement, the
rule is restored and recorded as having earned its text.
