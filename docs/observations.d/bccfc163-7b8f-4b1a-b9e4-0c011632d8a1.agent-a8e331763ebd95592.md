# Observations shard — session bccfc163-7b8f-4b1a-b9e4-0c011632d8a1 (tree agent-a8e331763ebd95592)

> Per-session-per-tree inbox shard (tempdoc 618 Seam C, keyed by the writing
> tree per tempdoc 862). Append-only; one writer, one file — do not append to
> another tree's or session's shard. Folded into docs/observations.md by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] The ui-web-gates recipe's kernel-gate command uses a comma list (--gate a,b,c) that scripts/governance/run.mjs does not support: it errors "gate id 'a,b,c' not found", and repeated --gate flags silently run only the LAST one, so an agent following the recipe verbatim runs zero or one of the six kernel gates — `governance/consult-register.v1.json:38` / `scripts/governance/run.mjs:69` (2026-08-25)
- [ ] 864 review F7: SearchV3View's palette?.open test is a narrower second answer to the question modalOwnsFocus() now owns (is a modal up?) — practically unreachable today, but it is the shape that drifts; fold it into the one authority when PR B/E touch this file — `modules/ui-web/src/shell-v0/views/search-v3/SearchV3View.ts:976` (2026-08-25)
- [ ] 864 review F7: test-isolation hazard introduced by the modal-owns-focus guard — modality depth is MODULE state, so any suite that leaves a dialog/overlay/palette open silently renders every later global-key assertion in that file inert (they pass for the wrong reason). Reset seam exists (__resetModalityForTest) but nothing forces its use; a shared setup-file reset would close it — `modules/ui-web/src/shell-v0/primitives/modality.ts:80` (2026-08-25)
- [ ] The ui-web-gates recipe's kernel-gate command is worse than 'unsupported': the comma form exits 2 AND repeated --gate flags are silently last-wins, so an agent following the recipe verbatim runs ZERO of the six kernel gates (confirmed at source by the 864 PR-D reviewer) — `scripts/governance/run.mjs:69` (2026-08-25)
- [ ] ui-web suite has two load-dependent flakes under a full parallel run (both green in isolation and on a clean re-run, zero import coupling to the change under test): PluginLoader 'module-mode: an ES-import plugin resolves @kernel/data and installs' and EnvelopeStream 'reconnects when the heartbeat-absence watchdog expires' — both are timing/dynamic-import shaped; they cost a full re-run to distinguish from a real regression — `modules/ui-web/src/shell-v0/streaming/EnvelopeStream.test.ts` (2026-08-25)
- [ ] SchemaMismatchStatusContractTest (modules:ui integrationTest) flakes locally on a busy box with 'Timeout waiting for worker port after 15000ms' — green on an immediate re-run, no code change between; the 15s worker-port budget is tight enough that an unrelated FE-only branch can hit a red build and spend a full rebuild ruling it out — `modules/ui/src/integrationTest/java/io/justsearch/ui/api/SchemaMismatchStatusContractTest.java` (2026-08-25)
