# Observations shard — session bccfc163-7b8f-4b1a-b9e4-0c011632d8a1 (tree agent-a234b1ec11f97186f)

> Per-session-per-tree inbox shard (tempdoc 618 Seam C, keyed by the writing
> tree per tempdoc 862). Append-only; one writer, one file — do not append to
> another tree's or session's shard. Folded into docs/observations.md by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] ui_shot.py's _start_vite_server and _resolve_ui_url duplicate the same _SERVER_INFO_PATH-read + _is_server_alive reuse-check block instead of sharing one helper — `scripts/jseval/jseval/ui_shot.py:370-379,494-500` (2026-08-25)
