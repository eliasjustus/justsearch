# Observations shard — session f6617483-ddd7-4dd3-af62-5fa852106fc4 (tree agent-aa51999fa41f5fdf9)

> Per-session-per-tree inbox shard (tempdoc 618 Seam C, keyed by the writing
> tree per tempdoc 862). Append-only; one writer, one file — do not append to
> another tree's or session's shard. Folded into docs/observations.md by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] 878 §D.1: the iteration-ceiling finalize has no telemetry counter — agent.budget_edge_finalize.total is named for the budget wall and deliberately not reused, so ceiling-finalize success/failure is unmeasured; adding a limit dimension is a metric-schema change — `modules/app-agent/src/main/java/io/justsearch/agent/AgentLlmCaller.java:attemptStepCeilingFinalize` (2026-08-26)
- [ ] check-store-recoverability gate is RED on main-derived trees: unclassified persistence write site — `modules/app-services/src/main/java/io/justsearch/app/services/ai/install/AcquisitionStage.java` (untouched by 878; reproduced on a clean base) (2026-08-26)
- [ ] Shared gradle single-lane wrapper leaks its lock: it writes the owner file INSIDE the lock dir, so the release 'rmdir $LOCK' always fails (Directory not empty) and a patient poller can starve for hours — fix is 'rm -rf' or an owner file as a sibling; also several agents use tmp/gradle-lane*.sh which bypass the lock entirely — `tmp/agent-orchestration/gradle-locked.sh` (2026-08-26)
