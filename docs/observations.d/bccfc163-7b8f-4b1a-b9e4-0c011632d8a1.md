# Observations shard — session bccfc163-7b8f-4b1a-b9e4-0c011632d8a1

> Per-session inbox shard (tempdoc 618 Seam C). Append-only; do not share with
> other sessions. Folded into docs/observations.md `## Inbox` by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] check-contrast-matrix parseBlock resolves only the FIRST block per selector; tokens.css declares [data-theme="light"] twice (layered at :425, unlayered ambient overrides at :848) so a future role/surface token added to the second block would be silently ignored by the gate — `modules/ui-web/src/styles/tokens.css:848` (2026-08-19)
- [ ] Windows Smart App Control blocks the unsigned ggml CPU-variant DLLs in the shared native-bin (observed: ggml-cpu-ivybridge.dll) on (re)staging/first-run of the cuda12 llama-server; popup recurs per stage. Harmless on the cuda12 path — VERIFIED this session: llama-server.log shows only 'loaded CUDA backend from ggml-cuda.dll' + 'loaded CPU backend from ggml-cpu-alderlake.dll' (host-matching variant), zero 'ivybridge' mentions, load_tensors offloaded 33/33 layers to GPU, selftest POST /v1/chat/completions 200, and a real completion returned in 1.6s. llama.cpp probes every ggml-cpu-*.dll to pick the host-best one; SAC flags an unselected probe candidate. — `modules/ui/native-bin/llama-server/variants/cuda12/` (2026-08-19)
- [ ] ai_activate fails 'Variant not installed: cuda12' in a fresh worktree while preflight reports llamaVariantResolvable OK — two different resolutions disagree: dev-runner resolveCuda12ServerExe() falls back to mainRepoRoot (so preflight/JUSTSEARCH_SERVER_EXE pass), but RuntimeActivationService.resolveVariantsRoot() uses RepoRootLocator, which returns the WORKTREE root, where modules/ui/native-bin does not exist. prepare-worktree.cjs does not link it. Workaround: junction worktree modules/ui/native-bin -> main checkout, then RESTART (variantsRoot is cached in the ctor at line 383). — `modules/app-services/src/main/java/io/justsearch/app/services/ai/runtime/RuntimeActivationService.java:1687` (2026-08-19)
- [ ] Search v3 emits two console.errors on most navigations: 'SES_UNHANDLED_REJECTION: TimeoutError: Transition was aborted because of timeout in DOM update' (a view-transition that never settles). Pre-existing, unrelated to 849/852; reproduces on plain surface navigation and shows up as console-real:2 in every sv3 ui-shot measure.json, so it permanently flags captures that are otherwise clean. — `modules/ui-web/src/shell-v0/views/search-v3/` (2026-08-19)
- [ ] The composer's stop-slot hint promises 'Enter steers the running agent' during an ask stream, where steerable is false and submit silently refuses — `modules/ui-web/src/shell-v0/views/search-v3/sv3-run.ts:578` (found by 509 review, pre-existing) (2026-08-19)
- [ ] jf-control has no delegatesFocus, so any adopted control that receives programmatic focus needs the caller to reach through its shadow root (852 S4 had to do this for the run-prompt focus) — `modules/ui-web/src/shell-v0/components/Control.ts:189` (2026-08-19)
- [ ] Search v3's raise-budget control refuses silently once the run is no longer live — the predicate lives in dispatchRunControl, so the window renders no availability reason (852 S4 could not close it without forking the seam's predicate) — `modules/ui-web/src/shell-v0/views/search-v3/SearchV3View.ts:2460` (2026-08-19)
- [ ] core.system-surface is a live RAIL surface with no SURFACE_ICONS entry, so the System rail button falls back to the generic hard-drive glyph — `modules/ui-web/src/shell-v0/utils/surfaceIcons.ts:11-31` (2026-08-19)
- [ ] governance/composition-surfaces.v1.json has exactly one adopter (views/UnifiedChatView.ts); when 852 S8-S11 deletes that view the register+gate go vacuous and must be retired or re-pointed — `governance/composition-surfaces.v1.json:12-14` (2026-08-19)
- [ ] consult-doc-hint resolves ONE region first-match-wins (governed-regions.mjs:57 .find()); the broad shell-v0 region shadows surface-catalog-parity AND the ui-web-gates recipe for every shell-v0 path incl. CorePlugin.ts — the surface-catalog-parity recipe's claim that ui-web-gates covers that direction is structurally false, and CLAUDE.md's 'pushed on edit by the consult hook' row is false for shell-v0 (found by 854 adversarial review B1) — `scripts/agent-analytics/hooks/consult-doc-hint.mjs:47` (2026-08-19)
- [ ] UnifiedChatView's j/k nav guard omitted SELECT (KeybindingRegistry.ts:163-167 includes it), so the workflow-picker <select> lost native type-ahead on j/k — fixed by 857 PR-A's shared isTypingTarget union; noted for the KeybindingRegistry predicate-fork history — `modules/ui-web/src/shell-v0/utils/keyboardHandler.ts` (2026-08-19)
- [x] RESOLVED same session: 854 tempdoc collision — 854-fusion-residue-lane.md merged to main first (PR #517); this batch renumbered 854 -> 857 (`docs/tempdocs/857-ratified-batch.md`), self-references swept, check-tempdoc-numbers clean — `docs/tempdocs/857-ratified-batch.md` (2026-08-19)
- [ ] check-a11y-closure rule (5) never scans views/search-v3/** or views/security/** — its VIEWS_DIR walk is a non-recursive readdirSync, so the successor window has no page-heading/main-landmark guard; making it recursive goes red today on a real second page <h1> in the hero (854 D-17; 854 PR-A lands a scoped test as a stand-in) — `scripts/ci/check-a11y-closure.mjs:134` (2026-08-19)
- [ ] Search v3 hero emits a second page <h1> beside the Shell topbar's sole one — a real a11y defect in the window about to become THE window; fixing it is a hero design call (demote to h2, or suppress the topbar heading in that state) and must land with the check-a11y-closure recursive-walk fix (854 D-17) — `modules/ui-web/src/shell-v0/views/search-v3/Sv3Composer.ts:1403` (2026-08-19)
- [ ] AdvisoryInboxDrawer's bare j/k row handler calls preventDefault() without stopPropagation(), so its keys bubble to every window-level listener in the app; 854 PR-A guards the Search v3 side with a defaultPrevented check, but the missing stopPropagation is still the drawer's own defect — `modules/ui-web/src/shell-v0/components/advisory/AdvisoryInboxDrawer.ts:379` (2026-08-19)
- [ ] Tempdoc number 854 is claimed by TWO in-flight worktrees — 854-ratified-batch.md (854 PR-A, this branch + agent-a215fe3e42cc0f67c) and 854-fusion-residue-lane.md (agent-a05038f933cfc34cf); neither is on main, so one must renumber before merge — `scripts/ci/check-tempdoc-numbers.mjs` (2026-08-19)
- [ ] Window-level J/K run-step nav has no modal-owns-focus guard: with focus on a palette popup button or a Shell drawer control that neither types nor preventDefaults, j/k jumps the transcript and pulls focus out of the open modal. Parity with the retiree (UnifiedChatView had the same hole), not a regression — recorded so the guard set (defaultPrevented + modifiers + typing + arm) is not read as exhaustive — `modules/ui-web/src/shell-v0/views/search-v3/Sv3Main.ts:1322` (2026-08-19)
- [ ] check-tempdoc-numbers only detects a number claimed by DIFFERENT worktrees — two tempdocs with the same number inside ONE tree pass: docs/tempdocs/ currently holds both 854-fusion-residue-lane.md (merged from main) and 854-ratified-batch.md and the check reports OK, so nothing stops two same-numbered tempdocs landing on main once one side merges first — `scripts/ci/check-tempdoc-numbers.mjs` (2026-08-19)
- [ ] config-surface changeset coverage is a function of HISTORY POSITION, not content: loadChangesets only counts changesets added relative to the baseline ref (HEAD~1), so a merged declaration stops covering its own growth and every branch cut afterwards fails silent-growth. Reproduced legibly on this branch — identical tree and identical generated matrix passed while HEAD~1 was pre-merge and failed once a later commit moved HEAD~1 past the merge. Third occurrence (803 documented it, #517 re-triggered it); the durable fix each time is advancing baseline.txt, so consider making declared growth write the baseline — `scripts/governance/gates/config-surface/enforcer.mjs:106` (2026-08-19)
- [ ] i18n resource catalog missing key `index.unavailable` — raw key rendered as user-visible fallback text in Search v3 status deck (seen live 2026-08-20) — `modules/ui-web/src/i18n/resourceCatalog.ts:210` (2026-08-19)
- [ ] GET /api/thread/{id} does a full Files.list scan of every agent-run dir + meta.json read per request (no conversationId index) — O(all runs) on every thread fetch — `modules/app-agent/src/main/java/io/justsearch/agent/AgentRunStore.java:475` (2026-08-19)
- [ ] Search v3 honours none of the shipped appearance escapes: zero `.high-contrast` handling and no `--glass-blur-scale` multiplier anywhere in the window, so 'solid surfaces' and high-contrast reach every shipped surface except this one — `modules/ui-web/src/shell-v0/views/search-v3/sv3-tokens.css.ts:20` (2026-08-20)
- [ ] steering-surfaces register omits `.resolveContextGate(` from runDirectiveChannels.callPatterns, so a hand-rolled context-gate resolution outside the dispatchRunControl seam would not be caught (the four sibling directives are listed) — `governance/steering-surfaces.v1.json` (2026-08-20)
- [ ] sv3 record reconciliation blanks a delegate turn's question: dispatchShapeDriven persists no USER_MESSAGE, so the record turn opens with question:'' and applySv3Record's reconcile() spreads the recorded turn first — the reader's own prompt disappears from the transcript at the run's terminal refresh (859 §9 suspect (c), out of 859 C-small scope) — `modules/ui-web/src/shell-v0/views/search-v3/sv3-sessions.ts:862-870` (2026-08-20)
- [ ] EvidenceItem.score is dead through its only producer: toEvidenceItem is called once (CitationsPanel.renderSourceCard) and that card never reads item.score — the labeled-metric projection 559 built has no live reader — `modules/ui-web/src/shell-v0/components/chat/evidenceProjection.ts:719` (2026-08-20)
- [ ] check-controls-a11y is RED on main (not in expected-state.v1.json): 1 title-on-disabled instance over baseline 0 in UnifiedChatView — verified byte-identical to main, so any 859 PR trips it innocently; either fix the site or add an expected-state row — `modules/ui-web/src/shell-v0/views/UnifiedChatView.ts:2138` (2026-08-20)
- [ ] strip-token-fallbacks --check is RED on main (not in expected-state.v1.json): 6 design-token fallbacks in ActionLedgerView.ts (2) + RecentsMenu.ts (4), both untouched by 859 — same RecentsMenu file as the known theme-token-closure row, so the two probably share a root cause — `modules/ui-web/src/shell-v0/components/RecentsMenu.ts` (2026-08-20)
- [ ] 859 design item 9 landed only partially: UnifiedChatView got the citationScorer threaded through its three resolveAnswerCitations call sites, but its source/match derivation was never moved onto the shared components/chat/agentEvidence.ts — so the legacy window still derives delegate evidence itself and agentEvidence has two importers, not three. Migrating it is the remaining half of the 'one projection' claim — `modules/ui-web/src/shell-v0/views/UnifiedChatView.ts:5182` (2026-08-24)
- [ ] Flaky: KnowledgeServerBootRecoveryTest failed once with ConcurrentModificationException during a full ./gradlew test, green on isolated re-run — `modules/app-services/src/test/java/io/justsearch/app/services/worker/KnowledgeServerBootRecoveryTest.java` (2026-08-20)
- [ ] check-controls-a11y RED on main: title-on-disabled at `modules/ui-web/src/shell-v0/views/UnifiedChatView.ts:2138` (baseline 0) — pre-existing, untouched by 859 C PR-2 (2026-08-20)
- [ ] check-accent-as-text + strip-token-fallbacks RED on main: accent-fill-as-text and 2 token fallbacks in `modules/ui-web/src/shell-v0/components/ActionLedgerView.ts`, 4 fallbacks in RecentsMenu.ts — pre-existing (2026-08-20)
- [ ] check-controls-a11y is RED on main and is NOT in expected-state.v1.json (which lists only theme-token-closure + accent-as-text): a title-on-disabled instance over baseline 0 — `modules/ui-web/src/shell-v0/views/UnifiedChatView.ts:2138` (2026-08-20, re-observed from 859 B)
- [ ] strip-token-fallbacks --check is RED on main (6 design-token fallbacks: ActionLedgerView.ts x2, RecentsMenu.ts x4) and is not in expected-state.v1.json — same two files as the documented theme-token-closure/accent-as-text reds — `modules/ui-web/src/shell-v0/components/RecentsMenu.ts` (2026-08-20, re-observed from 859 B)
- [ ] check-color-tokens is RED on main since #527 (855 settings window) and is not in expected-state.v1.json: 4 bare colored literals (#2e3440 #88c0d0 #f5ead8 #b88a3e) in the themes catalog — `modules/ui-web/src/shell-v0/themes/themesCatalog.ts` (2026-08-24)
- [ ] 859 D15: `resolveContextGate` is absent from `governance/steering-surfaces.v1.json` callPatterns (which lists .steer(/.cancelSession(/.raiseBudget(/.resumeSession(/.resolveBudgetGate() — a hand-rolled context-gate resolution outside the seam would not be caught — `governance/steering-surfaces.v1.json` (2026-08-24)
- [ ] Agent resume rebuilds a FRESH AgentRequest with no effort rung, so a resumed run silently re-sizes at Standard rather than the rung it was dispatched with (859 D §2.9 'check' row, verified) — `modules/app-agent/src/main/java/io/justsearch/agent/AgentRunQueryService.java:227` (2026-08-24)
- [ ] Live/record divergence, pre-existing: an agent `progress` event appends a live run-feed note but projects NOTHING on the record (`AgentInteractionMapper.fromRunEvent` has no `progress` case), so a reloaded run silently loses its progress steps — `modules/app-agent/src/main/java/io/justsearch/agent/AgentInteractionMapper.java:253` (2026-08-24)
- [ ] Test-infra limit: J/K cannot be walked over a landmark that is a shadow HOST (jf-reasoning-block, jf-tool-call-card) — happy-dom's ShadowRoot.activeElement throws inside `deepActiveElement`, so Sv3Main navigation cases fall back to a mocked jumpTo — `modules/ui-web/src/shell-v0/utils/keyboardHandler.ts:48` (2026-08-24)
- [ ] 859 D record-durability gap: the sized-continue gate's `budget_raised` / `context_gate_reapplied` narration is SESSION-LIFETIME only — both are `AgentProgress` events and `fromRunEvent` has no `progress` case, so a reloaded run shows a raised budget with no trace of the raise. D's "the run narrates every re-application" reads as durable and is not. Candidate fix: project progress notes durably (a PROGRESS InteractionEvent) in a follow-up — `modules/app-agent/src/main/java/io/justsearch/agent/AgentInteractionMapper.java:253` (2026-08-24)
- [ ] sv3 run-feed J/K walk stalls at tool-call cards: from a jf-reasoning-block repeated `j` never advances onto the following jf-tool-call-card, and from a card repeated `k` never steps to the preceding card — focus oscillates between two adjacent items (measured live on two records, 859 D-leg) — `modules/ui-web/src/shell-v0/views/search-v3/Sv3Main.ts` (2026-08-25)
- [ ] sv3 composer notice strip floats over the transcript with a 4% white fill and NO backdrop-filter in every appearance mode (normal, prefers-reduced-transparency, data-surface-mode=solid), so transcript glyphs read straight through its label: measured contrast 18.16:1 over empty surface but 3.34:1 over body text and 1.00:1 over a heading; axe grades it color-contrast INCOMPLETE — `modules/ui-web/src/shell-v0/views/search-v3/Sv3Composer.ts` (2026-08-25)
- [ ] the new sv3-composer-occlusion ui-shot step is registered in governance/ui-proportion-baseline.v1.json but NOT in governance/ui-a11y-baseline.v1.json, so ui-a11y-gate covers no Search v3 surface at all — `governance/ui-a11y-baseline.v1.json` (2026-08-25)
- [ ] sv3-composer-occlusion fixtures capture emits a framework-selfcheck console error '[jf-control] no accessible name' (classified env, so it does not show in console-real) — a nameless jf-control renders on that surface — `scripts/jseval/tmp/ui-shot/sv3-composer-occlusion.measure.json` (2026-08-25)
- [ ] scripts/jseval tests/test_ui_proportion_gate.py is RED on main — 26 of 56 fail (rows carry no 'selector' key, GROWN reported as 'ok'); the gate's report row shape and its test have drifted apart — `scripts/jseval/tests/test_ui_proportion_gate.py:63` (2026-08-25)
- [ ] governance/ui-a11y-baseline.v1.json points at a $schema file that does not exist (governance/ui-a11y-baseline.schema.json) — its sibling ui-proportion-baseline.schema.json does; the a11y register is unvalidated — `governance/ui-a11y-baseline.v1.json:2` (2026-08-25)
- [ ] Browser-automation evidence from the off-screen MCP tab is not user-visible truth for anything rAF-gated: Chrome suspends rAF there, so measureCoalesced's frame flag latches true and every deferred measure is swallowed (nav landmarks froze at 2 while the DOM held 7) — the 859 J/K-stall finding was this artifact; probe in a visible window (script-opened via user gesture) before chartering any rAF-adjacent defect — `modules/ui-web/src/shell-v0/primitives/navigation.ts:206` (2026-08-25)
- [ ] Stale line-number citations into primitives/navigation.ts drift on every edit — the collapsed-row skip rule is cited as `navigation.ts:345` but lives at :513; consider citing the rule/symbol name instead — `modules/ui-web/src/shell-v0/views/search-v3/Sv3Main.navigation.test.ts:13` (2026-08-25)
- [ ] check-controls-a11y reports a wrong line number for a finding (flagged Sv3Composer.ts:837, a CSS line, for a handler in the template at ~:1145) — neutralizeInterp/stripComments line accounting drifts, so the fix location has to be found by hand — `scripts/ci/check-controls-a11y.mjs:156` (2026-08-25)
- [ ] EnvelopeStream.test.ts heartbeat-watchdog reconnect case is load-flaky — failed once inside a full 453-file vitest run (expected 1 to be >= 2), green alone and on a clean full re-run — `modules/ui-web/src/shell-v0/streaming/EnvelopeStream.test.ts` (2026-08-25)
- [x] RESOLVED by tempdoc 860 P3 (PR #544): NavigationController.measureCoalesced latched — measureFrame was set true and cleared only from a requestAnimationFrame callback, so in a tab where rAF is suspended (hidden/off-screen/minimized) the flag never cleared and EVERY later measure was swallowed into measurePending (proven live: same build+record, hidden tab landmarks=2 measureFrame=true, visible window landmarks=7). The mechanism is gone: `measureFrame` no longer exists and the latch is the shared FrameLatch, whose rAF-primary release has a 1s elapsed-time fallback, adopted at all six rAF-latch sites. The harness-discipline half of this finding (browser-automation evidence from an off-screen MCP tab is not user-visible truth) stays OPEN above — that is 860 D4/D5's lane, not P3's — `modules/ui-web/src/shell-v0/primitives/frameLatch.ts` (2026-08-25)
- [ ] check-shape-handler-regen --live is a guaranteed false positive: it compares eventSchema entries (objects) with reference equality, so every shape reports DRIFT even when deep-equal — `scripts/ci/check-shape-handler-regen.mjs:81` (2026-08-25)
- [ ] jseval ui-shot step sv3-citation-selected can never pass on Windows: the cite_key (a Windows path full of backslashes) is interpolated raw into a CSS attribute selector, so CSS hex-escape sequences (\598-, \419-, \docs) mangle it — needs CSS.escape — `scripts/jseval/jseval/ui_check.py:1533` and `:1548` (2026-08-25)
- [ ] POST /api/chat/dispatch (the route Search v3 delegate runs actually stream over, via host.ai.streamShape) has no SSE heartbeat — only AgentController's routes wrap withHeartbeat — so any run parked >40s at a budget/context gate trips the FE STREAM_WATCHDOG_STALE_MS watchdog and the gate is lost ("Connection lost — the response was interrupted") — `modules/ui/src/main/java/io/justsearch/ui/api/routes/AiRoutes.java:73` vs `AgentController.java:131` (2026-08-25)
- [ ] budget_raised progress note is formatted with String.format("+%,d tokens") without Locale.ROOT, so on a German-locale JVM it renders "+7.777 tokens" in an English UI — `modules/app-agent/src/main/java/io/justsearch/agent/AgentStepRunner.java:961` (2026-08-25)
- [ ] budget_update events with phase iteration_start carry contextWindow:0 while llm_response events carry the real 4096 — an inconsistent wire fact on the same event type — `modules/app-agent/src/main/java/io/justsearch/agent/AgentStepRunner.java:254` (2026-08-25)
- [ ] GET /api/chat/conversations/{id}/history returns {"messages":[]} for a delegate conversation (storeBacked:false), so the legacy UnifiedChatView renders an empty transcript for delegate runs — 859 §5 C-persistence still open on the legacy path (2026-08-25)
- [ ] Search v3 record opens with navigation focus already pinned to the LAST row, so a fresh 'j' press clamps immediately instead of walking from the top (observed live in a visible window during the 859 A/B; unrelated to the pin-release fix) — `modules/ui-web/src/shell-v0/primitives/navigation.ts:252` (2026-08-25)
- [ ] HelpSurface's keyboard-shortcut comment is stale: it claims 'the only registered keybindings are mod+k / mod+z / mod+shift+z' but Shell also registers mod+l and '/' (both shell.focus-composer) — `modules/ui-web/src/shell-v0/views/HelpSurface.ts:67` vs `modules/ui-web/src/shell-v0/chrome/Shell.ts:950-961` (2026-08-25)
- [ ] hook-integrity gate has no file->manifest direction: an orphaned hook file in scripts/agent-analytics/hooks/ that is absent from governance/agent-hooks.v1.json is never flagged (found via ui-shot-cleanup.mjs, unwired yet documented as live) — `scripts/governance/gates/hook-integrity/enforcer.mjs` (2026-08-25)
- [ ] ConfirmDialog's Enter guard is defeated by shadow retargeting: the listener is on document (`ConfirmDialog.ts:164`) but tests `e.target` (`:206-207`), which retargets to the <jf-confirm-dialog> host — never INPUT — so pressing Enter WHILE TYPING the typed-confirm phrase also fires confirm(). Destructive-action dialog; the other three typing-guards in the app use composedPath()[0] or deepActiveElement() precisely to avoid this — `modules/ui-web/src/shell-v0/components/ConfirmDialog.ts:206` (864 census) (2026-08-25)
- [ ] Two unrelated tests both named 'T11' across the 859 PR-1/PR-1b files (delegate-question reconcile vs truncated-run disclosure) — ambiguous when a doc cites 'T11' — `modules/ui-web/src/shell-v0/views/search-v3/sv3-record.evidenceProjection.test.ts:191` and `modules/ui-web/src/shell-v0/views/search-v3/SearchV3View.delegateEvidence.test.ts:539` (2026-08-25)
- [ ] Ctrl+Z has two independent authorities: AdvisoryToastHost binds it on document unconditionally, app-wide, preventDefault without stopPropagation (`AdvisoryToastHost.ts:245-257`), while KeybindingRegistry separately registers mod+z -> shell.undo (`Shell.ts:926-931`). One chord, two mechanisms — `modules/ui-web/src/shell-v0/components/advisory/AdvisoryToastHost.ts:245` (864 census) (2026-08-25)
- [ ] toolSearchCard sets SearchProvenance.matchCount = resultCount, claiming total-matches equals returned-count; a real search has matchCount >= resultCount, so the agent card's provenance header may overstate/understate corpus matches — pre-existing, found during 865 survey — `modules/ui-web/src/shell-v0/components/chat/toolSearchCard.ts:92-101` (2026-08-25)
- [ ] Live leaked ui-shot Vite found during 861 chartering: PID 41064 on port 5174, parent PID 9444 dead, serving MAIN's modules/ui-web and holding main's node_modules; its record (provenance.head cb95fc3b, main is at 2a99dcd6) survives from an earlier session at scripts/jseval/tmp/ui-shot-server.json — left running deliberately as evidence, needs a manual Stop-Process — `scripts/jseval/jseval/ui_shot.py:309` (2026-08-25)
- [ ] hook-integrity gate's own tests run in CI nowhere: enforcer.test.mjs + truth-table.test.mjs are manual-only (run-all-tests.mjs discovery is scoped to scripts/agent-analytics/**, ci.yml:118-119), so the mechanism guarding the hook layer has no automated verification — same shape as the unwired ui-shot-cleanup defect one level up — `scripts/governance/gates/hook-integrity/enforcer.test.mjs` (2026-08-25)
- [ ] The 'navigate' Effect writes a BARE surface id into the hash: `window.location.hash = effect.to` where effect.to is e.g. 'core.unified-chat-surface', producing '#core.unified-chat-surface' — which URLSource.parseHash rejects outright (it requires a '#justsearch://' prefix, `router/sources/URLSource.ts:100-101`). It also fires AFTER the router already pushed the correct canonical URL, so a palette/action-driven navigation corrupts a good hash into an unparseable one; nothing re-reads it (no hashchange listener exists anywhere in modules/ui-web/src) — `modules/ui-web/src/shell-v0/substrates/actions/index.ts:459-462` (864 audit) (2026-08-25)
- [ ] FE-only surfaces are silently evictable by any wire catalog refresh: rebuildIndex REPLACES entriesById wholesale (`api/registry/SurfaceCatalogClient.ts:248-256`) while mergePluginSurfaceContributions only copies it (`:462`), and the merge site's comment (`:470-472`) documents a boot-ordering assumption nothing enforces. core.search-v3-surface is FE-only (declared at CorePlugin.ts:99-106; zero hits for 'search-v3' in modules/**/*.java), so a late catalog fetch evicts THE primary search window: stage renders 'No surface selected.' with activeId+hash untouched, and re-navigation dies at isKnownSurface (navigationHandler.ts:129-132) with no rail button (placement DEEPLINK) — reload is the only recovery. 852 promoted sv3 without adding it to the Java CoreSurfaceCatalog — `modules/ui-web/src/api/registry/SurfaceCatalogClient.ts:248` (864 audit, ranked-second hypothesis) (2026-08-25)
- [ ] Locked conversation store silently drops the agent-run record too: AgentRunStore.startRun's writeMeta throws KeyLockedException and the bare catch swallows it, so the run executes with no durable meta and the reader is told nothing — `modules/app-agent/src/main/java/io/justsearch/agent/AgentRunStore.java:192-194` (2026-08-25)
- [ ] Shipped code cites phantom sections of tempdoc 859 (which has only §0-§8): '§9(c)' in two sv3 test files — `modules/ui-web/src/shell-v0/views/search-v3/sv3-record.evidenceProjection.test.ts:196` (2026-08-25)
- [ ] Boot-time race can evict every plugin-contributed surface: bootSurfaceRegistry has TWO call sites (`main.jsx:134` awaited, `i18n.ts:56` inside a Promise.all) and its guard `if (bootAttempted && entriesById.size > 0) return` (`:284-287`) is not re-entrancy-safe — i18n's call sets bootAttempted while its fetch is in flight, entriesById is still empty so main.jsx's call proceeds, populates, and mergePluginSurfaceContributions adds the FE-only surfaces; then i18n's late resolution calls rebuildIndex (`:363`) which REPLACES the map, evicting them. core.search-v3-surface is FE-only, so Search v3 can be missing from boot with activeId+hash intact, unreachable (isKnownSurface fails, placement DEEPLINK so no rail button) — `modules/ui-web/src/api/registry/SurfaceCatalogClient.ts:284` (864 rev 2) (2026-08-25)
- [ ] 849's suppressGroundingFor is applied at render only, not in the panel's tier grouping — a 'dropped' source whose similarity computed 'high' still sits under the 'Grounds the answer' heading while its card is forbidden from stating the claim — `modules/ui-web/src/shell-v0/components/chat/CitationsPanel.ts:606-612` (2026-08-25)
- [ ] AgentContextCompressor can strip a tool message's Excerpt: lines and write the result back with NO [compressed-tool-output] marker when the stripped text falls below minChars — so marker-based detection of 'this result lost its excerpts' under-reports — `modules/app-agent/src/main/java/io/justsearch/agent/AgentContextCompressor.java:85-111` (2026-08-25)
- [ ] A delegate run whose AgentCitationResolver times out returns Resolved.none() (ScorerKind.NONE), so admittedMatches yields [] and the Sources panel renders 'Retrieved · not cited' on every source — a matcher verdict for a matcher that errored — `modules/app-agent/src/main/java/io/justsearch/agent/AgentCitationResolver.java:117-123` (2026-08-25)
- [ ] check-shape-handler-regen.test.mjs is not fail-closed: if the main-guard regresses, importing the module runs the static check and process.exit(0)s before any assertion — CI step goes green with zero assertions run; one-line fix is asserting the passed-count sentinel (PR #536 review finding A) — scripts/ci/check-shape-handler-regen.test.mjs:113 (2026-08-25)
- [ ] 9 of 22 scripts/ci/*.test.mjs are wired into no workflow — same dead-layer failure mode ci.yml:113-117 documents for agent-analytics; an auto-discovery runner for scripts/ci/*.test.mjs would generalize PR #536's one-off wiring — .github/workflows/ci.yml:272 (2026-08-25)
- [ ] sv3 Retry refuses SILENTLY when a turn genuinely has no recorded question (a record turn no user item opened): returns with no toast and no explanation, so the affordance looks broken rather than unavailable — independent of 863 B's reconcile gate — `modules/ui-web/src/shell-v0/views/search-v3/SearchV3View.ts:1473" (2026-08-25)
- [ ] sv3 Markdown export emits an EMPTY user block for a turn with no recorded question — it maps every settled turn to a user+assistant pair unconditionally, unlike the context inspector which guards on `question !== ''`; independent of 863 B's reconcile gate — `modules/ui-web/src/shell-v0/views/search-v3/SearchV3View.ts:2371" (2026-08-25)
- [ ] ui-web unit suite: PluginLoader.test.ts 'module-mode: an ES-import plugin resolves @kernel/data and installs' times out at 5000ms under full-suite load but passes in isolation and on a second full run — load-dependent flake, needs a larger testTimeout or a deterministic module-map stub — `modules/ui-web/src/shell-v0/plugin-api/PluginLoader.test.ts:141" (2026-08-25)
- [ ] PluginLoader module-mode ES-import test timed out (5s) once under full ui-web suite load, passes in isolation and on re-run — load-sensitive flake — `modules/ui-web/src/shell-v0/plugin-api/PluginLoader.test.ts:141` (2026-08-25)
