# Observations shard — session bccfc163-7b8f-4b1a-b9e4-0c011632d8a1

> Per-session inbox shard (tempdoc 618 Seam C). Append-only; do not share with
> other sessions. Folded into docs/observations.md `## Inbox` by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] check-contrast-matrix parseBlock resolves only the FIRST block per selector; tokens.css declares [data-theme="light"] twice (layered at :425, unlayered ambient overrides at :848) so a future role/surface token added to the second block would be silently ignored by the gate — `modules/ui-web/src/styles/tokens.css:848` (2026-08-19)
