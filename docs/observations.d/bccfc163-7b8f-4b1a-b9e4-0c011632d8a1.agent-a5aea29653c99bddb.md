# Observations shard — session bccfc163-7b8f-4b1a-b9e4-0c011632d8a1 (tree agent-a5aea29653c99bddb)

> Per-session-per-tree inbox shard (tempdoc 618 Seam C, keyed by the writing
> tree per tempdoc 862). Append-only; one writer, one file — do not append to
> another tree's or session's shard. Folded into docs/observations.md by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] AgentCitationResolver drops CitationMatchResult.sourceCoverage at its return (same shape of defect 859 §4 fixed for the scorer stamp), so the delegate plane cannot report BUDGET-starved unexamined sources — only the structurally-unexaminable subset 865 PR-1 derives FE-side; carrying the producer's real per-source report needs a new `done` wire field — `modules/app-agent/src/main/java/io/justsearch/agent/AgentCitationResolver.java:116` (2026-08-25)
- [ ] AgentStepRunner.executeIteration stores the PRE-stamp OperationResult in AgentSession.executedTools while emitting the grounding-stamped copy; harmless today (executedTools is only read for size/anyMatch-success since 865 made the accumulator the grounding authority) but a future reader of executedTools' structuredData would see a result the wire never carried — `modules/app-agent/src/main/java/io/justsearch/agent/AgentStepRunner.java:949` (2026-08-25)
