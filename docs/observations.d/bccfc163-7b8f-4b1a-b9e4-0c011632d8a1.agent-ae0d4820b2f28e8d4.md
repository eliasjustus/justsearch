# Observations shard — session bccfc163-7b8f-4b1a-b9e4-0c011632d8a1 (tree agent-ae0d4820b2f28e8d4)

> Per-session-per-tree inbox shard (tempdoc 618 Seam C, keyed by the writing
> tree per tempdoc 862). Append-only; one writer, one file — do not append to
> another tree's or session's shard. Folded into docs/observations.md by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] ui-web full-suite flake: PluginLoader 'module-mode: an ES-import plugin resolves @kernel/data and installs' timed out at 5000ms under full `npm run test:unit:run` load, passes in isolation — 5s default testTimeout is too tight for a dynamic-import test when 457 files run in parallel — `modules/ui-web/src/shell-v0/plugin-api/PluginLoader.test.ts:141` (2026-08-26)
