# Observations shard — session bccfc163-7b8f-4b1a-b9e4-0c011632d8a1 (tree agent-af06f4a51e146624b)

> Per-session-per-tree inbox shard (tempdoc 618 Seam C, keyed by the writing
> tree per tempdoc 862). Append-only; one writer, one file — do not append to
> another tree's or session's shard. Folded into docs/observations.md by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] hook-integrity gate fails in freshly-prepared worktrees: 6 unwired-hook findings (merge-full-suite-hint, dataset-cache-hint, exec-substrate-hint, mcpb-repack-hint, cwd-hint, edit-reread-hint) because the seeded gitignored settings.local.json lags .claude/settings.local.json.example, which does wire them — `scripts/dev/prepare-worktree.cjs` (2026-08-25)
