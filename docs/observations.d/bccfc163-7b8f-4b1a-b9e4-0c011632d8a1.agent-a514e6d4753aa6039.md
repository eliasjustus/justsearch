# Observations shard — session bccfc163-7b8f-4b1a-b9e4-0c011632d8a1 (tree agent-a514e6d4753aa6039)

> Per-session-per-tree inbox shard (tempdoc 618 Seam C, keyed by the writing
> tree per tempdoc 862). Append-only; one writer, one file — do not append to
> another tree's or session's shard. Folded into docs/observations.md by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] dead-code kernel gate passes on a STALE tmp/knip-report.json (green-by-staleness) — regenerating the report in a clean tree yields 31 findings, so the gate's green attests to the cached artifact, not the tree — `scripts/governance/gates/dead-code/` (routed by 860 P3 independent review) (2026-08-25)
- [ ] npm-audit, dead-code-jvm, module-deps and dead-code report kernel/input-missing as FAIL in a fresh worktree instead of not-evaluated — an environmental four-gate false delta that reads as a regression to any agent running the kernel outside the main checkout — `scripts/governance/run.mjs` (routed by 860 P3 independent review) (2026-08-25)
