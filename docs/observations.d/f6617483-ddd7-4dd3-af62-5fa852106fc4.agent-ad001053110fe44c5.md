# Observations shard — session f6617483-ddd7-4dd3-af62-5fa852106fc4 (tree agent-ad001053110fe44c5)

> Per-session-per-tree inbox shard (tempdoc 618 Seam C, keyed by the writing
> tree per tempdoc 862). Append-only; one writer, one file — do not append to
> another tree's or session's shard. Folded into docs/observations.md by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] Second ingest surface KnowledgeSearchController.handleIngest has no root containment and no operation-gate consent posture (independent of the agent tool path fixed in 875) — `modules/app-services/.../KnowledgeSearchController.java` (2026-08-26)
- [ ] Closest-existing-ancestor realpath containment now exists in three places (FileOperationExecutor, AgentToolPaths, IndexedRootGrantScope) across two modules; 877 centralises only within app-agent — the app-services copy needs a cross-module home (2026-08-26)
- [ ] 875 Move 2: IndexedRootGrantScope has a silent-wrong-value law (fail closed on every containment uncertainty) and is a candidate logic-seams.v1.json registration; not added here because governance/logic-seams.v1.json is shared with concurrent worktree agents — `modules/app-services/src/main/java/io/justsearch/app/services/intent/IndexedRootGrantScope.java:72` (2026-08-26)
- [ ] BrowseTool.formatResults' HINT branch is unreachable — it requires a non-absolute parent_path, but execute() rejects every non-absolute parent_path in validateParentPath before formatResults runs (was already dead before 875 Move 4, which only widened the rejection) — `modules/app-agent/src/main/java/io/justsearch/agent/tools/BrowseTool.java:217-227` (2026-08-26)
