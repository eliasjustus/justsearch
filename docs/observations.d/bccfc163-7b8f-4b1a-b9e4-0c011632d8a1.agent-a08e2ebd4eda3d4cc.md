# Observations shard — session bccfc163-7b8f-4b1a-b9e4-0c011632d8a1 (tree agent-a08e2ebd4eda3d4cc)

> Per-session-per-tree inbox shard (tempdoc 618 Seam C, keyed by the writing
> tree per tempdoc 862). Append-only; one writer, one file — do not append to
> another tree's or session's shard. Folded into docs/observations.md by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] execution-surface gate's tsRefPattern is a bare string match, so a PROSE mention of AnswerEvidenceSource in a javadoc-style comment (no import, no usage) fails the gate as an undeclared surface — hit while documenting a wire type — `governance/execution-surfaces.v1.json:scan.tsRefPattern` (2026-08-25)
- [ ] flaky under full-suite load only, green in isolation: app-services 'watched-root scan — collection carriage ... addWatchedRoot's scan reaches the wire' (in-process gRPC) and ui-web PluginLoader.test.ts 'module-mode: an ES-import plugin resolves @kernel/data' (5s vitest timeout) — `modules/ui-web/src/shell-v0/plugin-api/PluginLoader.test.ts:141` (2026-08-25)
- [ ] ts-any ratchet has 4 undeclared silent-growth entries on main (citationResolve.test.ts, indexingProgress.ts, sv3-sessions.test.ts, searchResultViewModel.ts, each 0 to 1) — the gate is red for the next unrelated PR that runs it — `scripts/ci/ts-any-baseline` (2026-08-25)
