# Observations shard — session 5acf8350-d85a-4969-bc46-c412985c9ccf

> Per-session inbox shard (tempdoc 618 Seam C). Append-only; do not share with
> other sessions. Folded into docs/observations.md `## Inbox` by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] docs/reference/model-inventory.md reranker row says 'GPU default (mem=2048MB, seq=512)' while D-001's rationale claims maxSequenceLength was changed 512->8192; the shipped resolved default is 512 (`ResolvedConfigBuilder.java:1264`) — D-001's line is stale — `docs/reference/search-quality-register.md` D-001 (2026-08-19)
- [ ] CE reranker input at the shipped 512-token cap is dominated by the QUERY on legal-clerc-200 (citation queries ~400 tok), leaving ~100 tok of evidence preview; offline reproduction of the F-052 arm-A legal level (0.5788) requires ~1024 tok, suggesting the 512 default silently starves the CE on long-query corpora — `ResolvedConfigBuilder.java:1264` (2026-08-19)
- [ ] RAG-path chunk cross-encoder has the same silent-drop shape as the interactive path had: a deadline miss logs at DEBUG and returns ChunkRerankResult.unchanged with no wire signal on RetrieveContextResponse — no rag_meta/reason-code field carries it — `modules/worker-services/src/main/java/io/justsearch/indexerworker/services/RagContextOps.java:1307-1314` (2026-08-19)
- [ ] The `<jf-search-trace>` search-explain panel is orphaned: SearchSurface (its only mount, `renderExplainPanel`) retired in Search Thread S5b, so the whole aggregate strategy — worded degradation reasons, QPP, the diagnostic stage list — renders nowhere in the shipping UI; only ResultsCard's meta line projects the trace — `modules/ui-web/src/shell-v0/aggregate-substrate/components/JfSearchTrace.ts:60` (2026-08-19)
- [ ] check-controls-a11y is RED on main and is NOT in expected-state.v1.json: 1 title-on-disabled instance over baseline 0 — `modules/ui-web/src/shell-v0/views/UnifiedChatView.ts:2137` (2026-08-19)
- [ ] Live datapoint for 844 D3/845 run-registry: a dev-runner run started via the CLI (node scripts/dev/dev-runner.cjs start, needed for env-var propagation the MCP start cannot do) carries owner.source=unknown/agentSessionId=null in run.json - a peer session's contention logic legitimately stale_reclaim'ed an ACTIVELY SUPERVISED run mid-campaign (run 8fd7b6df, 2026-08-19 14:34:51Z, killed 26s after the observed arc hit terminal). Either the CLI should stamp session identity like the MCP layer, or MCP start should accept env passthrough so campaigns don't need the CLI - scripts/dev/dev-runner.cjs / scripts/dev/justsearch-dev-mcp (2026-08-19, 825 live leg) (2026-08-19)
- [ ] LambdaMartBenchmarkTest flaky perf threshold (p50 latency 44.96ms > 5ms) failed during unrelated build -x test run, unrelated to tempdoc 854 W1 fusion-constant work — `modules/app-services/src/integrationTest/java/io/justsearch/app/services/gpl/LambdaMartBenchmarkTest.java` (2026-08-19)
- [ ] SupervisionDecision.backoffMs has the same silent shift-overflow guard bug 825 fixed in BootRecoveryDecision (scaled<0 misses base<<62==0); unreachable today because maxRestartAttempts is 3 — `modules/app-services/src/main/java/io/justsearch/app/services/worker/SupervisionDecision.java:98-108` (2026-08-19)
- [ ] Discipline-gate kernel protocol gap, measured live: a count-ratchet declared-growth changeset licenses growth ONLY while it is in the current diff vs the baseline ref (changeset-loader.mjs diffAddedModifiedFiles), so the moment the growth PR merges WITHOUT advancing gates/<id>/baseline.txt in the same PR, main goes red for every subsequent merge-group entry (undeclared-growth) until a separate advance PR lands - cost today: PR 515 dropped from the queue twice, 512 would have followed; unblocked by #518. Fix options: the gate README/skill should state advance-in-same-PR as mandatory, or --suggest-changeset should also stage the baseline advance, or the loader should honor changesets already ON the baseline ref until consumed - scripts/governance/lib/changeset-loader.mjs / .claude/skills/governance/SKILL.md (2026-08-19)
- [ ] F-053 follow-up from a recency sweep (2026-08-19): two candidates the CE upgrade screen should add before any live A/B decision - (1) mmBERT-based rerankers now EXIST (the F-053 watch item materialized as community fine-tunes, e.g. llm-semantic-router/mmbert-rerank-32k-2d-matryoshka, 32K ctx, 1800+ langs, encoder-only = keeps the incumbent's speed/ONNX/no-chat-template virtues; quality/license/training rigor unverified); (2) Contextual AI Reranker v2 (open-sourced ~2025-10, instruction-following) is absent from F-053's table AND exclusions - possible screen gap, needs license/size check. No major eligible release found in 2026-06..08 itself - docs/reference/search-quality-register.md F-053 (2026-08-19)
- [ ] Full jseval pytest suite (scripts/jseval) fails collection on 4 test files needing optional extras not installed in this environment: test_agent_behavioral_789.py, test_delivery_tier_735.py, test_partial_usage_capture_757.py (missing `inspect_ai`, the [agent] extra) and test_suite_stats_properties.py (missing `hypothesis`, the [dev] extra) — `pip install -e scripts/jseval[agent,dev]` likely resolves it; pre-existing, unrelated to Q-020 work. (2026-08-19)
- [ ] CE skip reason is mislabeled: GrpcSearchService reports skipReason=DEADLINE_EXCEEDED for ANY RerankedResult.skipped(), including the OrtException catch â€” a reranker BFCArena OOM at CE window>20 (batch bucket 64x512 needs ~600MB, default JUSTSEARCH_RERANK_GPU_MEM_MB=2048 leaves ~121MB) is reported to ce_coverage as a deadline miss, sending investigators to raise the deadline (no effect). Measured 199/200 queries, tempdoc 854 W2b campaign â€” `modules/worker-services/src/main/java/io/justsearch/indexerworker/services/GrpcSearchService.java:487` + `modules/reranker/src/main/java/io/justsearch/reranker/CrossEncoderReranker.java:318-332` (2026-08-19)
- [ ] index.hybrid.leg_recall_complete_top_n is not independently raisable: spliceRecallComplete gets limit = searchLimit = max(requestedLimit, rerank.top_k) = 20 at defaults, so when protected-minus-fused >= limit it sets keepFused=0 and DISCARDS the entire fused prefix, replacing it with leg-iteration-order rescued hits. Measured (854 W2a, shared index): top_n 10->20 costs nDCG@10 legal -0.263 / enron -0.421 / scifact -0.052; top_n 10->50 on legal yields 0.0037 with comparable=false + ann_proof FAIL. A user or experiment raising this documented env var alone silently destroys ranking, with no warning â€” `modules/adapters-lucene/src/main/java/io/justsearch/adapters/lucene/runtime/HybridFusionUtils.java:97` (2026-08-19)
