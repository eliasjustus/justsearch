# Observations shard — session bccfc163-7b8f-4b1a-b9e4-0c011632d8a1 (tree agent-a59c7dd46205b00a7)

> Per-session-per-tree inbox shard (tempdoc 618 Seam C, keyed by the writing
> tree per tempdoc 862). Append-only; one writer, one file — do not append to
> another tree's or session's shard. Folded into docs/observations.md by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] governance kernel's summary line reports a gate whose generated INPUT is merely absent as `fail`, indistinguishable from a real violation: in a fresh worktree module-deps/dead-code/dead-code-jvm/contract-projection/config-surface all print `fail` and only the SARIF reveals ruleId `kernel/input-missing` (e.g. 'tmp/knip-report.json missing - produce it with ...'); a distinct `skipped-no-input` verdict would stop agents reading five environmental gaps as five violations — `scripts/governance/run.mjs` (2026-08-25)
- [ ] world-state.test.mjs failed once inside a full run-all-tests.mjs run and passed both in isolation and on a clean full re-run — it probes live world state, so a dev stack stopping mid-suite can flip it; zero coupling to the change under test (no 'reaper'/'agent-spawn' reference in world-state.mjs or its test) — `scripts/agent-analytics/world-state.test.mjs:29" (2026-08-25)
