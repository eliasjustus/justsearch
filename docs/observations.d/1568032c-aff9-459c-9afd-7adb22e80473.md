# Observations shard — session 1568032c-aff9-459c-9afd-7adb22e80473

> Per-session inbox shard (tempdoc 618 Seam C). Append-only; do not share with
> other sessions. Folded into docs/observations.md `## Inbox` by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] score-session.mjs reads outcome?.task_type from outcomes.ndjson, but post-622 that field lives under outcome.inference.task_type — the per-type ceiling lookup silently always resolves null — `scripts/agent-analytics/score-session.mjs:490,519` (2026-08-19)
- [ ] scripts/ci/test-preview-squash-message.mjs is invoked by no workflow or runner — run-all-tests.mjs only discovers scripts/agent-analytics/**, and ci.yml names only test-config-surface-dead-config.mjs — so the squash-message preview's tests are dead in CI (tempdoc 745's 'a layer nothing invokes is dead' class) — `.github/workflows/ci.yml:119` (2026-08-19)
- [x] CORRECTED + FIXED 2026-08-20 (was: "otlp-sink-ensure.test.mjs mkdirs a real empty tmp/agent-telemetry/"). That file creates nothing — it imports pure functions and never invokes main(), verified by running it alone repeatedly. hook-base.mjs does not eagerly create the dir either. The real cause was four OTHER hook tests (edit-reread-hint, edit-reread-integration, intervene, exec-substrate-hint) that legitimately write real per-session marker files under telemetryDir but whose finally blocks removed only the file, never the directory. All four now conditionally rmdir the directory when they created it. Logged here because the original note named the wrong file and would have sent the next reader to a clean one — `scripts/agent-analytics/hooks/edit-reread-hint.test.mjs` (2026-08-20)
- [x] CORRECTED + FIXED 2026-08-20. Finding stands: overhead-taxonomy.mjs ran main() at module load with no isDirectRun guard, so merely importing it executed a full session scan and wrote tmp/agent-telemetry/overhead-taxonomy.json; it is now guarded (`overhead-taxonomy.mjs:637`). The original note's justification — "every other analytics script guards this" — was FALSE. Counted 2026-08-20: 14 scripts use the `process.argv[1] === import.meta.url` guard (analyze-trends, baseline-economics, cache-efficiency, context-attribution, dev-tool-usage, fold-observations, merge-links, note-observation, observations-triage, outcome-session, record-merge, recover-merge-links, signature-census, world-state) and 7 still end in a bare top-level `main();` (aggregate-friction:83, analyze-session:1039, cost-session:294, evaluate-session:793, friction-timeline:155, generate-index:104, mine-friction:261). The guard is right for overhead-taxonomy because its main() writes a report file as an import side effect, not because a convention was universal — `scripts/agent-analytics/overhead-taxonomy.mjs:637` (2026-08-19, corrected 2026-08-20)
- [ ] Seven analytics scripts end in a bare top-level `main();` with no direct-run guard, so importing any of them for its exported helpers runs the whole CLI (aggregate-friction:83, analyze-session:1039, cost-session:294, evaluate-session:793, friction-timeline:155, generate-index:104, mine-friction:261). Not fixed with overhead-taxonomy — that would have been scope creep; logged so the guard convention can be made uniform deliberately — `scripts/agent-analytics/analyze-session.mjs:1039` (2026-08-20)
- [ ] overhead-taxonomy.mjs, aggregate-friction.mjs and friction-timeline.mjs each fork their own copy of loadExclusionMatcher/loadExclusions over friction-excluded-sessions.json, and all three still report a zero match as an observed exclusion (858 §7 fixed only baseline-economics.mjs) — `scripts/agent-analytics/overhead-taxonomy.mjs:117,459` (2026-08-19)
- [ ] Several scripts cite 'tempdoc 118' for the r=0.064 PHI finding, but no 118-* file exists on disk or anywhere in this repo's git history — so the citation is unresolvable *for a reader of the public repo*. It is NOT evidence 118 never existed: public history starts at the v0.1.0 release squash (29579e51, 2026-06-25), so it cannot reach anything pruned before publication, and tempdocs 262/264/272/276 discuss 118's content, which is positive evidence it did exist privately. The verifiable source for r=0.064 at N=116 is `277-scoring-calibration-and-reframing.md:65,111` (§C4); doc 21 was corrected to cite it, the ~5 code citations were not. RESOLVED 2026-08-20: every code citation of 118 is gone — most left with the files deleted by the PHI retirement, and the last one (`analyze-session.mjs:7`) now points at doc 21's Session Report Schema section; a tree-wide grep for `tempdoc[ -]?118` outside `docs/tempdocs/**` returns only doc 21's deliberate "not tempdoc 118" caveat — `scripts/agent-analytics/score-session.mjs:178` (2026-08-19, resolved 2026-08-20)
- [ ] WITHDRAWN (was: evaluate-session --fallback reads the flat `outcome.task_type` and so re-runs its heuristic on rows already carrying inference.task_type). Not drift: `evaluate-session.mjs:43` aliases `JUDGE_OUTCOMES_FILE as OUTCOMES_FILE`, so that loop iterates judge-outcomes.ndjson, whose rows ARE flat — `outcome-session.mjs:246` reads `judge.task_type` flat to build the inference block. The flat read is correct there. Kept as a record because this alias caused three separate agents in one session to misread which file is written; the de-aliasing is the real fix — `scripts/agent-analytics/evaluate-session.mjs:43` (2026-08-19)
- [ ] RESOLVED in 858 P2/P6: the three forked exclusion matchers and their two divergent match rules are consolidated into lib/telemetry-io.mjs (loadExclusionKeys/makeExclusionMatcher/fmtScopeExclusion); supersedes the earlier note in this shard — `scripts/agent-analytics/lib/telemetry-io.mjs:36` (2026-08-19)
- [ ] Dangling tempdoc-118 citation (hyphenated spelling, missed by the space-separated grep used for the other 4) — `scripts/agent-analytics/analyze-session.mjs:7` (docstring: 'matching the schema in tempdoc-118'); same unresolvable-for-public-readers issue as the other tempdoc 118 citations fixed in this file. FIXED 2026-08-20: the docstring now points at `docs/explanation/21-agent-analytics-pipeline.md` § Session Report Schema (v1), which is in-repo and verifiable (2026-08-19, resolved 2026-08-20)
- [ ] SUPERSEDED by the tempdoc 858 §4.5 PHI retirement: the two earlier notes in this shard about score-session.mjs (the outcome?.task_type field path, and the dangling tempdoc-118 citation at :178) name a file that no longer exists — score-session.mjs, correlate-signals.mjs, scores.ndjson and scripts/ci/check-agent-quality-trend.mjs were deleted. The r=0.064 finding now lives in `docs/explanation/21-agent-analytics-pipeline.md` § Retired: process-hygiene scoring, cited to 277 §C4 — `docs/explanation/21-agent-analytics-pipeline.md:125` (2026-08-19)
- [ ] outcome-session.mjs's rotation-horizon docstring cites `lib/telemetry-io.mjs:138` for 'reads only those two files', but line 138 is normalizeMergeLinkRow — loadEvents (the two-file reader) is near :185. Line-anchored citation drifted when telemetry-io grew the merge-link helpers; name-anchor it instead — `scripts/agent-analytics/outcome-session.mjs:166` (2026-08-20)
- [ ] fold-observations has no safe moment in a parallel-agent tree: it reads every shard then deletes them, so an append landing between read and delete is lost. Deferred three times across two days (2026-08-19 x2, 2026-08-20) — each time 2-3 sessions were busy and shards had mtimes within minutes. This is structural, not scheduling: with 3-4 concurrent agents the tree is never quiet. Fix: fold only shards whose owning session is not live (world-state knows) or whose mtime is older than a quiescence threshold, and report the skipped ones rather than silently folding a moving file — `scripts/agent-analytics/fold-observations.mjs` (2026-08-20)
