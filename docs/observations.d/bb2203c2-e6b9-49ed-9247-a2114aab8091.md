# Observations shard — session bb2203c2-e6b9-49ed-9247-a2114aab8091

> Per-session inbox shard (tempdoc 618 Seam C). Append-only; do not share with
> other sessions. Folded into docs/observations.md `## Inbox` by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] check-controls-a11y is RED on the main-derived base: UnifiedChatView has a disabled <button> carrying a title (title-on-disabled > baseline 0) — `modules/ui-web/src/shell-v0/views/UnifiedChatView.ts:2137` (2026-08-19)
- [ ] Flaky under full-suite load: EnvelopeStream heartbeat-watchdog reconnect test uses a real-timer 70ms wait and failed once in a full `npm run test:unit:run` (passes in isolation) — `modules/ui-web/src/shell-v0/streaming/EnvelopeStream.test.ts:488` (2026-08-19)
- [ ] check-controls-a11y gate is RED on main-based worktrees: UnifiedChatView title-on-disabled 1 > baseline 0 (file untouched by 855) — `modules/ui-web/src/shell-v0/views/UnifiedChatView.ts:2137` (2026-08-19)
- [ ] EnvelopeStream heartbeat-watchdog test is flaky under full-suite load (70ms wall-clock assertion fails in test:unit:run, passes standalone) — `modules/ui-web/src/shell-v0/streaming/EnvelopeStream.test.ts:488` (2026-08-19)
- [ ] Light-palette AA contrast failures (#717682 on #f8f9fc, 11px) in chat-view .trigger/.new-chat-btn, status-deck .key/.endpoint, walkthrough-card span — pre-existing, newly exposed behind the 855 settings modal in ui-shot settings-light; owned by those surfaces — `modules/ui-web/src/shell-v0/views/UnifiedChatView.ts` / `components/StatusDeck.ts` (2026-08-19)
- [ ] jf-enter-action-picker select lacks accessible name (axe select-name, critical; baselined for settings dark+light) — proper fix chartered in 855 P1 — `modules/ui-web/src/shell-v0/views/SettingsSurface.ts:1287` (2026-08-19)
- [ ] Pre-existing unused eslint-disable directive for no-console in handleActivateWorkspaceProfile's catch block — `modules/ui-web/src/shell-v0/views/SettingsSurface.ts:2868` (2026-08-19)
- [ ] happy-dom getBoundingClientRect always reports zero-height, so SettingsSurface.measureAnchors() (scroll-spy, re-run from updated() every render) resets activeAnchor to null on the next render after any jumpToAnchor call in unit tests — activeAnchor's final value is untestable in this env; tests should assert on scrollIntoView's target instead — `modules/ui-web/src/shell-v0/views/SettingsSurface.ts:2510-2532` (2026-08-19)
- [ ] ui-shot 'overflow' fact only detects DOCUMENT-level overflow — interactive elements clipped inside an overflow:hidden ancestor read as 'overflow none' (855 closure audit: the unscrollable settings nav hid 4 categories incl. the danger group at 1280x720 and every measured pass missed it; fingerprint was scrollableRegions missing the nav). Harness follow-up: flag interactive elements whose rect falls outside their nearest overflow:hidden ancestor — `scripts/jseval/jseval/ui_check.py` (2026-08-19)
- [ ] Dead StorageKey 'justsearch-high-contrast' declared in persistence/contract.ts but never read/written anywhere in modules/ui-web/src — a third, unused HC-persistence pathway alongside UISettings.highContrast (backend) and userConfig.accessibilityProfile.contrast (localStorage) — `modules/ui-web/src/persistence/contract.ts:32` (2026-08-19)
- [ ] secondary-views-behavior.md still describes the Settings Appearance tab's High-Contrast toggle syncing via a React-era `onHighContrastChange` prop; the control is now the Accessibility jf-switch and the prop never existed in shell-v0 — `docs/reference/secondary-views-behavior.md:410` (2026-08-19)
- [ ] POST /api/settings/v2 response omits indexPaths (returns []) while GET includes them — response-shape inconsistency, not a clobber (verified by follow-up GET); could mislead a client reading the POST echo — `modules/ui/src/main/java/io/justsearch/ui/api/SettingsController.java` (2026-08-19)
- [ ] ui-user-readiness.md:144 also claimed vimMode is client-side-only preference; corrected alongside the HC fix since secondary-views-behavior.md documents vimMode as a persisted backend UISettings field too — worth a wider staleness sweep of this readiness doc — `docs/reference/ui-user-readiness.md:144` (2026-08-19)
- [ ] SettingsSurface.renderLayout() uses .card/.card-row classes with NO corresponding CSS rules in the component's static styles (pre-existing, not touched by 855 R3 flat-row remediation) — the Layout picker renders unstyled buttons — `modules/ui-web/src/shell-v0/views/SettingsSurface.ts:renderLayout` (2026-08-19)
- [ ] check-declared-surfaces verifies a governed region MOUNTS the declared engine but not that the declared schema content stays in sync with the hand-path fallback — two 855 review rounds each caught a control shipped only on the dead fallback path (R2 cross-link row, R3 swatch trio). A content-parity check (declared body vs fallback renderer coverage) would make this class unrepresentable — `scripts/ci/check-declared-surfaces.mjs` (2026-08-19)
- [ ] SettingsSurface unit tests (e.g. SettingsSurface.v15.test.ts) never import renderers/registry.js, so any test drilling into <jf-declared-surface>'s nested content (jf-vertical-layout etc.) silently renders the DeclaredSurface fallback-for-no-renderer div instead of real content — only production boot registers the default renderer set. Fixed locally for SettingsSurface.highContrast.test.ts (fix-round F1) but other declared-surface-adjacent test files may have the same latent gap if they ever assert into that depth — `modules/ui-web/src/shell-v0/views/SettingsSurface.highContrast.test.ts:21` (2026-08-19)
- [ ] Settings window deep-link cold boot renders raw i18n keys (settings.group.general, settings.search.placeholder) — message catalogs boot async with no re-render, so any surface mounted before catalog arrival keeps raw keys permanently — `modules/ui-web/src/i18n.ts:30` (2026-08-19)
- [ ] 855 delta re-audit follow-ups (out of remediation scope): Skins/Editor member surfaces keep the pre-remediation card dialect inside the settings window (M5); theme grid is tab-through tiles vs the trio's arrow model — keyboard asymmetry (M2); plugin list nests per-row card chrome inside a composite panel (N7); Security member page scrolls in a nested container the window scroll-spy cannot track — harmless while it declares no sub-anchors (N4); jf-switch 42x22 target relies on the SC 2.5.8 spacing exception — human a11y judgment call (N1) — `modules/ui-web/src/shell-v0/views/SettingsSurface.ts` (2026-08-19)
- [ ] SettingsSurface unit tests never call initLayoutCatalog() (only modules/ui-web/src/main.jsx does at real boot) — listLayouts() silently returns [] in every settings-surface unit test unless a test explicitly calls it, which none did before 855 fix-round F2 S1 added the first Layout-picker test — `modules/ui-web/src/shell-v0/layout/LayoutManifest.ts:126` (2026-08-19)
- [ ] record-merge.mjs attributed session bb2203c2 to the LOCAL 'Merge remote-tracking branch origin/main' commit (c4a981c9) rather than the PR's squash commit (dbe3314d) that actually carries the Session-Id line — the durable ADR-0045 attribution is correct, the local analytics link is not; relevant to 856 merge-attribution-authority — `scripts/agent-analytics/record-merge.mjs" (2026-08-20)
- [ ] remove-worktree.cjs reported 'kill: taskkill /F /PID' for the same vite PIDs on two consecutive runs and still failed with 'failed to delete' — the taskkill did not actually terminate them; removal only succeeded after an explicit PowerShell Stop-Process on the four holders. Worth verifying the kill's exit status before proceeding to the delete — `scripts/dev/remove-worktree.cjs" (2026-08-20)
