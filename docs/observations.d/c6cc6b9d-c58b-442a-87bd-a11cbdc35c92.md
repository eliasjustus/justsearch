# Observations shard — session c6cc6b9d-c58b-442a-87bd-a11cbdc35c92

> Per-session inbox shard (tempdoc 618 Seam C). Append-only; do not share with
> other sessions. Folded into docs/observations.md `## Inbox` by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] The `publishing { repositories { … GitHubPackages … } }` blocks in `modules/app-api/build.gradle.kts:104` and `modules/api-contract-projection-java/build.gradle.kts` never fire — no CI workflow runs a publish task, and `io.justsearch` is not advertised as a consumable library in any doc or README. So no release artifact has ever been published. Tempdoc 793 retired Revapi partly on this basis (nothing exists for a baseline to compare against); the publishing blocks themselves were left in place as a separate decision — publish for real, or retire them as the same class of never-fired scaffolding. (2026-07-29)
- [ ] CI 'Windows-native tests' has only ~2.3x timeout headroom: timeout-minutes: 15 (.github/workflows/ci.yml:354) against a job that normally completes in ~6m25s. On PR #337 it hit the ceiling at 15m09s and was reported as CANCELLED (GitHub's label for a timed-out job), which reads as a mysterious cancel rather than a timeout — the same job had passed twice earlier the same day on the same code. GitHub-hosted windows-latest runners vary widely; consider raising the budget or splitting the step, and note that a timeout surfaces as 'cancelled' in gh pr checks, which is easy to misread as benign. (2026-07-29)
