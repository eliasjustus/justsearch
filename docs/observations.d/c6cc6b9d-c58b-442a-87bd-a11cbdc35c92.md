# Observations shard — session c6cc6b9d-c58b-442a-87bd-a11cbdc35c92

> Per-session inbox shard (tempdoc 618 Seam C). Append-only; do not share with
> other sessions. Folded into docs/observations.md `## Inbox` by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] Revapi is orphaned residue: no build script applies the plugin, yet config/revapi/{analysis,differences}.json + config/revapi/app-api-baseline.json and gradle.properties' 'Revapi baselines' comment all survive. Tempdoc 793 removed the settings.gradle.kts resolution repo that existed to serve it (it broke Dependabot's whole Gradle library lane); the remaining fingerprints are false authority and invite re-adding it — retire-with-a-sweep candidate. (2026-07-29)
