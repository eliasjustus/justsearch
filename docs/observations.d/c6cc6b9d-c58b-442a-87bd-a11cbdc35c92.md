# Observations shard — session c6cc6b9d-c58b-442a-87bd-a11cbdc35c92

> Per-session inbox shard (tempdoc 618 Seam C). Append-only; do not share with
> other sessions. Folded into docs/observations.md `## Inbox` by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] Revapi is a PARKED COMMITMENT whose trigger already fired — not residue to sweep. `docs/reference/contracts/api-evolution.md:71` states the baseline is "self-referential until first public release; becomes actionable for real Java API surface comparison then". The first public release shipped 2026-06-25 (v0.1.0), so the stated condition is met, yet no build script applies the plugin (verified across .kt/.kts/.gradle/.toml — the version catalog has no revapi entry either). What survives is scaffolding waiting to be switched on: `modules/app-api/config/revapi/analysis.json`, `config/revapi/app-api-baseline.json`, 63 revapi checksum entries in `gradle/verification-metadata.xml`, `gradle.properties:30`, and defensive tool-config exclusions in `build-logic/.../LockingConventionsPlugin.kt:19,66`. Decision needed — wire it up (the doc says it is now due) or retire it explicitly and sweep; leaving it dormant means the canonical doc asserts an API-evolution guard that does not run. NOTE: `LockingConventionsPlugin.kt:64` records that revapiOld resolves its baseline JAR from Maven Central, so this is independent of the GitHub Packages repo tempdoc 793 removed. (2026-07-29)
