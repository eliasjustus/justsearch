# Observations shard — session 776e10cd-eef9-4873-a027-1fc2887a334d

> Per-session inbox shard (tempdoc 618 Seam C). Append-only; do not share with
> other sessions. Folded into docs/observations.md `## Inbox` by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] check-api-client-regen freshness gate is not wired into CI or gradle — a stale generated apiRoutes.ts/route-manifest would fail nothing today (surfaced during MCP GET-405 route addition, 821 §O follow-up lane) (2026-08-13)
- [ ] wire gate cannot demand changesets for ADDITIVE contract changes: protobuf-truth-table.mjs:28-34 row 1 fires on buf-breaking=false and reports 'No contract changes' even with +23 added lines in contracts/wire/status.proto — pass reason literally false; also contracts/wire/.changesets/README.md:54 points at retired scripts/contract-governance/run.mjs (821 §P C3 review finding) (2026-08-13)
- [ ] LifecycleContractTest is load-flaky: two tests fail with java.net.http.HttpTimeoutException under concurrent machine load, pass in isolation and on re-run — `modules/ui/src/test/.../LifecycleContractTest.java:144,495` (2026-08-13)
- [ ] CI flake (hosted-runner class): system-tests 'Indexing → action-ledger projection coherence (550 Thesis III) > initializationError' TimeoutException on PR #419 whose diff touches only KnowledgeSearchEngine rerank + register docs — no plausible causal path; rerun issued — `modules/system-tests` (2026-08-13)
- [ ] CI init-timeout flake cluster: system-tests tier 'initializationError' hit 3 different tests across PRs #419/#420 within minutes (action-ledger coherence; two-batch starvation 798 B1; operation preview 550 F2) while main's identical-content runs were green — isolated-backend E2E boot budget vs hosted-runner variance, matches the 2.3x-headroom observation; consider a boot-budget bump or per-test retry in that tier — `modules/system-tests` (2026-08-13)
- [ ] run-gh.mjs checks-wait can exit 0 prematurely: after a push it saw only cla-assistant (pull_request_target, registers instantly) and declared PASS while the CI workflow's 11 pull_request lanes had not yet registered — its pre-poll should wait for the required-context SET (branch-protection contexts) not merely any-registered-check — `scripts/dev/run-gh.mjs` (observed on PR #423, 2026-08-13) (2026-08-13)
- [ ] CI system-tests: on initializationError, IsolatedBackendFixture preserves isolated-backend-failure.log / isolated-backend-worker.log in runner temp but the workflow only uploads test-results XML — the worker-side cause of a 90s /api/health timeout is undiagnosable post-hoc; consider adding the preserved logs to the integration-test artifact upload — `.github/workflows/ci.yml` (observed on PR #429 run 31730197618) (2026-08-13)
- [ ] ui-web gate set: three gates are main-red but NOT in expected-state.v1.json — check-controls-a11y (UnifiedChatView.ts:2089 title-on-disabled), gen-token-names --check (shell-v0/themes/token-names.generated.ts stale), strip-token-fallbacks --check (6 fallbacks in ActionLedgerView.ts + RecentsMenu.ts). Every ui-web-touching branch must re-prove these pre-existing; either fix the three or register them in expected-state — `governance/expected-state.v1.json` (found during PR #433 prep, 2026-08-13) (2026-08-13)
- [ ] isolated-backend 90s /api/health boot flake is recurring on hosted CI, elevated 2026-08-13: 3x OperationPreviewE2ETest (run 31730197618, PR #429) + 1x IngestStarvationE2ETest self-recovered-on-retry (run 31732439890, PR #430; failOnPassedAfterRetry=true correctly failed the lane). Port file appears in ~5s, health never 200s — worker-side cause; worker log preserved on runner but not uploaded (see companion note). Candidate small fix: upload failure logs artifact + reassess HEALTH_TIMEOUT_MS 90s for 4-vCPU runners — `modules/system-tests/.../IsolatedBackendFixture.java:73` (2026-08-13)
- [ ] isolated-backend boot flake ROOT CAUSE NARROWED (first captured worker.log, PR #434 run 31735720225, ActionLedgerE2ETest): worker boots fully in ~1s, then receives MmfWorkerSignalBus 'shutdown signal received' ~6s after start and cleanly self-terminates; health then 503s until fixture timeout. worker_signal.lock is dataDir-scoped (fresh random dataDir per test) so cross-test collision is excluded — the signal originates within the same run's Head/worker pair. NOT a slowness mode: budget raises cannot fix it, only in-run retry does. Head-side app log absent from preserve dir (only backend.log stdout + worker.log captured) — fixture should also copy runtime/*.log — `modules/indexer-worker/.../coordination/MmfWorkerSignalBus.java`, `modules/system-tests/.../harness/IsolatedBackendFixture.java` (2026-08-13)
- [ ] PRODUCTION BUG (boot-flake root cause, investigated 2026-08-13): transient KnowledgeServerBootstrap.start() failure is TERMINAL — catch at KnowledgeServerBootstrap.java:235 tears down the (healthy) worker via close(), HeadlessApp.java:447-453 pins worker DEGRADED with NO KnowledgeServerHealthMonitor started (only created when knowledgeServer != null, :434-438), lifecycle projects ERROR, /api/health 503s forever while Javalin keeps serving. Most-probable trigger (unconfirmed, needs Head log): validateWorkerPid 5000ms window consumed by a single cold 5s-deadline RPC so retries never iterate (KnowledgeServerConfig.java:68, RemoteKnowledgeClient.java:845-850). Fix direction: bootstrap retry or recovery monitor when knowledgeServer==null; don't tear down a live worker on PID-validation timeout. Fixture preserves the wrong Head log name (app.log; real: logs/headless-backend.log per modules/ui logback.xml:29) — diagnostics PR pending (2026-08-13)
- [ ] LifecycleReasonCode WORKER_SPAWN_FAILED ('worker.spawn.failed') is the generic fallback for every DEGRADED worker capability except restart_exhausted, so /api/health cannot distinguish a terminal spawn failure from a recoverable health-check-budget timeout that the monitor retries — `modules/ui/src/main/java/io/justsearch/ui/api/StatusLifecycleHandler.java:1105` (2026-08-13)
- [ ] H1 CONFIRMED (first captured headless-backend.log, run 31742266298, IngestionDiagnosticsContractTest, 2026-08-13): boot-flake root cause is verbatim 'IllegalStateException: PID validation timeout after 5000ms: expected PID 8556' at KnowledgeServerBootstrap.validateWorkerPid:344 <- start:201 <- HeadlessApp.tryStartKnowledgeServer:924; Head then writes shutdown to the healthy worker and pins lifecycle ERROR permanently (terminal DEGRADED, §O.4). Boot hint 'Ensure the indexer-worker module is built' is wrong for this cause — misleading operator guidance. Fix lane started: per-attempt RPC deadline inside the validation window + non-terminal bootstrap failure (2026-08-13)
