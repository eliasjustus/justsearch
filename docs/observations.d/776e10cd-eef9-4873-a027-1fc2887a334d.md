# Observations shard — session 776e10cd-eef9-4873-a027-1fc2887a334d

> Per-session inbox shard (tempdoc 618 Seam C). Append-only; do not share with
> other sessions. Folded into docs/observations.md `## Inbox` by
> `node scripts/agent-analytics/fold-observations.mjs`.
- [ ] Stale comment: McpToolSurface#parseFilters calls the collection scope "unused by the MCP search surface" although it is parsed and forwarded into KnowledgeSearchRequest.Filters — `modules/ui/src/main/java/io/justsearch/ui/api/mcp/McpToolSurface.java:1725` (2026-08-12)
- [ ] Flaky under machine load: LifecycleContractTest.statusReadinessDegradesIndexServingWhenThroughputStalls fails with HttpTimeoutException in a full-suite run but passes in isolation (GPU eval backend was running) — `modules/ui/src/test/java/io/justsearch/ui/api/LifecycleContractTest.java:495` (2026-08-12)
- [ ] Multi-leg search legs silently ignore request query_syntax=LUCENE: TextQueryOps.searchText/searchTextWithFilter parse SIMPLE-only, so a hybrid/degraded LUCENE-syntax request retrieves a different query than the user asked for (only the sparse-only shortcut honours it) — `modules/adapters-lucene/src/main/java/io/justsearch/adapters/lucene/runtime/TextQueryOps.java:630` (2026-08-12)
- [ ] Ctrl+L fires both shell.focus-composer (KeybindingRegistry, window-capture) and Shell.handleGlobalKey's legacy copy-url branch (document-capture) because neither listener calls stopPropagation — dual side-effect on one keypress, likely a leftover from the Search Thread S2 Ctrl+L reassignment — `modules/ui-web/src/shell-v0/chrome/Shell.ts:1934-1936` (2026-08-12)
- [ ] modules/ui-web/src/api/domains/agent.ts exports a correctly-typed, Zod-validated listAgentSessions()/AgentSessionSummary but it is never called anywhere in FE — AgentSessionController.loadSessions() reinvented an unvalidated fetch+cast instead, which is how the startedAt/state vs startedAtEpochMs/status field mismatch (tempdoc 821 §4) went unnoticed — `modules/ui-web/src/api/domains/agent.ts:172` (2026-08-12)
- [ ] health-readiness-contract.v1.md's READY sample shows `"reasonCode": null` but ReadinessComponentView is @JsonInclude(NON_NULL), so a null reasonCode is omitted from the wire — the pre-existing sample renders a field the API never emits — `docs/reference/contracts/health-readiness-contract.v1.md:26` (2026-08-13)
- [ ] GPU readiness can report DEGRADED/gpu.saturated from placeholder data when the Worker RPC fails: computeGpuActivityGate's processingJobsCount term zeroes on the fallback view, removing a saturation-suppression signal. 821 §3-C1 now labels it stale=true, but the verdict itself should arguably suppress saturation when worker activity is unknown — `modules/ui/src/main/java/io/justsearch/ui/api/StatusLifecycleHandler.java:1024` (2026-08-12)
- [ ] jseval env trap: scoop `python` (python313) lacks ir_measures/jseval deps — the working interpreter is the `py` launcher default (3.14); a detached Start-Process jseval run with bare `python` dies at import. Worth pinning in the jseval skill's worktree-invocation note (2026-08-12)
- [ ] jseval interpreter trap resolved: scoop python313 site-packages had lost ir_measures/ir_datasets (likely a scoop version-bump reset); restored via pip install ir_measures ir_datasets — scoop python is the canonical jseval interpreter (has click/httpx/inspect stack), py-launcher 3.14 and Astral 3.12 are NOT (2026-08-12)
- [ ] LIVE-CONFIRMED (821 needs-live session): the 'Semantic search degraded' banner's retrieval composite degrades on `lambdamart.not_configured` — a deliberately-absent, F-021-measured-harmful optional reranker pollutes the composite; meanwhile the embedding readiness dim reads READY (worker_health_check embedding_ready = encoder-loaded, not corpus-embedded) while compat REBUILDING + hybridFallback REBUILD_IN_PROGRESS was live — resolves obs:unanchored-drift-9 with mechanism — `StatusLifecycleHandler readiness composites + health_check semantics` (2026-08-12)
- [ ] LIVE-CONFIRMED severe (821 needs-live session): embedding fingerprint never persists — TWO consecutive plain dev-stack restarts each re-flagged compat REBUILDING with embeddingFingerprintStored EMPTY and reset re-embed coverage (9%→0.2%): installed base would re-embed the whole corpus on every launch; raises merge priority of worktree 819-fingerprint-boot-race — resolves obs:unanchored-general-28 as STILL-TRUE/worst-case (2026-08-12)
- [ ] LIVE-CONFIRMED (821 needs-live session): POST /mcp accepts foreign Origin header (200) — Streamable-HTTP spec Origin validation absent; GET /mcp 404 — confirms obs:localapiserver conformance gap — `modules/ui/src/main/java/io/justsearch/ui/api/LocalApiServer.java:567` (2026-08-12)
- [ ] LIVE-CHECKED (821 needs-live session): obs:unanchored-missing-2 partial — CE cold query executed 426ms no DEADLINE_EXCEEDED (not reproduced); /api/knowledge/status key set IDENTICAL warm vs post-restart, crossEncoderAvailable absent in BOTH (field no longer exists — schema-drift claim moot on current main) (2026-08-12)
- [ ] collection filter is case-sensitive with no normalization or value enumeration: keyword field (fields.v1.json:124-131), neither ProtoConverters.java:102-104 nor buildRagFilters lowercases (unlike meta_* :86-88) — 'Agent-History' silently matches nothing; now agent-advertised via MCP FILTERS_SCHEMA so worth a normalization or vocabulary hint — pre-existing on search, surfaced by 821 RAG-scoping review (2026-08-12)
- [ ] LIVE root-cause (Search v3 skeleton-stuck report): /api/shell-events/stream floods ~200KB/s on an 851-doc stack mid-re-embed — surface:indexing-jobs and surface:action-ledger each re-broadcast ~104KB FULL-STATE frames on every tick (no bounding/pagination/delta); FE enters Reconnecting… loop, live-delivered search results never land, skeletons persist; also the likely mechanism behind the earlier false 'Backend disconnected' banner. Backend stream-frame bounding needed — C1/C4-adjacent — `ShellEventsStreamController` / MultiplexedSseWriter channel sources (2026-08-12)
- [ ] TikaOcrRuntimeTest.languageProbeUsesPackagedTessdataPrefix fails locally with ocr.language_missing (no packaged tessdata on this machine) — same environmental class as the listed vdu-pdf-fixtures-local-env entry but NOT covered by it, so every full-suite run reads red for an unrelated reason — `modules/worker-services/src/test/java/io/justsearch/indexerworker/extract/TikaOcrRuntimeTest.java` (2026-08-13)
- [ ] Delegated subagent workers all resolve note-observation.mjs to the ORCHESTRATOR's session shard (docs/observations.d/776e10cd-….md written independently on 3+ worktree branches this session) — the shard-per-session durability invariant (618 Seam C) collapses for workers, producing guaranteed add/add merge conflicts; session-id resolution in telemetry-io should distinguish subagent contexts or workers should get per-worktree shard names (2026-08-13)
- [x] RESOLVED in the same change (register F-046 / tempdoc 821 §P): the Head composed a LUCENE-syntax query from RAW user text in the LLM query-expansion re-search. The original note recorded only the parse-error half; the worse half was SILENT MISPARSE (`covid -vaccine` inverting to an exclusion, `error:timeout` becoming a phantom field query, with `expansionApplied=true` and no degradation signal) on requests the user sent as SIMPLE. Both fixed: user half escaped in `mergeExpansion` (`#escapeLuceneSyntax`) + `RuntimeException` fallback to base results — `KnowledgeSearchEngine.java:788-796,1226-1258` (2026-08-13)
- [ ] Flaky: LifecycleContractTest#statusReadinessInferenceStarting failed once with HttpTimeoutException (4.1s) under a 5-suite parallel gradle run, green on isolated re-run — HTTP client timeout too tight for a loaded machine — `modules/ui/src/test/java/io/justsearch/ui/api/LifecycleContractTest.java:346` (2026-08-13)
- [ ] Flaky under machine load: LambdaMartBenchmarkTest#inferenceLatencyUnder5ms failed with p99 99.85ms vs 30ms threshold during a full `build -x test`, green on isolated re-run (4s) — wall-clock latency assertion has no load tolerance and no warmup guard — `modules/app-services/src/integrationTest/java/io/justsearch/app/services/gpl/LambdaMartBenchmarkTest.java:80` (2026-08-13)
- [ ] check-api-client-regen freshness gate is not wired into CI or gradle — a stale generated apiRoutes.ts/route-manifest would fail nothing today (surfaced during MCP GET-405 route addition, 821 §O follow-up lane) (2026-08-13)
- [ ] wire gate cannot demand changesets for ADDITIVE contract changes: protobuf-truth-table.mjs:28-34 row 1 fires on buf-breaking=false and reports 'No contract changes' even with +23 added lines in contracts/wire/status.proto — pass reason literally false; also contracts/wire/.changesets/README.md:54 points at retired scripts/contract-governance/run.mjs (821 §P C3 review finding) (2026-08-13)
- [ ] Repo-wide junit.jupiter.execution.timeout.default=30s governs EVERY @BeforeAll (class-level @Timeout covers testable methods only, never lifecycle) — any other module with a heavyweight @BeforeAll is exposed to the same silent 30s cap that flaked system-tests — `build-logic/src/main/kotlin/conventions/JvmBaseConventionsPlugin.kt:117` (2026-08-13)
- [ ] TRAP: a PR in CONFLICTING state never gets a `pull_request` CI run at all (GitHub cannot build refs/pull/N/merge), and the absence reads as a queue delay rather than a blocker — PR #429 sat with only CLA Assistant until origin/main was merged in, after which CI registered in ~2s; publication flows should check `gh pr view --json mergeable` before concluding CI is slow — `.github/workflows/ci.yml:9-13` (2026-08-13)
- [ ] Pre-existing UTF-8 mojibake (U+00E2 U+20AC U+201D instead of em dash) in two comments — `modules/app-services/src/main/java/io/justsearch/app/services/worker/RootLifecycleOps.java:293` and `:447` (2026-08-12)
- [ ] CORRECTED (was: "Javalin's exception mapper may drop the Host/token guards' deny bodies in production" — WITHDRAWN, that was a test-harness artifact: production registers `app.exception(HttpResponseException.class, …)` at `LocalApiServer.java:397-399` which preserves the handler-set body, and my hermetic app registered no mapper). What remains true: the security-filter tests re-declare a mirrored filter instead of exercising `ApiSecurityFilters.install()`, so they cannot catch a filter dropped from `install()` or registered on the wrong path — `modules/ui/src/test/java/io/justsearch/ui/api/LocalApiHostValidationTest.java:103-122` (2026-08-13)
- [ ] ApiSecurityFilters.resolveAllowedOrigin rejects IPv6 loopback origins (http://[::1]:port) because URI.getHost() returns the bracketed literal '[::1]' while LOOPBACK_HOSTS stores '::1'; isAllowedHost strips brackets but resolveAllowedOrigin does not — `modules/ui/src/main/java/io/justsearch/ui/api/ApiSecurityFilters.java:622,645,656` (2026-08-13)
- [ ] GET /api/mcp/token is token-exempt and not Origin-guarded (covered only by the Host allowlist + CORS), while it hands out the session token that gates every mutating API call — worth separate review now that /mcp itself validates Origin — `modules/ui/src/main/java/io/justsearch/ui/api/LocalApiServer.java:622` (2026-08-13)
- [ ] RESOLVED by the MCP conformance lane (this branch): GET /mcp now returns 405 + `Allow: POST, DELETE, OPTIONS` instead of 404 — closes ONLY the `GET /mcp 404` half of the 2026-08-12 live-confirmed note above; the `POST /mcp accepts foreign Origin` half was fixed on the sibling Origin-validation branch (worktree-agent-a9392a68dde8ec2cd), not here. Separately closes the 2026-08-13 note on the unguarded token bootstrap: GET /api/mcp/token is now Origin-guarded on the same allowlist as /mcp — `modules/ui/src/main/java/io/justsearch/ui/api/mcp/McpProtocolHandler.java:handleGet`, `ApiSecurityFilters.java:setupMcpOriginValidation` (2026-08-13)
- [ ] MCP conformance gap (unrelated to the GET/405 clause, found reviewing it): the spec's Protocol Version Header section requires the server to validate the `MCP-Protocol-Version` REQUEST header on every non-initialize request and answer 400 on an unsupported/invalid value; McpProtocolHandler only EMITS a version at initialize and never reads that header (its only ctx.header reads are Mcp-Session-Id at :81,:161), so an unsupported version is silently accepted — `modules/ui/src/main/java/io/justsearch/ui/api/mcp/McpProtocolHandler.java:37,217` (2026-08-13)
- [ ] No test boots the REAL LocalApiServer route table, so a route's Allow-header string can drift from its actual bindings undetected: zero test/integrationTest files call `new LocalApiServer`, and LegacyEndpointGuardTest re-declares a hand-maintained MIRROR of setupRoutes() (`LegacyEndpointGuardTest.java:183-187`) that omits the whole /mcp family — same mirror-instead-of-real class as the security-filter note above. Structural, pre-existing; surfaced by the GET /mcp 405 work whose `Allow: POST, DELETE, OPTIONS` literal is pinned only against a hermetic app — `modules/ui/src/main/java/io/justsearch/ui/api/mcp/McpProtocolHandler.java:183` (2026-08-13)
- [ ] BKD-005 (from retired docs/reference/issues, 821 D5): inference mode transitions (CPU->GPU, model swap) proceed without pre-validating model file existence / VRAM sufficiency / config consistency — invalid configs only fail late when llama-server won't start — `modules/app-inference/src/main/java/io/justsearch/app/inference/InferenceLifecycleManager.java` (2026-08-12)
- [ ] BKD-012 (from retired docs/reference/issues, 821 D5; matches 821 class C3/C4 stream-cancellation): streamChat is fire-and-forget (CompletableFuture.runAsync) with no cancellation token from caller -> OnlineAiService -> ILM -> OnlineModeOps -> HTTP; abandoned streams run to completion server-side — `modules/app-inference/src/main/java/io/justsearch/app/inference/OnlineModeOps.java` (2026-08-12)
- [ ] BKD-013 (from retired docs/reference/issues, 821 D5): user-configured exclude patterns are applied post-indexing via /api/indexing/excludes/apply, not during the Worker walk (hardcoded skip-dir pruning only) — excluded trees are still traversed+enqueued; needs a gRPC schema change to pass excludes to the walk phase — `modules/worker-services/src/main/java/io/justsearch/indexerworker/services/GrpcIngestService.java` (2026-08-12)
- [ ] BKD-014 (from retired docs/reference/issues, 821 D5): Windows junction points are traversed by walkFileTree (isSymbolicLink() is false for them) so the same file can be indexed twice under different doc IDs; no toRealPath()/attrs.isOther() check exists anywhere in the worker walk (verified 2026-08-12: grep for toRealPath/isOther in worker-services+indexer-worker returns nothing) — `modules/worker-services/src/main/java/io/justsearch/indexerworker/services/GrpcIngestService.java` (2026-08-12)
- [ ] BKD-015 (from retired docs/reference/issues, 821 D5): encrypted/corrupt/unsupported files are indexed with placeholder content 'File: [filename]' and there is no user-visible signal (no /api/status unindexable count, no UI surface) that a file could not be extracted — `modules/indexer-worker/` (2026-08-12)
- [ ] BKD-016 (from retired docs/reference/issues, 821 D5): SQLite job queue has write contention / WAL checkpoint stalls under bulk ingestion (10k+ files); reassess when ingestion benchmarks show >2x throughput regression vs a direct queue — `modules/indexer-worker/src/main/java/io/justsearch/indexerworker/queue/SqliteJobQueue.java` (2026-08-12)
- [ ] BKD-018 (from retired docs/reference/issues, 821 D5): no backup/restore path for the Lucene index — corruption (power loss, disk error, failed migration) forces a full reindex whose cost scales unbounded with collection size; a Lucene snapshot-based backup is the minimal viable fix — `modules/adapters-lucene/` (2026-08-12)
- [ ] BKD-022 (from retired docs/reference/issues, 821 D5; still live, re-verified 2026-08-12): SqliteJobQueue carries two error contracts — outcome-aware paths throw OutcomeWriteException while legacy paths catch SQLException and return success-shaped values (enqueue returns 0 at :341-343, pollPending returns List.of() at :436-438), so a transient write failure is indistinguishable from 'no work' — `modules/indexer-worker/src/main/java/io/justsearch/indexerworker/queue/SqliteJobQueue.java:341` (2026-08-12)
- [ ] BKD-023 (from retired docs/reference/issues, 821 D5; still live, re-verified 2026-08-12): once the extraction content cap is hit, every endElement early-returns (:138) and addElement drops elements (:279), so a </table>/</ol>/</ul> arriving after the cap silently loses the whole partially-built table/list — the final flush rescues only the text buffer (flushTextBufferFinal :269) — `modules/worker-services/src/main/java/io/justsearch/indexerworker/extract/StructuredContentHandler.java:279` (2026-08-12)
- [ ] BKD-024 (from retired docs/reference/issues, 821 D5; still live, re-verified 2026-08-12): IndexingJobsChangeStream.flushPending() delivers deltas synchronously in-line to every subscriber (:251-253) — a slow/backpressured subscriber (e.g. a stalled core.indexing-jobs gRPC consumer) can stall delta delivery, and possibly every queue mutation if this runs under the SqliteJobQueue write-lock (verify the call-chain before acting) — `modules/indexer-worker/src/main/java/io/justsearch/indexerworker/queue/IndexingJobsChangeStream.java:251` (2026-08-12)
- [ ] BKD-025 (from retired docs/reference/issues, 821 D5; UNVERIFIED static-audit hypotheses, confirm against source first): after a fast Worker crash-restart (a) the gRPC circuit breaker may stay OPEN for its ~10s cooldown with no reset from the restart path, (b) WorkerSpawner may not force the non-READY->READY edge KnowledgeServerHealthMonitor needs to re-run reindexPersistedRoots()/startPeriodicSync() (risking an empty index), (c) KnowledgeServerBootstrap.close() may leak its health monitor thread — `modules/app-services/src/main/java/io/justsearch/app/services/worker/WorkerSpawner.java` (2026-08-12)
- [ ] GPU-001 residue (from retired docs/reference/issues, 821 D5): AMD/Intel GPUs are not detected and surface as a generic 'No GPU detected' message rather than 'NVIDIA required' — the vendor lock itself is by design (cuda12-only, fails closed), but the misleading user-facing wording is the live part — `modules/gpu-bridge/src/main/java/io/justsearch/gpu/GpuCapabilitiesService.java` (2026-08-12)
- [ ] GPU-004 (from retired docs/reference/issues, 821 D5): NvmlService queries deviceCount but only ever reads device index 0, so multi-GPU systems report only the first GPU's VRAM and there is no way to select which GPU inference uses — `modules/gpu-bridge/src/main/java/io/justsearch/gpu/NvmlService.java:174` (2026-08-12)
- [ ] GPU-008 (from retired docs/reference/issues, 821 D5; still live, verified 2026-08-12 — no Temperature/PerformanceState query anywhere in gpu-bridge): only VRAM is monitored, so thermal throttling shows up as unexplained silent slowdown; NVML already exposes nvmlDeviceGetTemperature/PowerState/PerformanceState — `modules/gpu-bridge/src/main/java/io/justsearch/gpu/NvmlService.java` (2026-08-12)
- [ ] GPU-013 (from retired docs/reference/issues, 821 D5; still live, verified 2026-08-12 — no getenv(PATH)/pathSeparator scan in the file): warnIfCudaRuntimeMissing() checks only the server dir and System32 for CUDA DLLs, so a valid CUDA toolkit on PATH in a non-standard location produces a false 'CUDA runtime not found' warning — `modules/app-inference/src/main/java/io/justsearch/app/inference/CudaRuntimeDetection.java` (2026-08-12)
- [ ] INS-004 residue (from retired docs/reference/issues, 821 D5): ORT FP16-on-CPU was only partially fixed (EXTENDED_OPT -> BASIC_OPT); the deeper fix (ship FP32 CPU models or pre-optimize offline) is still open and tracked only in tempdoc 376, so CPU-only machines still get degraded/slow enrichment — `modules/ort-common/src/main/java/io/justsearch/ortcommon/OnnxSessionCache.java` (2026-08-12)
- [ ] INS-007 (from retired docs/reference/issues, 821 D5): modules/shell/src-tauri/target/release/nsis/ retains corrupted temp files across installer builds, causing a persistent NSIS 'Internal compiler error #12345' that survives retries until the directory is manually removed — candidate for expected-state.v1.json rather than a code fix — `scripts/ci/package-installer-win.ps1` (2026-08-12)
- [ ] UIX-006 successor (from retired docs/reference/issues, 821 D5): no automated accessibility check runs in CI, and the harness it referenced has since decayed — modules/ui-web/e2e/accessibility-audit.spec.ts no longer exists while scripts/ci/a11y-runner.sh survives as an orphan with nothing to run; either wire an a11y lane or sweep the orphaned runner — `scripts/ci/a11y-runner.sh` (2026-08-12)
- [ ] UIX-013 (from retired docs/reference/issues, 821 D5): Library folder cards show path, file count and last-modified but no file-type breakdown ('12 PDFs, 3 docs'); needs a backend per-folder type-distribution field before the FE can render it — `modules/ui-web/src/shell-v0/views/LibrarySurface.ts` (2026-08-12)
- [ ] ACC-003 (from retired docs/reference/issues, 821 D5; still present, verified 2026-08-12): match-reason pills come from computeMatchedFields(), a case-insensitive substring check against stored title/path values — it reports 'your terms appear in this field', not 'Lucene matched this field', so pills can imply title/path affect ranking when they do not — `modules/worker-services/src/main/java/io/justsearch/indexerworker/util/TextAnalysisUtils.java:88` (2026-08-12)
- [ ] 821 D5 retirement of docs/reference/issues/decisions.md — these closed-decision rationales had NO other home and are preserved here only as a stub for the orchestrator to place: GPU-012 (hardcoded CUDA DLL size thresholds 400MB/200MB are intentional, config would add noise), EXC-001 (exclude-patterns UI stays behind Advanced mode), EXC-002 (DirectoryWatcher methvin 0.18.0 has no filter API so watcher events are filtered at the handler, revisit if the lib adds one), INF-001 (no MCP wrappers for Gradle; bash fallback), INF-002 (no central test registry; naming convention suffices), ACC-002 (axe-core nested-interactive on result rows accepted — conditional tabIndex, matches VS Code/GitHub), UIX-010/011/012 (system tests, Playwright E2E and SSE streaming are not runnable in agent sessions — expected-state.v1.json material), UIX-015 (first run is NOT zero-corpus: 5 bundled help docs are auto-ingested into the justsearch-help collection by KnowledgeServerBootstrap.tryIngestHelpFiles) — `docs/observations.md` (2026-08-12)
- [ ] GrpcIngestService#submitBatch's force-reindex marking normalizes with PathNormalizer only, skipping Path.normalize() — FileFreshnessSnapshot.capture applies toAbsolutePath().normalize() first, so a submitted path containing a '..' segment produces a forcedPaths key the extractor never matches and the force silently no-ops (WorkerScanOps' 821 §3-C3 arm normalizes correctly) — `modules/worker-services/src/main/java/io/justsearch/indexerworker/services/GrpcIngestService.java:559` (2026-08-13)
- [ ] WatchedRootScanCollectionTest$ProductionWireForwarding intermittently fails the FULL app-services suite with 'Failed to close extension context' / DirectoryNotEmptyException on its @TempDir 'data' dir (Windows keeps the MainSignalBus memory-mapped signal file open past @AfterEach); passes in isolation — from the merged watched-root branch, not 821 §3-C3 — `modules/app-services/src/test/java/io/justsearch/app/services/worker/WatchedRootScanCollectionTest.java:195` (2026-08-13)
- [ ] Flaky under full-suite load (same HttpTimeoutException class as the LifecycleContractTest entries): UpgradeLifecycleContractTest#prepareFreezesMutationsKeepsReadsAndCommitRequestsOrderlyShutdown failed with HttpTimeoutException in a full `gradlew test`, green on isolated re-run — the ui module's HTTP-client timeouts have no load tolerance across the whole contract-test family, not just one class — `modules/ui/src/test/java/io/justsearch/ui/api/UpgradeLifecycleContractTest.java:67` (2026-08-13)
- [ ] check-controls-a11y is RED on main (not in expected-state.v1.json, unlike its theme-token-closure/accent-as-text siblings): UnifiedChatView.ts 1 title-on-disabled instance > baseline 0 — every worktree touching modules/ui-web/src/** reads this as a fresh finding and must re-prove it pre-existing — `modules/ui-web/src/shell-v0/views/UnifiedChatView.ts:2089` (2026-08-13)
