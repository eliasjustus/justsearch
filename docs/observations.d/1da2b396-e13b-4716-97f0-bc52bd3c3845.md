# Observations shard — session 1da2b396-e13b-4716-97f0-bc52bd3c3845

> Per-session inbox shard (tempdoc 618 Seam C). Append-only; do not share with
> other sessions. Folded into docs/observations.md `## Inbox` by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] execution-surface register had stale orphan entry `fe-generated-pb` → `modules/ui-web/src/api/generated/knowledge_pb.d.ts` (file removed by 683/daa74bd, entry left behind — gate red on main); removed it while adding the 658 mcp-evidence-projection surface — `governance/execution-surfaces.v1.json` (2026-07-07)
- [ ] capture_evidence crashes on Windows with a libuv fail-fast (`Assertion failed: !(handle->flags & UV_HANDLE_CLOSING), src/win/async.c`) after capturing api-status/api-health — blocks durable EvidenceBundle capture; live verification had to fall back to manual /mcp HTTP calls (tempdoc 658) — `scripts/…/capture-evidence` (MCP dev tool) (2026-07-07)
- [ ] contract-surfaces: registered InferenceStatusResponse with EMPTY consumers — its generated FE Zod type (`inference-status-response.ts`, 683-onboarded) has no parse-boundary consumer; 663 Stage-4's typed FE consumption is unfinished (`inferencePoll.ts` still uses untyped `mode: string`). Follow-up for 663/688: wire the FE to parse via `inferenceStatusResponseSchema` or drop the codegen target — `modules/ui-web/src/shell-v0/utils/inferencePoll.ts:13` (2026-07-07)
