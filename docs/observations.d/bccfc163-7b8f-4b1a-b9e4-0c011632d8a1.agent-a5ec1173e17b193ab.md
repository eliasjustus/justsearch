# Observations shard — session bccfc163-7b8f-4b1a-b9e4-0c011632d8a1 (tree agent-a5ec1173e17b193ab)

> Per-writer inbox shard (tempdoc 618 Seam C, keyed by session AND writing tree
> per tempdoc 862). Append-only; no other writer touches this file. Folded into
> docs/observations.md `## Inbox` by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] governance $schema pointers are a broken class, not one instance: `declaration-kinds.v1.json` and `sandbox-defect-classes.v1.json` name schemas that are not files at all; `design-reference.v1.json` points at a missing sibling; `registry.v1.json` (34 errors) and `agent-hooks.v1.json` (2) do not validate against the schemas they DO name. Found by a generic ajv walk while fixing the ui-a11y case; new `check-ui-baseline-schemas` is deliberately scoped to the two UI baselines because the rest needs its own owners — `governance/registry.v1.schema.json` (2026-08-25)
- [ ] RecentsMenu (global topbar chrome) styles against `--space-1`/`--radius-sm`/`--surface-raised`/`--border`/`--radius-md`, which are declared ONLY under a search-v3-scoped selector in `sv3-tokens.css.ts` — so in chrome they always resolve to the hardcoded fallback. check-theme-token-closure counts them as declared (it scans all declaration sites, not scope), so the gate cannot see it — `modules/ui-web/src/shell-v0/components/RecentsMenu.ts:92" (2026-08-25)
- [ ] expected-state pin `ui-web-typecheck-ts5101` may be stale: `npm run typecheck` in modules/ui-web ran clean (exit 0, no TS5101) on 2026-08-25 in a worktree off main. Its exitProbe is the same command, so a triage --probe run should retire it — `scripts/agent-analytics/expected-state.v1.json:6" (2026-08-25)
- [ ] consult-register's `ui-web-gates` recipe documents `run.mjs --gate ambient-purity,style-literal-ratchet,...` comma syntax, which run.mjs REJECTS — gates must be passed one at a time. Two agents hit this independently this wave; the recipe is the authority the consult hook pushes, so it teaches a command that fails — `governance/consult-register.v1.json:38` (2026-08-25)
- [ ] observations conditions `obs:theme-token-closure-red` (seen 8) and `obs:accent-as-text-red` (seen 6) are RESOLVED by PR #539 — both gates are green on main and their expected-state pins were retired. Left in the store rather than edited from a worktree (shared-file reset risk); resolve at the next fold — `docs/observations.md` (2026-08-25)
