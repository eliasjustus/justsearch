# Observations shard — session e18930bf-02d4-4718-84c1-be4f17d19963

> Per-session inbox shard (tempdoc 618 Seam C). Append-only; do not share with
> other sessions. Folded into docs/observations.md `## Inbox` by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] GET /api/chat/agent/history is vestigial for agent-run accountability: it reads FileOperationLog (file-mutation undo batches) only, while agent runs record to AgentRunStore + /api/action-ledger (tempdoc 561 P-B1); the endpoint name over-promises and a stale FE wrapper remains — `modules/ui-web/src/api/domains/agent.ts:148`, `modules/ui/src/main/java/io/justsearch/ui/api/AgentRoutes.java:84` (2026-08-05)
- [ ] remove-worktree partial failure leaves a GUTTED directory (contents+.git link deleted, dir shell locked): a later cd into it succeeds and git resolves UPWARD to the main checkout — commands intended for the worktree silently run against main (near-miss 2026-08-06: a catch-up merge aimed at a PR branch ran in main, blocked only by another agent's dirty file). remove-worktree should delete the .git link LAST or fail atomically — `scripts/dev/remove-worktree.cjs` (2026-08-06)
