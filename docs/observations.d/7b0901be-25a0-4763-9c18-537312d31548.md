# Observations shard — session 7b0901be-25a0-4763-9c18-537312d31548

> Per-session inbox shard (tempdoc 618 Seam C). Append-only; do not share with
> other sessions. Folded into docs/observations.md `## Inbox` by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] HybridSearchOpsTest.java confidentDense() comment says top 0.7 (>= 0.55) but ARBITRATION_DENSE_CONFIDENT_MIN was 0.5 pre-702 and is now 1/3 — stale/inaccurate comment, pre-existing, test still passes numerically — `modules/adapters-lucene/src/test/java/io/justsearch/adapters/lucene/runtime/HybridSearchOpsTest.java:302` (2026-07-10)
- [ ] computeLowSignalGatingUsesDefaultsWhenConfigNull / NullConfigHighSignalUsesDefaults comments say 'below/above default 0.40' but the real ResolvedConfigBuilder default is now 0.294 (tempdoc 702) — assertions still pass (0.2/0.9 are unambiguous either way) but the inline comment text is stale — `modules/adapters-lucene/src/test/java/io/justsearch/adapters/lucene/runtime/HybridSearchOpsTest.java:201,215` (2026-07-10)
