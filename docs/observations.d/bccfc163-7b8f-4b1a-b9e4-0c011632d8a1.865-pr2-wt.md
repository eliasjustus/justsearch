# Observations shard — session bccfc163-7b8f-4b1a-b9e4-0c011632d8a1 (tree 865-pr2-wt)

> Per-session-per-tree inbox shard (tempdoc 618 Seam C, keyed by the writing
> tree per tempdoc 862). Append-only; one writer, one file — do not append to
> another tree's or session's shard. Folded into docs/observations.md by
> `node scripts/agent-analytics/fold-observations.mjs`.

- [ ] ui-web full unit suite: PluginLoader ES-import module-mode case times out at 5000ms on a cold run (transform 33.9s) but passes in isolation in 2.6s — a cold-transform flake, not a logic failure; consider a longer testTimeout for that case — `modules/ui-web/src/shell-v0/plugin-api/PluginLoader.test.ts:141` (2026-08-25)
