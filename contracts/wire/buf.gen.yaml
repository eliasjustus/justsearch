# Emitter configuration for JustSearch wire-Category contract.
# See ADR-09a §"Decision" for the rationale of this emitter set.
#
# Tempdoc 683: the protoc-gen-es TS emission is retired — the FE consumes the
# record → JSON Schema → {TS, Zod} projection in
# modules/ui-web/src/api/generated/schema-types/ (gen-wire-schema-types.mjs),
# not proto-generated types. No TS plugins remain here.
#
# Java emission is unaffected: it is generated via the com.google.protobuf
# Gradle plugin from inside modules/api-contract-projection-java/, not via
# this template.

version: v2
plugins: []
