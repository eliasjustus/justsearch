# i18n catalog for registry-prompt primitive (per tempdoc 429 §E.16 + 434 §8).
# Served at GET /api/messages/registry-prompt/en via MessageCatalogController.
#
# V1 ships an empty catalog; concrete Prompt entries land in a future slice.
