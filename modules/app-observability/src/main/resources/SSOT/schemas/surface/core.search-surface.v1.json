{
  "$comment": "Slice 489 §5 — was the URL-addressable state contract for core.search-surface. Search Thread S5b (the great retirement): the standalone core.search-surface Surface entry is REMOVED from CoreSurfaceCatalog.java (folded into core.unified-chat-surface); no production code loads this resource anymore. Kept as SurfaceStateSchemaLoaderTest's fixture ONLY — it is the one classpath resource demonstrating an integer-typed field (modifiedFromMs/modifiedToMs), which the F3 validator-demonstrable test needs and core.unified-chat-surface.v1.json's schema does not have. Not tied to a live surface; do not re-wire a Surface() entry to this id.",
  "type": "object",
  "properties": {
    "query": {
      "type": "string",
      "$comment": "Free-text search query. Maps to searchState.query via storeId='search'."
    },
    "modifiedFromMs": {
      "type": "integer",
      "$comment": "Inclusive lower bound on modified-time filter (epoch ms). Maps to searchFiltersState.modifiedFromMs via storeId='search.filters'."
    },
    "modifiedToMs": {
      "type": "integer",
      "$comment": "Inclusive upper bound on modified-time filter (epoch ms). Maps to searchFiltersState.modifiedToMs via storeId='search.filters'."
    }
  }
}
