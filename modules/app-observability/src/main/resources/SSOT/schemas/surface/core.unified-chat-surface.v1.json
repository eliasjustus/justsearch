{
  "type": "object",
  "properties": {
    "query": { "type": "string" },
    "docIds": { "type": "array", "items": { "type": "string" } },
    "affordance": {
      "type": "string",
      "enum": ["documents", "extract", "none"]
    }
  }
}
