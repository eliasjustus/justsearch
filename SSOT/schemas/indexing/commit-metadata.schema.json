{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ssot.justsearch/v1/schemas/indexing/commit-metadata.schema.json",
  "title": "Index Commit Metadata (SSOT-aligned)",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_fp",
    "field_catalog_hash",
    "synonyms_hash",
    "grammar_ver",
    "grammar_hash",
    "template_ver",
    "prompt_pack_hash",
    "build_state",
    "commit_id",
    "commit_time"
  ],
  "properties": {
    "schema_fp": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "field_catalog_hash": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "synonyms_hash": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "index_fingerprint": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "SHA-256 over the canonical JSON of the effective physical index shape: catalog schema version, per-field physical projection (type/stored/docValues/multiValued/analyzer/roles/vector dimension+similarity), analyzer fingerprint, vector storage format, HNSW build parameters, chunking parameters, and the embedding + SPLADE model digests. The one rebuild-requiring parity key. Absent when a configured model's digest could not be resolved, because an indeterminate input must not be stamped as an identity (tempdoc 915)."
    },
    "index_fingerprint_inputs": {
      "type": "string",
      "minLength": 2,
      "description": "The canonical JSON that index_fingerprint is the SHA-256 of, stamped verbatim. Present even when index_fingerprint is absent: when a configured model's digest is unresolvable the digest cannot be computed truthfully, but every input that model does not touch (field shapes, vector dimension/similarity, analyzers, chunking, HNSW, analysis library versions) is still known, and the parity guard compares those instead of declining outright (tempdoc 931 C.5). Not a parity key of its own — it is the same statement as index_fingerprint, and comparing both would report one shape change twice."
    },
    "similarity_fp": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "boosts_fp": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "dag_hash": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "pipeline_budget_profile": { "type": "string", "minLength": 1 },
    "grammar_ver": { "type": "string" },
    "grammar_hash": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "template_ver": { "type": "integer", "minimum": 0 },
    "prompt_pack_hash": { "type": "string", "pattern": "^[a-f0-9]{64}$" },
    "grammar_on": { "type": "boolean" },
    "build_state": { "type": "string", "enum": ["BUILDING", "COMPLETE"] },
    "commit_id": { "type": "string", "minLength": 1 },
    "commit_time": { "type": "string", "format": "date-time" },
    "embedding_model_sha256": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "SHA-256 fingerprint of the embedding model GGUF file used to generate vectors in this index. Optional for legacy indexes; when present, vector/hybrid queries require a matching current model."
    },
    "vector_format": {
      "type": "string",
      "enum": ["float32", "int8_sq"],
      "description": "Vector storage format used in this index. float32 = standard HNSW, int8_sq = scalar-quantized HNSW."
    },
    "splade_model_sha256": {
      "type": "string",
      "pattern": "^[a-f0-9]{64}$",
      "description": "SHA-256 fingerprint of the SPLADE sparse encoder ONNX model. When present, indicates the sparse encoding model version used for this index."
    }
  }
}


