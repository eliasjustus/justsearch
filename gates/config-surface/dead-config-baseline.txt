# Dead-config baseline — tempdoc 799 §O.3. One entry per line: `key:<key>` or `component:<name>`.
# These are settings that resolve but nothing reads. They are RECORDED, not blessed: the gate
# fails on any NEW one. Shrinking this list is the point; it never ratchets up without a changeset.
#
# Left in place rather than deleted because each is a wire-it-or-withdraw-it product call, the same
# judgement 754 deferred — the gate's job is to stop the list GROWING while those calls are made.
key:justsearch.embed.dimension
key:justsearch.path_resolution.retention_days
key:search.facets.enabled
key:search.hybrid.ann_k
key:search.hybrid.auto_embed
key:search.hybrid.bm25_k
key:search.rerank.enabled
key:search.rerank.k
key:search.rerank.reduced_k
#
# ---- history ----
#
# 2026-07-31 (799 §R) — `component:simulatedLatencyMs` REMOVED, and the block that used to list ~10
# settings as "undetectable by the scanner" is gone with it. The whole ResolvedConfig.Llm record was
# retired: it was a dead parallel of the live ResolvedConfig.Ai record and nothing read it. Unread
# components are now 0, so the scanner's known blind spot has no live instance — the limitation is
# still real and still documented in dead-config.mjs, it just no longer hides anything here.
#
# 2026-07-31 (799 §Q.2) — `key:justsearch.fieldCatalog` REMOVED: it was never dead. It is read via
# EnvRegistry.FIELD_CATALOG at JustSearchConfigurationLoader.java:116. A false positive caused by
# the scanner excluding the entire configuration package from its reader corpus.
