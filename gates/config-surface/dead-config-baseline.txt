# Dead-config baseline — tempdoc 799 §O.3. One entry per line: `key:<key>` or `component:<name>`.
# These are settings that resolve but nothing reads. They are RECORDED, not blessed: the gate
# fails on any NEW one. Shrinking this list is the point; it never ratchets up without a changeset.
#
# Measured 2026-07-31 after 799 withdrew 22 shadowed knobs. Left in place rather than deleted
# because each is a wire-it-or-withdraw-it product call, the same judgement 754 deferred — the
# gate's job is to stop the list GROWING while those calls are made.
component:simulatedLatencyMs
key:justsearch.embed.dimension
key:justsearch.path_resolution.retention_days
key:search.facets.enabled
key:search.hybrid.ann_k
key:search.hybrid.auto_embed
key:search.hybrid.bm25_k
key:search.rerank.enabled
key:search.rerank.k
key:search.rerank.reduced_k
#
# NOT LISTED, because the scanner cannot currently see them (tempdoc 799 §Q). ResolvedConfig.llm()
# is never called in production, so the whole Llm record is unreachable and ~10 documented settings
# behind it are false promises — justsearch.llm.{min_p,rng_seed,temperature,top_p,threads,
# context_length,max_new_tokens,deadline_ms,model_sha256} plus simulatedLatencyMs (the only one
# above, because its name is unique). Bare-name matching masks the rest. Surfaced for an owner
# wire-it-or-withdraw-it decision rather than deleted here; see 799 §Q.
#
# REMOVED 2026-07-31: justsearch.fieldCatalog was never dead — it is read via
# EnvRegistry.FIELD_CATALOG at JustSearchConfigurationLoader.java:116. It was a false positive
# caused by the scanner excluding the entire configuration package from its reader corpus.
