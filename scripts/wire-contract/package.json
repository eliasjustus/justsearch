{
  "name": "wire-contract-tooling",
  "private": true,
  "version": "0.0.0",
  "description": "Pinned buf CLI for the JustSearch wire-Category contract. Used only by the `wire` governance gate (`buf breaking`).",
  "type": "module",
  "devDependencies": {
    "@bufbuild/buf": "1.69.0"
  }
}
