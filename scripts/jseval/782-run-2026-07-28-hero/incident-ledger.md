# 782 hero campaign incident ledger — run 2026-07-28
- 0f LAUNCH AUTHORIZATION: granted in advance by the founder's 2026-07-28 delegation, verbatim: "ive decided to let you takeover the work as i want you to run the remaining work autonomously end to end now... you can make all relevant decisions yourself" + "you shouldnt require my input for anything from now on" (sleeping, sole-agent session). Recorded here per §E.6; publication (623) remains explicitly NOT covered and stays founder-only.
- 60-min-kill defusal: tracked utility-run task stopped ~2 min in, process tree verified dead, relaunching as detached driver (resume-safe: identical args, same --log-dir; per-cell budgets bound waste at <=6x$0.50)
- s1 FAILED RELAUNCH #1: resume after the 60-min-kill defusal hit the source-identity guard (source_git_state mismatch — driver.out/err/pid + ledger appends changed the untracked set between capture and resume). Guard fired correctly. Remedy: wiped s1 logs/ (3 files, <=1 paid cell <=\$0.50, from the 2-min tracked launch) and restarted as a FRESH run with all driver files pre-created so the untracked set is stable across any future resume. Stop-rule-1 count for s1: 1 of 2.
- s1 VOID RUN (stop rule 5 invoked, resolved by Amendment 2): the eval completed (120/120 samples, status success, $38.87) but EVERY cell carries the mixed-model guard error "resolved provider model changed within one cell: [haiku-4-5, sonnet-5]" — the Claude CLI's background haiku calls, the EXACT 624 Phase-2 amendment-2 incident (624:4689-4703), whose --agent-env mitigation the frozen plan dropped ("No --agent-env", campaign-plan:219). No offline recovery exists (verified: utility-recompose/judge/evidence all fail-closed on metadata.error). Log archived to logs-void-mixed-model-guard/. RELAUNCH COUNTER REASONING (recorded, not hidden): guard-void runs are treated per the 624 precedent as protocol-defect discoveries, not failed relaunches — s1 counter stays 1 of 2 (the identity-mismatch resume). AMENDMENT 2 (pre-any-further-measured-cell, under the founder's 2026-07-28 delegation): (a) add --agent-env ANTHROPIC_DEFAULT_HAIKU_MODEL=claude-sonnet-5 and CLAUDE_CODE_SUBAGENT_MODEL=claude-sonnet-5 to step f UNIFORMLY across all three strata (attribution pinning, not an estimand change; agent_cohort_key does NOT detect mixing, so uniformity is enforced by this record); (b) max_budget re-derived per the FROZEN clamp rule using the now-measured per-cell cost distribution (p95 right-censored at the $0.50 floor cap; 23/120 capped, B 17/60 vs A 6/60) -> ceil2(1.6 x 0.50) = $0.80. Verification before spend: 1-cell smoke asserting a single resolved model on CLI 2.1.220. Masking defect (guard destroys exhaustion labels, agent_utility_inspect.py:1404 vs :1424) logged for post-campaign fix.
- s2 pre-cell abort (no spend): orchestrator launched with a WRONG --corpus-signature (middle 40 hex chars fabricated from an ellipsized table — the exact premise-verification failure class); killed pre-cell, logs empty, relaunched with the signature READ from frozen cells.v1.json. Not counted as a failed relaunch (no run identity was minted, no cell spent).

## 2026-07-28 — Step 4 (judge overlay) and Step 5 (close-out compose)

- **Step 4 SUBSTRATE — root cause of the two `llm=True` start failures (diagnosed, fixed operationally, no engine code touched).** Both attempts died in `backend.py:251` with *"LLM inference did not become available: inference stayed offline"*. The exe path and models dir were NOT the cause (the 12:06 boot resolved `justsearch.server.exe` to the main checkout's cuda12 binary, `headless-backend.log:639`). The real chain: `-Pllm=true` only sets `JUSTSEARCH_AI_AUTOSTART_ENABLED` (`modules/ui/build.gradle.kts:2200-2205`), which makes `InferenceWiring.seedAutostartSpec` call `RuntimeSpecStore.seedAutostartIfUnset()` -> `settingsStore.setChatEnabled(true)` (`RuntimeSpecStore.java:61-70`). **Under eval mode the settings store is IN_MEMORY/read-only** — `POST /api/settings/v2` returns `409 SETTINGS_READ_ONLY` — so that write silently no-ops while the boot log still prints *"AI auto-start seeded runtime spec chatEnabled=true (fresh profile)"*. `RuntimeReconciler.reconcileToSpec` then computes `effective = spec.chatEnabled() && policyOnlineEnabled()` (`RuntimeReconciler.java:498`) as **false**, so it never converges the engine UP and never spawns llama-server. Live confirmation: `GET /api/settings/v2` -> `chatEnabled: null` *after* the seed ran; `/api/inference/status` -> `mode=offline, starting=false, generation=0`; **zero** `RuntimeReconciler` lines in the head log (a silent at-spec settle, not a failed spawn). Two dead ends ruled out by probe, not by guess: `POST /api/ai/runtime/activate` fails `RUNTIME_VARIANT_NOT_INSTALLED` (`installedVariants: []` — that path wants an installed variant pack, not a dev `native-bin` tree), and `POST /api/inference/mode {"mode":"online"}` returns `200 {"success":true,"mode":"offline"}` and does nothing, because the reconciler owns the engine and would converge it straight back DOWN to a spec that says chat-off.
- **Operational fix adopted:** run llama-server **out of band** on the configured port 8081 and let the Head proxy to it. `OpenAiCompatController.proxy` is an unconditional pass-through to `llamaServerPortSupplier` with **no engine-mode gating** (`OpenAiCompatController.java:115-125`), so `/v1/models` + `/v1/chat/completions` serve correctly through the Head at 33221 while the Head's own engine stays offline. Spawn args mirror `LlamaServerOps.startLlamaServer` (`--jinja --host 127.0.0.1 --metrics --port 8081 -c 4096 -ngl 99`). `serve-judge-backend.py` updated to `llm=False` + a fail-closed `/v1/models` readiness assert, so the `judge-serve.ready` marker now means *the judge substrate actually answers* rather than *the Head's engine mode flipped*. Validity check before use (both polarities, pre-registered): the judge's exact request shape (`max_tokens: 4`, `chat_template_kwargs.enable_thinking=false`) returned `YES` on a correct candidate and `NO` on an incorrect one. Without that kwarg the model reasons and returns EMPTY `content` — which `utility_judge._judge_once:93-94` would have silently scored as "not YES", i.e. a fake `judge_flips: 0`. Confirmed not hit: `call_failures: 0`, `degraded_to_em: false` on all three overlays.
- **Step 4 RESULT — three real overlays, no degradation.** Judge identity on all three: `kind=hybrid-em-llm, model=Qwen_Qwen3.5-9B-Q4_K_M.gguf, prompt_hash=6e84a3d0…` — a different model family from the sonnet agent under test, so the self-preference control is intact. enron-1k: EM-pass 50, judged-misses 61, flips 2, disagreements 5, agreement 0.9180. enron-10k: EM-pass 48, judged-misses 63, flips 2, disagreements 3, agreement 0.9524. legal-clerc-1k: EM-pass 43, judged-misses 62, flips 0, disagreements 2, agreement 0.9677. (Contrast 624, which measured 246 judged misses and **zero** flips across three corpora; this campaign's 4 flips are within the same regime.)
- **Step 5 BLOCKED — fail-closed cohort split (§E.6 stop rule 4). No composed record exists; `combined/` and `derived/` are empty.** The single frozen recompose raised `UtilityComposeError: runs are not one harness cohort (agent_cohort_key differs): ['4a6f7c9e99d8', '54325b49ffae', '6eb1a02b382c']`. Cause, read off the three per-cell `cohort` blocks: everything identity-bearing matches across strata — `cli_version`, `mcp_tool_surface_hash` (no fatal tool-surface split), `prompt_template_hash`, `judge`, `decoding`, `eval_limits`, `environment`, `search_config_cohort_key`, `git_sha` — but **`source_git_state` differs**: `untracked_count` 24 -> 35 -> 46 with three distinct `untracked_sha256`, because the campaign wrote its own artifacts into the working tree between strata. `agent_cohort_key` folds `source_git_state` (`agent_manifest.py:180`), so the three strata were measured in three different harness identities. `git_dirty: true` on all three cells also violates §E.2 R1 outright. This is the SAME failure class as the s1 FAILED RELAUNCH #1 above, whose remedy was scoped per-stratum (stable untracked set *within* a resume) and therefore never protected the *cross-stratum* invariant. **A resume cannot repair it** — the untracked sets are already baked into completed logs — and per §E.6 *"Never hand-patch an identity to get past a guard"* no identity was edited. Campaign STOPS here with no promoting artifact; re-measuring all three strata inside one clean-tree cohort window is founder-gated (paid, against the $300 cap).
- **§E.5 acceptance checklist NOT EVALUABLE.** It is defined as "read off the composed record, not re-derived"; no composed record was produced, so every item is *blocked*, not pass and not fail. The three per-cell records each read `claim_verdict.status: rejected, outcome: inconclusive` — expected and non-diagnostic, since each single-stratum record cannot satisfy the `all_required` strata rule on its own.
- Teardown clean: `judge-serve.stop` -> `judge-serve.stopped`, out-of-band llama-server stopped, ports 33221 + 8081 free, no residual head/worker java processes. No commits made.
- CAMPAIGN WINDOW 2 (2026-07-28): window 1's compose failed closed on agent_cohort_key split — source_git_state.untracked_count moved 24->35->46 because the run dir lived INSIDE the tracked tree; git_dirty was true on all cells (S E.2 R1 violated); the between-strata R1 re-assertion prescribed by step g was NOT executed by the orchestrator after s1/s2 (owned: would have caught it ~$90 earlier). Unrepairable in the existing logs (identity is baked; never hand-patch). Window 2 under identical frozen parameters: run dir relocated to gitignored tmp/ (verified: agent_utility_inspect.py:1560 uses git ls-files --others --exclude-standard, so ignored paths never enter the fingerprint; datasets/ junctions likewise ignored), calibrations REUSED (same git_sha bea1ac37 + CLI 2.1.220, 624 precedent), R1 porcelain-empty asserted mechanically BEFORE EVERY stratum launch. Window-1 evidence preserved at tmp/782-run-2026-07-28-hero-window1/. Spend: window 1 ~=\$130 sunk; window 2 projected ~=\$110; cumulative within the \$300 cap.
- w2-s1 FAILED RELAUNCH #1 (externally induced — serve substrate killed mid-run): the 12:38:46 confirm launch (driver detached, PID 8124) ran against a serve backend that was still hosted as a HARNESS-TRACKED background task; the harness killed that task mid-run (same kill class as the 60-min tracked-task kill, agent-lessons.md), taking the head/worker children with it. Driver kept running against a dead port: 110/120 samples touched, 98 samples with connection-refused tool failures, no out/ record, killed pre-finalize at 13:05 (orchestrator Stop-Process 8124 after health probe confirmed 33221 dead, no serve-eval-backend.py process, no serve.failed marker — external kill, not a crash). Spend: unrecoverable exactly (log killed pre-finalize; agent-arm spend rides the CLI, not Inspect events); bounded above by the $36 calibration estimate, realistic ~$10-20 (most samples failed fast on refused connections). Poisoned log archived to en-email-enron-raw-1k-verbose/logs-void-backend-death/. Stop-rule-1 count for w2-s1: 1 of 2. ROOT-CAUSE FIX for all remaining strata: serve-eval-backend.py now launches DETACHED (Start-Process, serve.pid/out/err files) exactly like the driver — the detached driver survived the same window the tracked serve died in, which is the direct evidence for the fix. Watcher upgraded to also probe backend health (the previous watcher covered only driver states, so a dead backend was silent until sample failures).
- w2-s2 pre-cell abort (no spend, not a failed relaunch — s2-precedent: no run identity minted, logs/ and out/ empty): the s2 driver failed closed in assert_watched_roots_scoped (StrayWatchedRootError) because the persistent eval dataDir still carried s1's watched root (roots are accretive, RootLifecycleOps.addWatchedRoot; the s2 serve boot at 13:38 even re-indexed the stale 1k root alongside the 10k one). Guard's own printed remedy applied: DELETE /api/indexing/roots on the stray 1k root (response deletedJobs=1001 — the 1k documents were purged with the root, so no cross-corpus pollution remains), verified single remaining root = the 10k corpus-dir, ingest safety re-check green before relaunch. Root cause is serve reuse of one dataDir across strata without --clean; the guard chain is doing exactly its job. (How window 1's s2/s3 chain avoided this was not re-verified here — its serve invocations are preserved under tmp/782-run-2026-07-28-hero-window1/ if it matters.) Relaunch follows with identical frozen args.

## 2026-07-28 — Window 2 close-out (steps 4-5)

- Step 4 judge overlays x3: out-of-band llama on 8081 through the live s3 Head proxy (window-1 recipe), polarity pre-checked both ways (YES on correct, NO on incorrect, enable_thinking=false). Results: enron-1k EM-pass 44/judged-misses 71/flips 1/agreement 0.8873; enron-10k 41/63/0/0.9841; legal-1k 29/74/4/0.9459. call_failures 0 and degraded_to_em false on all three. First overlay attempt as a harness-tracked background task was killed externally (~6 min in, nothing written — utility-judge writes atomically); relaunched as a detached cmd driver with status markers, completed clean. Same kill class as the serve incident; every long-running process in this campaign now runs detached.
- Step 5 THE COMPOSE SUCCEEDED: one invocation, three strata, one cohort (agent_cohort_key identical across strata — the gitignored run dir closed window 1's identity split). combined/utility-comparison-cross-corpus.v1.json, semantic_digest e2bb70c31ece7bdbb665529d2d22334701a6910623d42af8a24957ac16be2ed3.
- VERDICT AS RECORDED: claim_verdict.status=rejected, accepted=false, outcome=inconclusive, arm=addition_b. Per-stratum outcomes: adoption-only on ALL THREE strata. 29 of 30 policy gates pass — verified_tool_surface rate 1.0 (180/180, single hash), no_leak_suspect_cells 0, minimum_adoption_rate observed 1.0, closed_book_at_hero_tier pass, completion_triple_reported pass, schema_strata_reported pass.
- FREEZE DEFECT #2 (recorded, escalated, NOT patched): the single failing gate is corpus_certification_complete, and it fails because utility_claim_policy.py:414-423 requires cert.query_count == cell.query_count exactly; the 781 certifications certify the 50-query committed gold set while the frozen §E.1 design runs the pre-registered 20-qid subset (cells.v1.json qid_list_sha256 / selected_query_sha256). 50 != 20 on every stratum. No run under this frozen design could ever pass this gate — same defect class as BLOCKER-1 (caught at freeze) but this one was only reachable at compose. gold_sha DOES match (the query identity chains the full committed set); only the count comparison fails.
- CODE-CERTAIN COUNTERFACTUAL (read from utility_claim_policy.py:852-867, not re-run): with validity_passed true, control flow lands outcome="adoption-only", accepted=true, status="accepted". I.e. absent the 50-vs-20 defect this record reads ACCEPTED / ADOPTION-ONLY: the sonnet agent adopts the JustSearch MCP tool at rate 1.0 when offered, with no measurable accuracy benefit (and point-negative deltas on enron) at n=60/stratum.
- Per §E.6 stop rule 5 the amendment window is closed: fixing the gate (policy v4, subset-aware via selected_query_sha256) and re-composing OFFLINE (no re-measurement, $0) is a founder decision. Never hand-patch an identity/verdict to get past a guard — the record ships as rejected/inconclusive with this analysis beside it.
- Measured deltas (95% CI beside exact-McNemar p, power-honesty: no effect detected at n=60): enron-1k -0.1964 [-0.375,-0.018] p=0.063 (fixes 9, breaks 20); enron-10k -0.1304 p=0.146 (fixes 3, breaks 9); legal-1k +0.0222 p=1.0 (fixes 7, breaks 6). Window-1 signs reproduced on all three strata (-0.154/-0.093/+0.022) — two independent windows agree.
- Teardown: llama 8081 stopped, s3 serve stopped, ports 33221/8081 free.
- §E.4 derivation (post-compose, offline, $0): all five derived artifacts written reproducibly by derive-e4.py (committed beside them). Honest gaps recorded instead of imputed: (1) the RANK-OF-GOLD INSTRUMENT WAS DEAD for this run — 633/635 B-arm search calls captured gold_rank null because the MCP delivery emits per-hit identity as `path` while _gold_rank_capture reads only `h.get("id")` (agent_utility_inspect.py:921); legal-1k partially exempt (79/199 calls carried an id). §E.4 item 1 is unrecoverable offline (raw tool content never committed) and item 3(b) with it. Logged for the post-campaign harness-fix list next to the mixed-model-guard masking defect. (2) Span carriage is high and derivable: B-cell delivered-payload span share 0.867/0.800/0.883 per stratum. (3) Exhaustion ledger: all 38 exhaustions USD-type, zero wall-clock, zero null-cost cells, cross-checks against per_arm_loss agree in all six arms. (4) Duration ledger right-censoring flagged in all six arms (B 10k censored share 0.200 — the budget-asymmetry signature again).
