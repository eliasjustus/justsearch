{
  "tool": "justsearch_status",
  "delivered_tier": "prose",
  "provenance": "reconstructed-from-source, not recorded",
  "provenance_note": "No raw capture survived on disk (same gap as the other fixtures). justsearch_status never emits a structuredContent channel at all -- McpToolSurface.callStatus() (modules/ui/src/main/java/io/justsearch/ui/api/mcp/McpToolSurface.java:908-945) returns only `content: [{\"type\":\"text\",\"text\": <sb.toString()>}]`, no structuredContent key -- so this fixture's text body is the sb.toString() template reconstructed line-for-line from that method (lines 915-937): state/ready/documents/queueDepth/healthy/indexState always present; embeddingCoverage/spladeCoverage/nerCompleted only when the corresponding `status.extras()` key is a Number (lines 922-937). OPEN CAVEAT (not settled by the tempdoc 735 probe, which covered answer/search/status only at the delivered_tier level, not the delivered content SHAPE for a no-structuredContent tool): the probe's own language for this case (\"tools WITHOUT structuredContent (status) deliver prose text blocks\", tempdoc 735 line 288) is compatible with either a flattened `content: str` (this fixture's assumption, tier \"prose\") or an unflattened `content: [{\"type\":\"text\",...}]` single-block list (tier \"blocks\", like sdk_block_list.json in this directory) -- the raw-SDK debug probe was not re-run against a no-structuredContent tool specifically to disambiguate. This fixture takes the str reading because the tempdoc 735 W2 brief explicitly asked for a distinct 'prose text' fixture separate from the block-list one; --write-fixtures against a live stack (see this directory's README) is the way to settle which shape the CLI actually forwards for this case.",
  "cli_version": "2.1.209",
  "mcp_tool_surface_version": "0.3.1",
  "stamped": "2026-07-14",
  "result": {
    "is_error": false,
    "content": "state: RUNNING\nready: true\ndocuments: 128\nqueueDepth: 0\nhealthy: true\nindexState: READY\nembeddingCoverage: 100.0%\nspladeCoverage: 100.0%\nnerCompleted: 128 (0 pending)\n"
  }
}
