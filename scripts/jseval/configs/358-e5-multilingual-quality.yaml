## 358: multilingual-e5-base quality test (FP32 — FP16 conversion failed)
dataset: scifact
modes: [lexical, bm25_splade, full]
embedding: true
splade: true
pipeline: true
output_dir: tmp/eval-results

gpu:
  enabled: true
  embed:
    enabled: true
  splade:
    enabled: true
  ner:
    enabled: true

backend:
  clean: true

env:
  JUSTSEARCH_INDEX_SCHEMA_MISMATCH_POLICY: REINDEX
  JUSTSEARCH_EMBED_ONNX_MODEL_PATH: D:\code\JustSearch\models\onnx\multilingual-e5-base
