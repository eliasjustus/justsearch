// Fixture: a declared operation-lifecycle surface that references the canonical IndexingJobView.
import type { IndexingJobView } from '../api/indexing.js';
export const job: IndexingJobView | null = null;
