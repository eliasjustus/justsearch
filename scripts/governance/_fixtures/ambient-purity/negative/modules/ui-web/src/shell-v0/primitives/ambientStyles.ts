// The authority sheet (defines the facet, so positive-coverage (b) passes — the
// negative cause below is isolated to the component, not a missing facet).
export const ambientStyles = `::selection { background: var(--accent); }`;
