{
  "$comment": "Fixture (negative): a projection names a test: guard that does not resolve (dangling); a source declares a bare self, which is no longer representable (tempdoc 576 §3.1 rung-1 invalid-guard-form); and downgraded-consumer weakened a real test: guard to exempt: vs _baseline/ — the §5 no-silent-downgrade detector flags it (silent-guard-downgrade), proving that path bites in fixtureMode.",
  "surfaces": [
    { "id": "dangling-projection", "kind": "projection", "guard": "test:DoesNotExistConformanceTest" },
    { "id": "bare-self-source", "kind": "source", "guard": "self" },
    { "id": "downgraded-consumer", "kind": "consumer", "guard": "exempt:dropped the conformance test" }
  ]
}
