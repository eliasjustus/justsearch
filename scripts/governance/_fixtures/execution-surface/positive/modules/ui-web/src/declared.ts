// Fixture: a declared execution surface that references the canonical SearchTrace record.
import type { SearchTrace } from '../api/generated/index.js';
export const trace: SearchTrace | null = null;
