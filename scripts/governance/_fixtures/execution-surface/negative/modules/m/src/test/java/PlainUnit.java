// Self-test fixture (negative): a resolving but NON-conformance test (name has no
// Conformance/Projection/searchTrace signal) — so test:PlainUnit resolves (not dangling) yet fails
// Check 6's conformance-guard requirement for the PlainProjection surface.
class PlainUnit {}
