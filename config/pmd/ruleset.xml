<?xml version="1.0" encoding="UTF-8"?>
<ruleset name="JustSearch PMD Ruleset"
         xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.net/ruleset_2_0_0.xsd">
  <description>
    JustSearch PMD ruleset - Focus on bug prevention and thread safety.
  </description>

  <!-- Codestyle (existing) -->
  <rule ref="category/java/codestyle.xml/UnnecessaryFullyQualifiedName"/>

  <!-- Error-prone patterns -->
  <!-- AvoidBranchingStatementAsLastInLoop removed: flags valid patterns like break-after-find and early return -->
  <rule ref="category/java/errorprone.xml/BrokenNullCheck"/>
  <!-- CompareObjectsWithEquals removed: too many intentional identity comparisons (Thread, Throwable cause checks) -->
  <rule ref="category/java/errorprone.xml/DoNotTerminateVM"/>
  <rule ref="category/java/errorprone.xml/EmptyCatchBlock">
    <properties>
      <property name="allowCommentedBlocks" value="true"/>
      <property name="allowExceptionNameRegex" value="^expected$"/>
    </properties>
  </rule>
  <rule ref="category/java/errorprone.xml/EqualsNull"/>
  <rule ref="category/java/errorprone.xml/ImplicitSwitchFallThrough"/>
  <rule ref="category/java/errorprone.xml/MisplacedNullCheck"/>
  <rule ref="category/java/errorprone.xml/OverrideBothEqualsAndHashcode"/>
  <rule ref="category/java/errorprone.xml/ReturnFromFinallyBlock"/>
  <rule ref="category/java/errorprone.xml/UnconditionalIfStatement"/>
  <rule ref="category/java/errorprone.xml/UselessOperationOnImmutable"/>

  <!-- Multithreading -->
  <rule ref="category/java/multithreading.xml/AvoidThreadGroup"/>
  <rule ref="category/java/multithreading.xml/DoubleCheckedLocking"/>
  <rule ref="category/java/multithreading.xml/NonThreadSafeSingleton"/>

  <!-- Security -->
  <rule ref="category/java/security.xml/HardCodedCryptoKey"/>
  <rule ref="category/java/security.xml/InsecureCryptoIv"/>

  <!-- Best practices - unused code detection -->
  <rule ref="category/java/bestpractices.xml/UnusedLocalVariable"/>
  <rule ref="category/java/bestpractices.xml/UnusedPrivateField"/>
  <rule ref="category/java/bestpractices.xml/UnusedPrivateMethod"/>
  <rule ref="category/java/bestpractices.xml/UnusedAssignment"/>
  <rule ref="category/java/bestpractices.xml/UnusedFormalParameter"/>

  <!-- Logging hygiene: all output should go through SLF4J -->
  <rule ref="category/java/bestpractices.xml/SystemPrintln"/>

  <!--
    Tempdoc 930 chunk F — the Java half of the retired `todo-fixme` kernel gate. A parked marker
    is a deferred fix nobody scheduled (CLAUDE.md `log-pre-existing-issues`): fix it now or route it.
    PMD parses, so unlike the gate's raw-text scanner this cannot score the letters in an
    identifier or a URL. The TypeScript half is ESLint `no-warning-comments` in
    modules/ui-web/eslint.config.js.

    No `message` override here: CommentContentRule reports with its own per-violation message
    ("Line matches forbidden content regex (...)"), so a rule-level message attribute is silently
    ignored. Verified by planting `// TODO ...` in modules/app-util and running
    `./gradlew.bat :modules:app-util:pmdMain -PskipPmd=false` (2 violations, BUILD FAILED).

    SCOPE (930 §22.2 follow-up 2 closed the gap this comment used to record): `pmdMain` now runs
    in `check`/`build` by default and as the `Static analysis (PMD)` step of CI's required
    `Build (no model blobs)` job, so the whole ruleset bites without anyone opting in. The 130
    pre-existing violations of the other rules above were cleared in the same change.

    Java TEST sources are still out of scope — `pmdTest` stays behind `-Ppmd.includeTests=true`
    (JvmBaseConventionsPlugin), so a marker parked in a test file is caught by review, not here.
  -->
  <rule ref="category/java/documentation.xml/CommentContent">
    <properties>
      <property name="forbiddenRegex" value="(?i)\b(todo|fixme|xxx)\b"/>
    </properties>
  </rule>
</ruleset>
