See CLAUDE.md for project instructions.

Codex note: Claude Code project slash skills and hooks do not run in Codex.
To orient at session start, use the personal Codex skill `$justsearch-start`
if available, or manually read `CLAUDE.md`, `docs/llms.txt`, and
`docs/explanation/01-system-overview.md` before summarizing the repo
orientation, then run `node scripts/agent-analytics/world-state.mjs` for the
live picture of worktrees, sibling sessions, and the next free tempdoc number.
