<!--
  JustSearch mark - the 24px raster frame, light-ground colorway. HAND-SNAPPED; geometry identical
  to mark-24-dark.svg (see that file for the full snap rationale and the three edges that moved).

  Shipped so that a light-ground 24px raster surface has a file to derive from instead of scaling
  the 16-unit cut and inheriting its half-pixel edges.
-->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" width="24" height="24"
     shape-rendering="crispEdges" role="img" aria-label="JustSearch">
  <path id="mass" fill="#16181c" d="M2,3 H15 V9 H8 V15 H15 V21 H2 Z"/>
  <rect id="answer-line" x="16" y="9" width="6" height="6" fill="#00655B"/>
</svg>
